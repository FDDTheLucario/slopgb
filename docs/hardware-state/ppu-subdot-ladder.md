# PPU — sub-dot event-phase ladder (half-dot / eighth grid)

Lifts the class-H/A sub-dot floor incrementally: keep the pixel pipe dot-clocked, add sub-dot resolution only on the EVENT layer (observers sample at their own phase, events commit at their own phase). gambatte hwtests evaluate at a cc+2 access offset; our tick-then-access is cc+4 (M-cycle end); gbmicrotest (also hardware) pins cc+4 — the model must reproduce both. Full per-increment history (incl. dead-ends) is in memory `slopgb-subdot-eventphase-ladder.md`.

- **PHASE B (the cc-exact reclock) — ATOMIC-CORE ARCHITECTURE FIXED + thesis protocol (2026-06-23, mandated). The 9-piece flip decomposes into two RESOLUTION TIERS; Tier 2 is the structural root and the make-or-break.** Six-agent seam-map workflow (`wf_dd561556`) + first-hand T-by-T trace of SameBoy `sm83_cpu.c` dispatch confirmed the organizing fact:
  - **Tier 1 (whole-M-cycle leading-edge reads) is DONE + sufficient** for FF41 + OAM/VRAM/palette accessibility + the kernel pair. Mechanism: `leading_edge_sample` (`interconnect/cycle.rs:16`) reads the PPU *before* `tick_machine` (`interconnect.rs:706`) → cc+0. The discarded `clock.read()`/`clock.write()` returns are pure bookkeeping; consuming them changes nothing while `tick_machine` advances the PPU in flat 4-dot quanta.
  - **Tier 2 (sub-M-cycle dispatch reclock) is the BLOCKER and the thesis.** The interrupt-dispatch retime is **−2 T = half an M-cycle** (`dispatch_vector_retime`, `cycle_clock.rs:149`: `pending-=2; flush; pending=2`). Traced SameBoy: after the low push the IF-ack/vector latches at M5+2 dots, and the **`pending=2` re-park shifts the vector fetch AND the first handler instruction's reads 2 dots earlier — until the instruction-boundary flush** (`step` → `bus.flush_pending`, `cpu/execute.rs:21`). That is the "re-frames every read" mechanism. It REQUIRES the PPU (and timer/APU/serial) advance to be slaved to the clock's `pending` (deferred-commit), which is **B1** (split `tick_machine` `for cc in 1..=4`, `interconnect/tick.rs:46`, into pre/post-commit spans driven by the clock; needs `timer/apu/serial` `tick(dots)` variants, byte-identical at full-M-cycle). Switching the PPU advance eager(+4/op)→deferred(advance-prev-park) shifts the absolute frame one M-cycle → invalidates every boundary calibration (`vis_early`/`mode3_entry_dot`/A8 dot-254 dispatch) → **B1+B2+B3+recalibration must land together as the atomic core (flag-off byte-identical trivially since flag-gated; flag-on is red until convergence).**
  - **Why this is NOT yet refuted:** the 2026-06-21 "DISPATCH-RECLOCK REFUTED" measured the PPU-side dispatch move (B3, 254→252) *alone* → m0int 0→3, intr_2_0 PASS→FAIL. The thesis claims the CPU-side retime (B2, the `pending=2` reframe) is the missing half. **B2 has never been wired** (`dispatch_vector_retime` is dead outside `cycle_clock_tests`). So B2+B3 together is the untested make-or-break.
  - **THESIS TEST (next session, after B1+B2+B3 flag-gated):** measure the triad flag-on via `run_gambatte`/`run_mooneye` (fast, no full matrix) — kernel `m0int_m3stat_2`=0 ∧ `m2int_m3stat_1`=3 ∧ mooneye `intr_2_mode0_timing`(+`_sprites`) PASS ∧ gbmicrotest `int_hblank_halt` PASS. All co-hold ⇒ the A8 mutual-exclusion (m0int=0 forces intr_2 FAIL in cc+4) dissolves = thesis CONFIRMED ⇒ proceed to A1/A2/A4/B4/B5/flip. m0int=0 still costs intr_2 ⇒ REFUTED, dispatch stays 254, pin residual + revert B-group + keep A-group. Distinguish a wiring bug from a real refute by confirming gbmicrotest/mooneye2022 counters moved as predicted.
  - **THESIS RESULT (2026-06-23 #6) — PARTIAL CONFIRM: the A8 mutual-exclusion DISSOLVES, but via B1+B3a (NOT B2+B3), and the full triad has two residuals.** Wired the whole B-group flag-gated behind a NEW `tier2_reclock` flag (separate from `leading_edge_reads`, so the green S0 specs run leading-edge-only and stay on the validated Tier-1 frame — keeps the gate exit-0 each step). Pieces, all byte-identical OFF:
    - **B1 (the structural foundation, banked) — deferred-commit machine advance.** `Interconnect::advance_machine_t(from_t,to_t)` (`interconnect/tick.rs`) advances PPU/timer/APU/serial T-by-T slaved to the deferred-commit clock's just-paid debt instead of a flat 4-dot quantum; the eager `for cc` body is extracted to `tick_machine_dot(cc)` (shared, byte-identical), the timer to `tick_substep(substep)` (+`begin_mcycle`, byte-identical, pinned by `tick_substep_composes_into_tick`). Bus `read/write/tick/tick_addr/read_inc/flush_pending` branch to deferred helpers (`interconnect/cycle.rs`) when `tier2_reclock`. **This is the S2+S3 "deferred-commit reads + full leading-edge" lift the whole port needed.**
    - **B3a (the kernel separator, banked) — `early_lead`−2 under tier2** (`ppu/render/mode0.rs`). The deferred frame samples the kernel reads at line-1 dots 252 (m2int) / 256 (m0int) (instrumented); the cc+4-calibrated `vis_early` (dot 251) made m2int@252 read mode 0. Lowering `early_lead` by 2 fires `vis_early` 2 dots LATER (a lower `proj` threshold fires later as the pipe drains), landing the visible mode→0 boundary in (252,256] → **m2int reads 3 ∧ m0int reads 0 on BOTH models, while `intr_2_mode0_timing` PASSES (both models).** The A8 mutual-exclusion is dissolved. Pinned by gbtr `tier2_kernel_pair_matches_sameboy_target` (flag-on, green in the gate).
    - **B2 (the dispatch retime) — wired but MEASURED INERT.** `Bus::dispatch_retime`/`dispatch_reclock` forward `clock.dispatch_vector_retime` (`pending=2`) + move `bus.ack` after the low push (`cpu/execute.rs`, eager branch byte-identical). With `SLOPGB_NORETIME` neutering it, the kernel STILL separates and `intr_2` STILL passes — **B1+B3a do the work; the −2 retime is absorbed by the instruction-boundary flush before any observable read.** The prior "B2 is the missing half" hypothesis is itself disproven. Kept as faithful scaffold (may matter for the global rebaseline / other ROMs).
    - **The goal's prescribed "dispatch 254→252" (mode0.rs:182 `proj<=lead`) NOT implemented — measured WRONG-DIRECTION.** `int_hblank_halt` is already −2 (too low) in the deferred frame; moving the mode-0 IRQ 2 dots EARLIER lowers the count further. And moving `line_render_done` to 252 would re-break the kernel (m2int@252 would read it set). So the dispatch move is not the universal lever the goal assumed.
    - **RESIDUAL 1 — `intr_2_mode0_timing_sprites` FAIL (both models).** The sprite-line mode-0 dispatch lead needs deferred-frame recalibration (the A9 analog: sprite lines use `early_lead 4`→`2` under tier2, evidently the wrong sprite offset). The gambatte sprite *kernel* (`10spritesPrLine_m3stat_1/_2`) DOES separate (3/0) — so it is a dispatch-timing recal, not a read-frame break.
    - **RESIDUAL 2 — gbmicrotest `int_hblank_halt` FAIL (−1/−2), isolated to the deferred HALT-WAKE.** Non-halt mode-0 dispatch (`intr_2`) passes; only the halt path is low (scx0 actual 60 vs 62; the SCX dependence is partly flattened). Root cause: the deferred halt prefetch samples `pending_halt_wake` at cc+0, but SameBoy's `GB_cpu_run` samples the halt exit mid-cycle (cc+2) → the wake fires ~2 dots early. This is the halt-late-mask re-derivation (the goal's B4/B5), NOT a fundamental cc+0/cc+4 tension. (`int_hblank_halt` also fails LE-only, so it is incompatible with leading-edge generally until that mask lands.)
    - **VERDICT: not the REFUTE condition (m0int=0 does NOT cost intr_2 — they co-hold), not a full CONFIRM (sprites + halt-wake residuals + the global rebaseline remain). Banked the byte-identical-OFF B-group (the deferred-commit foundation + the validated kernel lever); did NOT flip the defaults.** Next levers: (a) the deferred halt-wake cc+2 sample (B4/B5, fixes int_hblank), (b) the sprite-line deferred lead (fixes intr_2_sprites), (c) THEN re-measure the full triad → if 4/4, proceed to the global flip + ~7000 rebaseline. Tooling: `SLOPGB_TIER2=1`/`SLOPGB_LE=1` on `run_gambatte`/`run_mooneye`; `SLOPGB_GBMICRO=1` on `run_mooneye` prints `$FF80/81/82`.
  - **THESIS RESULT #7 (2026-06-23) — both residuals SHARP-DIAGNOSED + SameBoy-ground-truthed; STILL PARTIAL CONFIRM (2/4), NO flip. Two prior diagnoses CORRECTED. Both residuals are S5-StatUpdate-engine-recalibration entanglements, not isolated levers.** No code shipped (every experiment flag-gated + reverted; working tree byte-identical to `46f2c20`). New durable tool: `docs/sameboy-port/tools/hramdump.c` (headless SameBoy 1.0.2 HRAM `$FF80/81/82` reader — the stock tester mashes menu buttons + can't read HRAM, so it could never ground-truth gbmicrotest).
    - **`int_hblank_halt` is a PORT BUG, not a hardware contradiction — SameBoy PASSES it.** Built `hramdump.c`, ran `int_hblank_halt_scx0..7` (DMG): SameBoy `$FF80` = 62,62,62,63,63,63,63,64 = the baked expected = PASS on all 8. So the "kernel wants cc+0 FF41 ∧ int_hblank wants cc+4 FF41" fundamental-tension worry is **dissolved**: SameBoy's single cc-exact model holds BOTH. The fix is a port-bug fix, not a fundamental wall.
    - **CORRECTION A — `int_hblank_halt` does NOT read FF41; it measures the TIMER-counted mode-0 STAT IRQ HALT-WAKE.** Source (`aappleby/GBMicrotest tests/int_hblank_halt_scx0.s` + `macros.inc`): `cycle_init`=`TAC=%101` (TIMA /16), `halt` waits the HBlank (mode-0) STAT IRQ, the ISR runs `cycle_count` (4× `add (hl)` on TIMA `$FF05`) → it times the mode-0 IRQ **halt-wake**, never polls FF41. So handoff-#6-top's "int_hblank = the cc+0 FF41 STAT read frame / decoupled-`mode_for_interrupt` lever" reframe is **WRONG**; RESIDUAL-2 above (halt-wake) was right, the goal's "decoupled-mode anchor" lever does not apply.
    - **`int_hblank` decomposition (DMG, env-swept `pending_halt_wake` + `m0_rise` probes + `if_late` late-set sweep): deferred base (no halt mask) = 60,60,60,60,61,61,61,61; target = 62,62,62,63,63,63,63,64 = base + 2 UNIFORM cycles + a mask{rise cc==4}.** Wake position is uniform `FF80 = wake_clk/4 − 41` (vs OFF `−40` = a constant −1 TIMA-phase). THREE entangled factors: (1) the deferred halt prefetch samples `pending_halt_wake` at cc+0 while SameBoy `GB_cpu_run` (`sm83_cpu.c:1625-1632`) samples `interrupt_queue` **mid-cycle on DMG** (`GB_advance_cycles(2)` → sample → `(2)`) / cc+0 on CGB — **but advancing 2 before the sample measured −1 WORSE (5F), not better: advancing the machine before the wake sample makes the IRQ visible EARLIER → wake earlier → LOWER count.** The delay must come from the `if_late` mask (delay the WAKE), not a forward advance; (2) the S5 `StatUpdate` engine raises the mode-0 STAT IRQ **1 M-cycle early** (`int_hblank` is −1 under LE-only too); (3) the dispatch retime (B2/B3a) shifts the ISR `cycle_count` TIMA reads (the constant −1). The deferred `m0_rise` lands at `cc = eager_cc + 1 (wrapping 4→1)` — a cc grid rotation a constant `lead_eighths` cannot undo. **`intr_2` PASS pins the mode-0 raise dot, so it can't move.** All three are S5-engine calibration in the deferred frame, entangled with passing tests.
    - **CORRECTION B — `intr_2_mode0_timing_sprites` is the sprite-line VISIBLE mode→0 (`vis_early`/`early_lead`), NOT the dispatch (`line_render_done`).** Source (`intr_2_mode0_timing_sprites.s`): after the mode-2 OAM IRQ halt-wake + a calibrated NOP delay it **FF41-polls** until mode 0 (`vis_mode()` returns 0 on `line_render_done || vis_early`), to 1-cycle precision per sprite config. The mode-2 wake is identical to bare `intr_2` (which PASSES under T2), so only the sprite-extended mode→0 boundary matters = `vis_early`. **Sweep of the sprite-line `early_lead` (4−sub) under T2: CGB `intr_2_sprites` PASSES at `early_lead=0` (sub 2→4) while the kernel pair stays 3/0 AND sampled gambatte sprite `m3stat_ds_1/_2` stay 3/0; DMG still FAILS at every sub → the sprite `early_lead` is MODEL-SPLIT (CGB wants 0, DMG wants other).** So it IS the `early_lead`/read lever (handoff #6's "NOT early_lead, it's the dispatch" is wrong), but it is entangled: `early_lead` also drives A9's +40 single-speed gambatte sprite `m3stat` lift, so any change needs the full gbtr matrix to prove no regression (only 3 rows sampled here). Not bankable without that.
    - **CORRECTION B refined (2-param `vis_early`+dispatch sweep, `testcase_id` peeked from HRAM `$FF80`): MODEL-SPLIT + a DMG sprite-EDGE residual; not a clean lever.** CGB fully PASSES at `early_lead=0` (`vis=disp=0`). DMG wants the boundary **1 dot later** (`vis=disp=−1`): that advances DMG from failing testcase #1 (1 sprite) all the way to **testcase #34 = "10 sprites at X=167"** (the far-right screen edge) — but DMG then **STICKS at #34 for every further negative offset** (the DMG kernel `10spritesPrLine_m3stat` holds 3/0 throughout; the dispatch is kernel-constrained to `lead ± 1`). So sprites needs (a) a model-split sprite `early_lead`/dispatch (CGB 0 / DMG −1) AND (b) a fix for the DMG X=167 sprite-edge geometry that no uniform boundary offset reaches — a sprite-position-dependent penalty subtlety in the deferred frame — AND (c) full-gbtr validation that the model-split holds A9's +40 lift. Multi-leg, not a single bankable lever. (Both residuals confirmed `SameBoy`-passing: the mooneye source header pins `pass: DMG..AGS`.)
    - **VERDICT #7: both residuals are PINNED port-bugs with identified levers (NOT refuted — the levers move them), but each is a multi-factor entanglement with the S5 `StatUpdate`-engine recalibration in the deferred frame, not an isolable single lever — i.e. they land WITH the global flip's S5 stage, not before it.** 4/4 is unreachable this session; defaults NOT flipped; B-group kept byte-identical OFF (658 lib + clippy `-D` clean; tree == `46f2c20`). Next-session levers: int_hblank = align the deferred halt path to SameBoy `GB_cpu_run` (sample-before-advance, DMG mid-cycle) + re-derive `if_late` for the cc+0 frame + the S5 mode-0-raise −1; sprites = model-split sprite `early_lead` (CGB 0) + full-gbtr A9-lift hold check. Ground-truth: `docs/sameboy-port/tools/hramdump.c`.
  - **THESIS RESULT #8 (2026-06-23) — `int_hblank` L1 RE-DIAGNOSED: the planned factor-2 lever ("S5 mode-0-raise −1") is REFUTED by direct sweep. STILL 2/4 PARTIAL, NO flip. No code shipped (all experiments flag-gated + reverted; tree byte-identical to `79cf8e3`).** Reproduced the triad exactly (kernel 3/0 ✓, intr_2 PASS ✓ both models; sprites FAIL; `int_hblank_halt_scx0` FF80=60 vs 62). Re-instrumented L1 with fresh probes (`SLOPGB_IHPROBE`/`IHPROBE2` on `m0_rise`/`pending_if`/`pending_halt_wake`, `SLOPGB_M0HOLD` to delay the engine mode-0 raise; all reverted):
    - **Confirmed the frame ladder: flag-OFF PASSES `int_hblank` exactly (62,62,62,63,63,63,63,64 = baked target). LE-only = uniform −1 (61,61,61,62,62,62,62,63). TIER2 = 60,60,61,61,61,61,62,62.** So the residual is reclock-induced; production is correct.
    - **Confirmed the deferred cc-rotation (handoff #7 factor 1): the `m0_rise` lands at `cc = eager_cc + 1` (probed scx0 OFF cc4→TIER2 cc1; scx2 OFF cc2→TIER2 cc3).** The `if_late` halt mask (`obs_pre_edge(MID_PHASE, event_phase(M0Rise, cc, 0))` = fires when `cc_eighth(cc) > 4`, i.e. cc∈{3,4}) therefore fires at the WRONG SCX in the deferred frame (TIER2 masks scx2,6; target mask{rise cc==4} wants scx3,7). Mask alone only shuffles ±1 M-cycle — does not supply the +2.
    - **NEW — factor (2) "the S5 StatUpdate engine raises mode-0 1 M-cycle early" is NOT an actionable `int_hblank` lever (REFUTED on the test's own output).** Held the IRQ-facing mode (`mode_for_interrupt`) at 3 for 0..12 EXTRA dots after `line_render_done` (decoupled from `vis_mode`/dispatch, so kernel+intr_2 are untouched — verified: 3/0 ∧ PASS held at every hold). The `pending_if |= IF_STAT` raise dot moved 254→266 (probe `IFRAISE`, mfi=0 stat_en=08), but **`int_hblank` FF80 was BYTE-IDENTICAL across ALL 13 holds (60,60,61,61,61,61,62,62)** — moving the engine mode-0 raise dot up to 3 M-cycles has ZERO effect on the wake count. The wake is M-cycle-quantized to the deferred halt-loop sample grid and masked by `if_late`, NOT determined by the engine raise dot. So "delay the S5 mode-0 raise" cannot fix `int_hblank`; the LE −1 is the LE read/halt FRAME, not a raise-dot any hold reaches. (Caveat: the `M0RISE`/`WAKE` cyc-probe values were early-boot dot-clock, not the measurement halt — the FF80 invariance is the solid ground truth, the cyc probe is not.)
    - **VERDICT #8: the L1 residual is the deferred HALT-LOOP FRAME (handoff factor 1, "advance-before-sample measured WORSE") + the dispatch-retime TIMA phase (factor 3, const −1) + the cc-rotation `if_late` mask — i.e. a faithful re-port of the deferred halt path to SameBoy `GB_cpu_run` (DMG advance-2→sample→advance-2, `sm83_cpu.c:1621-1628`) with the `if_late` mask re-derived for the cc+0 frame, landing atomically with the S5 flip. NOT the engine-raise lever, NOT a one-liner.** L2 (sprites) not re-investigated this session — #7 model-split (CGB 0 / DMG −1) + X=167 edge carried forward. Defaults NOT flipped; B-group byte-identical OFF (658 lib, clippy `-D` clean, gate exit-0 by construction = unchanged from `79cf8e3`).
  - **THESIS RESULT #9 (2026-06-23) — L1 `int_hblank` FIXED (2/4 → 3/4). The if_late re-derivation lands; SHIPPED flag-gated, byte-identical OFF.** After #8 refuted the engine-raise lever, executed the L1 mandate's PRIMARY part: re-derive the `if_late` mode-0 halt-wake mask for the deferred cc+0 frame (NOT a forward advance). New `Interconnect::m0_halt_hold` (`interconnect.rs` + `tick.rs`): when the mode-0 rise is taken during halt on the reclock path, mask `if_late` this cycle + a countdown of further cycles. **Calibrated by sweep (env `SLOPGB_HH`/`SLOPGB_HHCC`, then baked): 2 uniform masked M-cycles (this cycle + `m0_halt_hold = 1`) + the `mask{rise cc==4}` second-half term — exactly the handoff decomposition. The cc==4 (not the eager cc==3) is the deferred `cc = eager+1` rotation (#8).** Result: `int_hblank_halt_scx0-7` DMG FF80 = 62,62,62,63,63,63,63,64 = baked target, all 8 PASS flag-on (FF82=01). The kernel pair (FF41 reads) + `intr_2_mode0_timing` (mode-2 OAM halt-wake) stay 3/0 ∧ PASS — neither halt-wakes on mode 0, so the mode-0 halt mask is free to recalibrate. **CGB empirically unchanged** (its `GB_cpu_run` samples cc+0, no DMG mid-cycle). Pinned by gbtr `tier2_int_hblank_halt_passes_dmg` (flag-on, 8 scx). Flag-OFF byte-identical (gated on `tier2_reclock`; `advance_machine_t` only runs under tier2). Gate exit-0; 658 lib + the new test; clippy `-D` clean; cavecrew-reviewer 1 finding (explicit `tier2_reclock` guard at the countdown use-site) FIXED. **TRIAD NOW 3/4: kernel ✓ ∧ intr_2 ✓ ∧ int_hblank ✓ ∧ sprites ✗ (L2).** L2 re-measured + RE-DIAGNOSED this session (sprite `early_lead` model-split sweep + `$FF80` `testcase_id` peek, reverted — **CORRECTS handoff #7's "DMG sticks at #34/X=167"**): **CGB `intr_2_sprites` PASSES at early_lead ∈ {0,−1}** (clean lever). **DMG, under early_lead=−1, passes testcases 1–9 then FAILS at testcase `0x0A` = 10 sprites at X=0** (the MAX sprite-count config, `extra=16`), NOT at the X=167 edge (#34). At early_lead ≥ 1 it fails even earlier (#01). So the DMG leg is a **sprite-COUNT-dependent deferred mode-3 length error at the max-sprite (10) configuration** — the deferred frame mis-projects the 10-sprite mode-3 extension by ≥1 dot, while production (cc+4) gets every config right. It is NOT an `early_lead` offset (fails at all uniform values) and NOT specifically X=167. The proj computation (`obj_fetch_base` ×10 + the first-per-tile `5 − (v&7)` penalty, `mode0.rs:100-121`) is a pure dot count, frame-independent, so the error is in how the deferred read frame samples vs the boundary at max sprite count — broad deferred-frame sprite-geometry recalibration, S5-coupled. Banking the CGB partial is not worth it (residual needs both models + the A9 +40 full-gbtr lift must be held). **Two more #7 errors corrected by boundary probe (`SLOPGB_SPPROBE`/`SLOPGB_SPLEADD`, reverted): (a) a NEGATIVE sprite `early_lead` is STRUCTURALLY DEAD — the dispatch (`proj<=lead`) sets `m0_src` and short-circuits `m0_flip_events`, so `vis_early` at `proj<=lead−1` never fires (measured: 0 VIS_EARLY vs 22 DISPATCH under tier2). So "DMG wants −1" is impossible. (b) The DMG sprite dispatch `lead` (the `fetched!=0 && !cgb` +1 term, `mode0.rs:143`) is a SEPARATE code path from the bare-line kernel/`int_hblank` lead — it is NOT "kernel-bound"; it can move for sprite lines without touching the kernel.** But a full 2-param sweep (DMG sprite `lead` delta −1..2 × `early_lead` 0..3) **FAILS at EVERY combo** while production passes — so **no uniform (lead, early_lead) offset fits the DMG sprite boundary; it is genuinely PER-CONFIG** (different sprite counts/positions need different deferred-frame mode-3 corrections). This is the broad deferred-frame sprite mode-3 geometry port (the gambatte proj formula recalibrated per-config to SameBoy `display.c` sprite penalties + the deferred read frame), S5-coupled — the lone residual, lands with the flip. Next: port SameBoy's sprite mode-3 penalty model into the deferred proj, validate A9 +40 on FULL gbtr. Defaults NOT flipped (still need 4/4 + the ~7000 rebaseline).
  - **THESIS RESULT #10 (2026-06-23) — L2 `intr_2_mode0_timing_sprites` FIXED → PHASE B FULL CONFIRM (4/4). SHIPPED flag-gated, byte-identical OFF. The "per-config geometry port" reframes to a one-line GRID-SNAP.** Two diagnostic corrections to #9, both measured:
    - **The premise that there is a SameBoy closed-form sprite penalty to port is VOID.** `mode3_batching_length` (`display.c:1471-1506`) returns **0** whenever objects are present AND the HBlank+STAT IRQs are enabled (the exact `intr_2_sprites` setup) → SameBoy runs the FULL dot-by-dot fetcher FSM (`display.c:1946-1984`), no closed form. slopgb's `proj` (`obj_fetch_base` + `5−(v&7)`) is slopgb's OWN approximation of its OWN dot-exact fetcher; production (cc+4) passes every config, so the real fetcher is correct.
    - **The test resolves mode-3 length to whole M-cycles (4-dot buckets), not dots.** `round_a` runs `41+extra` NOPs then FF41-polls (expects mode 0 on poll 1); `round_b` runs `40+extra` (4 dots earlier, expects mode 0 on poll 2). So the visible mode→0 must land in a 4-dot window `(253+4·extra, 257+4·extra]` (measured on the poll line, ly 68; read grid ≡ 1 mod 4). slopgb's dispatch dot `D(config)` is a SMOOTH per-X staircase (10 sprites X=0→318, X=1→317, X=2→316…; all extra=16/15) — at cc+4 the CPU read's own M-cycle quantization snaps that staircase into the right `extra` buckets (production passes all 105 configs); at cc+0 the leading-edge read exposes the sub-M-cycle dispatch phase, so configs whose `D` straddles a read-grid boundary mis-bucket (X=1's 317 reads mode 0 one poll early → fail).
    - **FIX (B5, `mode0.rs`): snap the sprite-line dispatch + coincident `vis_early` to the CPU read grid — the next dot `≡ 0 (mod 4)` (one below the read dots ≡ 1) — with `early_lead = 0`.** Gating `proj<=lead` (and `vis_early`) on `dot % 4 == 0` naturally delays the flip to the grid dot ≥ D, so all configs in a bucket land on the same grid dot and reproduce the cc+4 quantization. Applies to **`r.n_sprites > 0`** (OAM scan found sprites), which INCLUDES the off-screen X≥168 configs (`fetched == 0`, `extra=0`) that otherwise take the bare `lead` and fail at the bucket bottom (their bare dispatch 254 snaps to 256 ∈ (253,257]). Bare lines keep `lead+1` (the B3 kernel −2); window/glitch keep `lead+2`. **Verified offline against the ROM's `extra` values: snap-to-≡0 gives 0 fails across all 103 rendered configs; raw D fails 42; snap phases 1/2/3 fail 42/47/15** (`/tmp/analyze.py`). The handoff #9 "no uniform (lead, early_lead) offset fits — genuinely PER-CONFIG" diagnosis was CORRECT; the resolution is a QUANTIZATION (re-grid), not a per-config offset table.
    - **4/4 TRIAD flag-on (`SLOPGB_TIER2=1`, both models): kernel m2int=3 ∧ m0int=0 ✓ · `intr_2_mode0_timing` PASS ✓ · `int_hblank_halt_scx0` FF82=01 ✓ · `intr_2_mode0_timing_sprites` PASS ✓.** The A9 +40 single-speed gambatte sprite `m3stat` lift HOLDS flag-on (80/80 = 40 ROMs × 2 models produce their `out` value). Pinned by new gbtr `tier2_intr_2_sprites_passes` (both models, flag-on). Byte-identical OFF: gbtr 188/188 + mooneye 91/91 gate exit 0; 658 lib; clippy `-D` clean; cavecrew-reviewer no issues. LE-only (Tier-1) path unchanged (snap/el-0 gated on `tier2_reclock`). **Defaults still NOT flipped — the C-stage (global flip + ~7000-row SameBoy-ground-truthed gambatte rebaseline + 146 golden recapture + every-oracle-zero-drop) is the remaining Phase-B work; 4/4 co-hold is the green light for it.**
    - **C-STAGE SCOPE MEASURED (2026-06-23 #10, worktree `/tmp/slopgb-flip` @8aede99 both defaults→true): gambatte 594 regr / 233 now-passing (net −361); mooneye 87/91 groups (4 fail).** The mooneye regressions are the convergence gate (SameBoy-agreeing; must reach zero-SameBoy-drop BEFORE the flip) and split into two classes: **(1) deferred-frame timer/serial recalibration — `boot_div` (S/dmg0/dmgABCmgb/cgbABCDE/A ×~7) + `boot_sclk_align` (dmg/mgb ×2): these read DIV/serial at the deferred cc+0 leading edge (1 M-cycle earlier than cc+4); PASS off + LE-only, FAIL tier2 → it is the deferred READ FRAME, the same 1-M-cycle shift the architecture says all calibrations re-derive against SameBoy. NOT the L2 fix (boot runs no sprites). (2) PPU render-frame — `intr_2_mode3_timing` (×6), `hblank_ly_scx_timing-GS` (×4), `lcdon_timing-GS` (×4): the cc+0 render-frame shift; ground-truth each vs SameBoy (some rebaseline, some recalibration).** The gambatte 594/233 is the ~7000-row baseline ratchet's flip delta (each newly-floored row SameBoy-ground-truthed at C2). C-stage is unambiguously multi-session; mooneye `boot_div`/`intr_2_mode3` must converge first. **Tooling: `/tmp/slopgb-flip` (worktree, both flags→true, ROM symlink); `CARGO_TARGET_DIR=/tmp/slopgb-flip/target cargo test --test mooneye/--test gbtr gambatte_matrix` prints the regr/stale lists; `/tmp/hramdump` + `sameboy_tester`+`sb_ocr.py` ground-truth.**
  - **THESIS RESULT #11 (2026-06-23) — C-STAGE STARTED. C0 (boot DIV/serial) + C1.1 (`intr_2_mode3`) SHIPPED flag-gated, byte-identical OFF, pushed (`356f45d`, `1e8a9e7`). mooneye flag-on 87/91 → 89/91; only `hblank_ly_scx_timing-GS` ×4 + `lcdon_timing-GS` ×4 remain (the entangled residuals). Defaults NOT flipped.**
    - **C0 — deferred-frame DIV/serial phase recalibration (`interconnect/boot.rs`, `356f45d`).** The post-boot `div_counter` constants (`model.rs`) are calibrated for the eager cc+4 sample; the Tier-2 deferred read samples the timer at the M-cycle leading edge (cc+0), 1 M-cycle (4 T) early, so reads positioned just after a DIV-byte increment lost 1. **Fix: `div.wrapping_add(4)` under `tier2_reclock`** at the boot hand-off (uniform across timer/serial/APU seeds, all derive from `div`). Empirically proven: DIVADJ sweep → +4 is exact (0/8 fail at the before/after-increment reads), fixes `boot_div` (9 model legs) ∧ `boot_sclk_align` (2). The phase is decided at construction (`apply_post_boot_state`), so the runtime `set_tier2_reclock` toggle is too late → added `GameBoy::new_with_reclock` + `harness::boot_with_reclock` (enable the flag before boot, mirroring the C-stage default-true flip). Pinned `tier2_boot_div_passes`. SameBoy passes `boot_div` reading at cc+0 (its `div_counter` rides the same +1-M-cycle leading-edge timeline).
    - **C1.1 — `intr_2_mode3_timing` (`ppu/stat_irq.rs mode3_entry_dot`, `1e8a9e7`).** The CPU-visible 2→3 entry was back-dated 4 dots (84→80) for the LEADING-EDGE-ONLY frame. The Tier-2 deferred read pays the parked debt then samples, landing the 2→3 entry at the TRAILING (cc+4) frame, so 80 made it see mode 3 a full M-cycle early (`test_iter 2` → 1 poll, want 2). **Fix: take NO back-date (84 = flag-off value) under `tier2_reclock`** (`if leading_edge && !tier2 && !ds {80} else {84}`). LE-only keeps 80 (the S0 kernel-pair specs). Both models PASS; pinned `tier2_intr_2_mode3_passes`. Confirms: tier2 read-frame ≠ LE read-frame; the deferred read trails where LE-only peeks early. (intr_2_mode3 fails ONLY tier2 — OFF + LE PASS — so it is the deferred MACHINE, not the cc+0 STAT value: LE+tier2 sample STAT at the same PPU position for a uniform stream, but the dispatch/halt frame shifts the post-IRQ instruction stream.)
    - **C1.2 (2026-06-23 #11b) — `lcdon_timing-GS` FIXED + `int_hblank` re-derived for the +4 frame. mooneye flag-on 89/91 → 90/91 (only `hblank` ×4 left). The "C0 +4 breaks int_hblank" coupling found + resolved.** Three lcdon legs, all the C1.1 pattern (the deferred read takes NO leading-edge −4 back-date): leg1 glitch mode-3 ENTRY (`stat_irq.rs` `start` 74→78 under tier2), leg2 glitch LYC readable compare (`lyc.rs` drop the last-4-dots line-1 view under tier2), **leg3 the bare-line mode-0 EXIT (`mode0.rs early_lead` 1→0 under tier2)** — leg3 was misdiagnosed in #11a as a "post-glitch read-frame lateness"; it is the bare `early_lead`: the visible mode→0 boundary at lead+1 (dot 253) is read AT 253 by lcdon's post-glitch line-1 STAT read (probed: read lands line-1 dot 253), which must read mode 3 → boundary must be lead+0 (254). The kernel only needs the boundary in (252,256] (253 ∧ 254 both satisfy m2int@252=3 ∧ m0int@256=0), and intr_2_mode0/mode3 + sprites all hold at 0 — so 0 is the correct unified value; el=1 was merely the minimal-for-kernel before lcdon was tested. **CRITICAL — the C0 +4 DIV breaks int_hblank under the REAL flip:** the `tier2_int_hblank_halt` pin used `boot` + `set_tier2_reclock` (post-boot, so C0's `apply_post_boot_state` +4 is SKIPPED), but the real flip sets tier2 at construction → the +4 advances the timer phase one M-cycle → the TIMA-counted halt-wake reads +1 (FF80 63 vs 62). Fix: `m0_halt_hold` base 1→0 (mask 2 M-cycles → 1: this cycle + `cc==4`), re-deriving the wake for the +4 frame; and the pin now uses `boot_with_reclock` (construction-time, +4 active) so it validates the real path. All 6 gbtr tier2 pins pass (kernel/sprites/mode3/boot_div/int_hblank/**lcdon NEW**); byte-identical OFF (all gated `tier2_reclock`, LE-only keeps the −4). **LESSON: every tier2 pin that is timer-coupled MUST `boot_with_reclock` — the set-after-boot path skips C0's +4 and mis-validates.**
    - **C1.3 (2026-06-23 #11c) — `hblank_ly_scx_timing-GS` FIXED → mooneye CONVERGENCE GATE MET (91/91 flag-on, zero-SameBoy-drop). `b60598c`.** The S7 sub-M-cycle halt-wake residual, cracked via a sub-M-cycle PHASE CARRY (not the full finer-grid rewrite). Mechanism: SameBoy's DMG mode-0-halt-wake resumes the CPU at the IRQ's sub-M-cycle clock → the CPU M-cycle phase is offset from the PPU dot grid by the rise's within-M-cycle position; slopgb's M-cycle-quantized deferred clock (CPU+PPU advance together) collapses that, so a post-wake LY read straddling the LY-increment reads the wrapped line. Fix (`Interconnect::halt_ly_phase`): the mode-0 halt-wake records the carry indexed by the rise's M-cycle phase `cc` (`HALT_LY_PHASE_BY_CC=[1,2,0,1]`; cc2→2 and cc3→0 are the geometrically-pinned straddlers, cc1/cc4 are clear of the wrap so any ≥1 is inert); the FIRST post-wake FF44 read back-dates the line by it (one-shot). **The key that made it bounded (vs the full CPU/PPU decouple): the carry is set ON THE WAKE, so the pre-halt `wait_ly` poll never sees it** — which is exactly why a uniform `vis_ly` back-date failed (it corrupted the poll). int_hblank (TIMA, not LY) and intr_2 (mode-2 wake) are untouched; all 7 gbtr tier2 pins green (+`tier2_hblank_ly_scx_passes`); byte-identical OFF (gbtr 192 + mooneye 91 exit 0). **The earlier "irreducible S7 / needs full finer-grid decouple" verdict (all six bounded levers refuted) was for levers applied UNIFORMLY or to the whole machine; the wake-gated one-shot carry is the seventh lever and it works — the sub-M-cycle phase only needs to reach the single post-wake measurement read, not the whole CPU/PPU phase.** C1 COMPLETE. Remaining C-stage: C2 (gambatte rebaseline, SameBoy-ground-truthed), C3 (flip), C4 (golden + oracles).
    - **C2 MEASURED (2026-06-23 #11d) — the flip is BLOCKED on S5 (StatUpdate engine), not a clean rebaseline.** With C1.3 + both defaults flipped (worktree), gambatte = **422 regr / 239 stale**. The regr are NOT mostly SameBoy-floor: DMG ground-truth (sameboy_tester + sb_ocr.py, parallel) shows ~106 of 114 DMG regr are slopgb-LE BUGS (SameBoy == gambatte-want, slopgb-leading-edge wrong) across window(47)/halt(20)/enable_display(7)/m1/lycEnable/vram_m3/oam_access/ly0/m2int/m2enable/lcdirq_precedence; only ~9 genuine floor. **Confirmed LE-ONLY** (the ENGINE, not the tier2 deferred machine nor C1): `enable_display/frame0_m0irq_count` OFF=90, **LE=0**, TIER2=0 — the flip switches `stat_events_tick`→`stat_update_tick` (the S5 StatUpdate engine), which is incomplete (the documented glitch-line/window/etc over/under-fires; PORT-PLAN S5 "~123 rows"). 303 CGB regr untriaged (`sb_ocr.py` returns empty on CGB color output — needs a CGB-OCR or hramdump path). **So the C-stage flip requires completing S5 first** (port SameBoy `GB_STAT_update` `display.c:523-560`); only then are the genuine-floor remainder rebaselineable. NEVER baseline the ~106 (SameBoy passes them → fix). The mooneye convergence gate (the timing-frame) is DONE; the gambatte side is the S5 engine port. Tooling: `/tmp/gbflip_full.txt`, `/tmp/c2_groundtruth.py`, `/tmp/c2_{agree,bug,diff}.txt`.
    - **S5 STARTED (2026-06-23 #11e, THESIS RESULT #11e) — first clean S5 slice SHIPPED (window `vis_early`), the regr triaged render-frame-vs-engine, the `frame_m0irq_count=0` mechanism nailed, durable measurement infra banked. `a084116`. Defaults NOT flipped.** Built a fast flag-on iteration loop and a re-measurable convergence gate, then split the 106 DMG + 303 CGB regr by **what** breaks them.
      - **INFRA (banked, byte-identical OFF):** (1) `tests/gbtr/gambatte_flagon_probe.rs` — an `#[ignore]`'d row-list runner (`SLOPGB_ROWLIST`) that boots an explicit set of gambatte rows flag-on (tier2, or LE-only via `SLOPGB_PROBE_LE`, or OFF via `SLOPGB_PROBE_OFF`) and OCRs with the REAL suite OCR (`read_hex_screen`) — reproduces `run_case`'s verdict exactly, runs a category in seconds (vs the ~4-min full-suite flip capture). (2) `tests/common/mod.rs` `SLOPGB_MOONEYE_RECLOCK=1` — boots the whole mooneye matrix on the construction-time reclock (the real C3 flip) so the convergence gate is re-measurable after EVERY S5 change; `91/91` = all 91 mooneye group-tests green flag-on. Both gated → OFF gate byte-identical (verified exit 0).
      - **THE KEY TRIAGE — render-frame (tier2-only) vs engine-dispatch (LE-level):** ran every DMG bug row OFF / LE-only / tier2. Rows that PASS LE-only but FAIL tier2 are caused by the tier2 RENDER recalibration (`vis_early`/`early_lead`/`mode3_entry`/C0) — potentially clean, isolable. Rows that FAIL under LE-only are the ENGINE-DISPATCH core (the `stat_update_tick` rising edge dispatches the IRQ at a dot the cc+0 read-frame mis-samples). Of the 106 DMG: **37 LE-pass-tier2-fail** (window 14, halt 14, enable_display 4, + tima/serial/m2int_m2irq/lycEnable/lyc153int_m2irq singletons) and **69 LE-fail** (the engine core). The CGB categories add sprites(87, render/L2-broad) + speedchange/dma/serial/tima/lcd_offset(~30, S6/S7 DS) which are OUT of S5 scope (S6 write-conflict / S7 DS).
      - **WINDOW SLICE SHIPPED (`mode0.rs`, the +14 win):** active-window lines took `early_lead = 2` (a +2 `vis_early` anticipation) under tier2, flipping the CPU-visible mode→0 two dots early → `window/arg/late_wy_*` m3stat reads saw mode 0 a poll early. **Fix: window lines take the SAME deferred-read law C1.2 proved for bare lines (`early_lead = 0`, no anticipation — the Tier-2 deferred read pays parked debt then samples at the trailing frame).** The LCD-enable glitch line is SPLIT OUT and KEEPS +2 (the C1.2 `lcdon` pin). **Measured: full gambatte/window category 287→301 (+14, both models), ZERO pass→fail (no SameBoy-passing row dropped); all 7 tier2 pins green; mooneye 91/91 flag-on.** Partial — the window engine-dispatch residual (aborted-window `late_disable_*` want=0/got=3 + the LE-level fails) is a SEPARATE root cause (the deferred IRQ DISPATCH dot, want0 fails LE too → not `vis_early`).
      - **`enable_display/frame_m0irq_count = 0` MECHANISM NAILED (the goal's named LE=0 proof case): NOT "the engine fires nothing".** Traced flag-on (`SLOPGB_S5DBG`, reverted): the `stat_update_tick` rising-edge engine FIRES ~144 mode-0 edges/frame (one per visible line, at dot 254 line≥1 / 251 line 0; `pending_if |= IF_STAT` set correctly each line). The count is 0 because the IRQs are NOT DELIVERED to the CPU in the cc+0 read-frame — a delivery/read-phase miss (the leading-edge CPU interrupt sample misses the edge that the production cc+4 sample catches), entangled with the halt-wake `if_late`/`second_half` masking (`tick.rs`). Also observed: the mode-0 edge dot DRIFTS frame-to-frame (254↔257). So `frame_m0irq_count` is the engine-DISPATCH/read-frame core, not a missing emission — confirms the 69 LE-fail rows are the deferred dispatch↔read coordination, the documented atomic work (needs a CPU-delivery + SameBoy-dot tracer; principled, no curve-fit).
      - **CGB ground-truth path (C2 prereq) — COMPLETED + the 303 CGB regr TRIAGED (`docs/sameboy-port/tools/cgb-groundtruth.md`).** The frame-alignment blocker is solved: SameBoy `--cgb` needs `--length 4` (CGB boot ~2.5× DMG's 2s; at length 2 the gambatte result hasn't rendered), then `sb_ocr2.py` (palette-agnostic per-tile-bg) reads it. Triaged all 293 slopgb-tier2 CGB fails (script `/tmp/c2_cgb_groundtruth.py`, cross-checks length 4∩6): **BUG (SameBoy==want, slopgb-LE wrong → FIX, NEVER baseline) = 248 · AGREE (SameBoy==slopgb-tier2 → genuine floor → baseline) = 39 · DIFF = 6 · UNREADABLE = 0** (robust). ~85% real bugs (mirrors DMG ~93%). The 39 floor (m1/m0enable/lycEnable/window/lyc153/tima/serial/m2enable/…) are the CGB C2 baseline candidates; the 6 DIFF (display_startstate ×3 sb=80, 2 lcd_offset DS, 1 hdma DS) need manual review. C2 now has a SameBoy verdict for EVERY flip regression on both models — baseline = AGREE rows only, NOT the BUG rows (which need the atomic reclock).
      - **CGB ground-truth path (superseded note) — was blocked on SameBoy-tester frame alignment.** `read_hex_screen` already OCRs CGB on the SLOPGB side (the gbtr `CgbColorMap`), so a non-baselined CGB regr that the flip breaks is a bug by the same premise (production==SameBoy on non-baselined rows). For the SAMEBOY side: wrote `/tmp/sb_ocr2.py` (palette-agnostic: per-tile background = the glyph's always-blank top-left pixel, on = differs) — correct shape, OCRs DMG identically. BLOCKER: SameBoy's `--cgb` gambatte output renders the digit on a COLORED FULL-SCREEN field whose content depends on the `--length` (boot-timeline) — at `--length 2` the print hasn't landed (top row uniform), at 4 the whole screen is colored. The 15-frame gambatte protocol must be frame-aligned to the SameBoy CGB boot timeline (a bounded C2 sub-task), THEN sb_ocr2 reads it.
      - **SameBoy DISPATCH-DOT GROUND TRUTH BUILT (the tracer, `docs/sameboy-port/tools/stat-irq-trace.md`) → the "engine-dispatch" diagnosis is REFINED: the dispatch dots already MATCH; the residual is the FF41-mode READ frame.** Instrumented SameBoy `GB_STAT_update` IF|=2 (`display.c:558`) to log `cycles_for_line` (= the within-line dot, 0..456, directly comparable to slopgb `dot`). Measured (DMG, `--length 2`): **SameBoy dispatches mode-2 STAT IRQs at cfl=0 (line start) and mode-0 at cfl=257.** slopgb's flag-on dispatch dots ALREADY match these — mode-2 at dot 0 ✓, mode-0 at 257 under the tier2 retime (the `m0int_m3stat_2` kernel pin passes). Confirmed across categories: `window/late_disable_1` (want=3), `window/m2int_wxA6_scx3` (want=0/3), `oam_access/10sprites` (want=3) ALL dispatch at cfl=0 mfi=2 (correct); `halt/m0int_m0stat_scx2_1`, `frame0_m0irq_count` at cfl=257 mfi=0 (correct). **So the 69 "engine-dispatch" (LE-fail) rows are NOT wrong dispatch dots — they are the ISR's FF41-mode READ (the cc+0 leading-edge `vis_mode` sample) landing at the wrong effective dot, or `vis_mode`'s mode-3 LENGTH wrong under the deferred read — the SAME class the window `early_lead` fix solved.** `frame_*_count` is a SEPARATE DELIVERY issue (dispatch correct, but the count needs the IRQ delivered+counted each line; the halt `if_late`/`second_half` mask or the cc+0 IF sample miss — m0int_m3stat single-read passes at the same cfl=257, only the count fails).
    - **S5 #11g (2026-06-24) — WINDOW family EXHAUSTIVELY measured → #11f's "low-WX over-extends" diagnosis REFUTED → confirmed NO clean tier2 window slice (4 distinct mechanisms). Tooling shipped (byte-identical OFF). NO row shipped; defaults NOT flipped.** Full per-config table + method in `docs/sameboy-port/tools/measurements/window-groundtruth-2026-06-24.md`.
      - **REFUTATION:** #11f read the SETUP-frame bare exit (cfl257), not the MEASUREMENT-frame exit. Fresh measurement (rebuilt SB tester + new slopgb `visflip`/`winmatch` traces, measurement-frame isolated via the rare-exit heuristic): **SameBoy extends ALL 53 DMG window measurement lines to ≈263+SCX&7** (wx00/wxA5/late_wy/late_disable alike) — NO low-WX no-extend, NO over-extend; slopgb UNDER-extends most.
      - **4 mechanisms, offsets BIDIRECTIONAL+non-uniform even within a sub-family (no uniform lever):** (1) **wxA6 read-frame/sub-dot wall (~13, S7):** `visflip == SBex EXACTLY` (257=257…262=262), LE passes `_1` phase, tier2 fails on deferred-read over-advance — boundary PERFECT, pure S7. (2) **normal-window boundary −2 (~7, m2int_wx0x):** triggers OK, `visflip 261 vs SBex 263`; needs the visible mode-3 to LAG the dispatch but `vis_early` can only ANTICIPATE + `lead` is production-shared/kernel-pinned → needs a NEW "vis-hold". (3) **late_wy `wy_ok=false` (~16):** WY-latch FALSE where SameBoy triggers → renders bare, window never enters slopgb render → no flip lever reaches it (render-level WY-latch). (4) **late_disable `en=false` aborted (~10):** SameBoy keeps the aborted +6; slopgb `win_aborted` SUBTRACTS from `lead` (render-level).
      - **VERDICT:** window = the multi-config port the goal scopes — needs a tier2-gated PARALLEL window-length model that replicates SameBoy's trigger (late-WY + disabled-extend) AND a vis-HOLD (visible mode-3 lags dispatch). Neither primitive exists. Banked the refined map; ZERO SameBoy-passing rows touched; tracers (`render/mode0.rs` visflip, `render/window.rs` winmatch) byte-identical OFF (gated `SLOPGB_S5DBG`/`leading_edge_reads`).
      - **FF41-READ GROUND TRUTH (decisive, extended the tracer to `read_high_memory:629` + slopgb `read_deferred`):** measured the m3stat ISR read dot, slopgb vs SameBoy — kernel m2int scx0: slopgb reads ly1 **dot252** mode3 / SameBoy **cfl256** mode3 (✓ both); m0int scx0: slopgb **256** mode0 / SameBoy **261** mode0 (✓); **m2int scx3: slopgb 256 mode3 ✗ / SameBoy 260 mode0** (want 0). **ROOT CAUSE: slopgb's deferred FF41 read lands ~4–5 dots EARLIER than SameBoy's AND does not shift with SCX (scx0+scx3 both read dot 256; SameBoy 261 vs 260); slopgb's mode-3 boundary DOES extend +SCX&7 (scx3≈259), so the early read at 256 falls before it → mode 3, while SameBoy's read at 260 falls after → mode 0.** The read frame + the boundary are each self-consistent in slopgb's vs SameBoy's frame, offset ~4 dots — **shifting either alone breaks the scx0 kernel pin** (kernel reads 252 vs boundary 256; +4 read → flips to mode 0). So the SCX-extended bare-line m3stat reads are the **atomic read-frame↔boundary reclock** (confirmed with exact dots), NOT a tier2-gated `vis_early` nudge — the window slice was the lone corner with enough slack. Detail + table: `docs/sameboy-port/tools/stat-irq-trace.md`.
      - **HALT `*_m0stat_*` ATTEMPTED → the sub-M-cycle wall, proven by cc-collapse (NOT shipped).** The post-mode-0-halt-wake FF41-mode read (`want=0 got=2`, 20 DMG): SameBoy reads ly2 **cfl0 mode0**, slopgb line2 **dot4 mode2** — a uniform +4-dot DMG wake over-advance (CGB samples cc+0, no over-advance). Built the full C1.3-analogue (`halt_vis_back`, DMG-only, first post-wake FF41 read in `[4,8)`→mode 0): **+11 DMG / −3** (`scx5_2`×2 + `scx3_2b`, all `want=2` SameBoy-passing) = net A/B. Tried the C1.3 cc-index: **REFUTED — `scx3_2` (want=0) and `scx3_2b` (want=2) are BOTH cc=1**; opposite wants collapse to identical slopgb `(line2,dot4,cc1)`. cc is the finest phase the M-cycle-quantized deferred clock has → genuinely unresolvable at this resolution (the sub-cc T-within-M wake phase is quantized away). The deep S7 deferred-halt-wake residual (the wall C1.3 only partly climbed for LY); needs the sub-M-cycle wake clock. REVERTED. Detail: `docs/sameboy-port/tools/stat-irq-trace.md`.
      - **NEXT (S5 core) — the residual is genuinely the atomic reclock for the SCX/bare m3stat reads + the sub-M-cycle wall for halt; per-line-type `vis_mode` nudges only reach the corners with kernel-frame slack (window done):** extend the window/C1.2 `early_lead` approach (the deferred read takes NO anticipation) to the remaining line-types — sprite lines (snap already done for the mooneye pin, but the gambatte `m3stat` rows + oam_access need the per-config read offset), aborted-window lines (`win_aborted`, the `late_disable*` want=0/3 — different mode-3 length than active window), and the mode-3 ENTRY/length per SCX — each calibrated against the SameBoy cfl dispatch + the fixed cc+0 read offset, re-validating the 7 pins + mooneye 91/91 each step (the window fix is the template). The halt family (14 `*_m0stat_*`) is the C1.3 analogue for the FF41-MODE read (post-mode-0-halt-wake reads land on the next line's mode 2) — a `halt_ly_phase`-shaped one-shot `vis_mode` carry, halt-wake-coupled. `frame_*_count` = the delivery/mask path (`tick.rs` m0_rise). Principled per-SameBoy only (no curve-fit) — the dispatch-dot tracer is the ground truth.
    - **S5 #11h–#11k (2026-06-24/25) — the m1/lycEnable IF-delivery engine family: SPLIT into roots, then the engine-driver roots SHIPPED flag-gated (byte-identical OFF, defaults NOT flipped). Detail + tables: `docs/sameboy-port/tools/measurements/m1lyc-ifdelivery-groundtruth-2026-06-25.md` + `atomic-reclock-recipe.md` #11i.**
      - **#11h:** FF0F (IF) ground truth — the `want=3↔1`/`E0↔E2` rows read IF not FF41. 17 DMG rows classified, 16/17 LE-engine-only. Two driver roots: (1) MISSING vblank-entry m1 re-arm, (2) SPURIOUS ly153→ly0 wrap / late-disable. Measurement only.
      - **#11i:** C-stage mechanism SPLIT (read-observer / wake-clock / engine-driver) with `dc` sub-dot evidence. Measurement only.
      - **#11j (`421fd30`) — root 1 SHIPPED: the vblank-entry LYC-latch DROP.** At line 144 dot 0 drop a held-true LYC match (`lyc!=144`) iff VBlank armed (`stat_irq.rs::stat_update_tick`) — the missing dip that lets the dot-4 mode-1 rise re-arm a fresh `IF|=2`. +16 m1-rearm rows (incl 2 BLANK-OCR). Pin `tier2_m1_vblank_rearm_passes`.
      - **#11k (this session) — root 2 SUB-CASE SHIPPED: the line-0 VBlank carry.** SameBoy's `stat_interrupt_line` is the SINGLE mfi-selected mode source OR LYC (NOT wired-OR), and mfi is NEVER re-set between line-144 entry (`=1`) and line-0's OAM step (`=2`) — so with VBlank enabled the line stays continuously HIGH across the wrap and the line-0 OAM rise raises no fresh edge. Fix (`update_mode_for_interrupt`, line-0 `dot<4` arm): carry **mode 1 (VBlank)**, not `vis_mode` (mode 0 on DMG; CGB already read 1 → DMG-only, CGB byte-identical). **+4/−0 flag-on** (3 family + 1 `lcdirq_precedence`, 0 SameBoy-passing dropped). Pin `tier2_line0_vblank_carry_passes` (DMG `lycwirq_trigger_ly00_stat50_1` outE0). The named target `m2m1irq_ifw_2` DISPATCH now matches SameBoy (spurious ly0 gone) but OCR still `got=3` = the **mech-1 read-frame** residual (convergence trap), not root 2. **Root-2 residuals banked:** the LYC-source / late-FF45-write sub-case (`lycwirq_*_stat50_2`, `lyc0_late_ff45_enable_3` still E2 — the `ly_for_comparison`-wrap + write-trigger lever), CGB lycwirq E2, and late-disable (`m2enable/late_enable_m0disable_2`).
      - **#11m (2026-06-25) — MECH 2 WAKE-CLOCK measured → FALL BACK (no code; the goal's primary path).** Built the NEW SameBoy HALT-exit `dc` tracer (`SBWAKE` at the two `GB_cpu_run` halt-exit branches + `SBCYR ff41 pre/post` bracketing the deferred read flush; `sm83_cpu.c`, SB_TRACE-gated). Decisive: the halt `*_m0stat_*` want-0/want-2 split is the **read-flush sub-dot landing** across the `ly2` mode-2 commit — every want-0 flush ends at `ly2 dc2` (line-start mode-0 hold → mode 0), every want-2 at `ly2 dc8` (mode-2 OAM commit → mode 2), driven by the CPU sub-dot wake phase (IRQ rise `dc4`/want-0 vs `dc8`/want-2, same `cfl260`). **slopgb collapses the pair** (`scx3_2` want0 ∧ `scx3_2b` want2) to identical `ly1 dot257 dispatch / ly2 dot4 read / cc1` — no slopgb-computed quantity differs, so no finer-than-`cc` field to record the phase on. The discriminator is constant+distinct but is the CPU 8 MHz sub-dot wake position = the **S7 sub-M-cycle deferred clock** (out-of-session). First DIRECT SameBoy-`dc` confirmation (vs #11e's inferred `cc`-collapse). Table + decision: `measurements/wake-clock-groundtruth-2026-06-25.md`; tracer in `tools/stat-irq-trace.md`. **Fallback (mech-3 late-disable) ground-truthed → no clean fix:** `m2enable/late_enable_m0disable_2` [Dmg] is a **C2 AGREE-floor row** (SameBoy reads mode 2 ≠ gambatte `out0`; slopgb flag-on already matches SameBoy — not a bug), and the two remaining late-disable fails are CGB-only lcd-offset/lycwirq-E2 residuals (`late_m1disable_ly0_2`, `lyc153_late_m1disable_2`) = the goal's separately-scoped CGB territory. Docs-only; gate byte-identical to `0db1b83`.
      - **#11n (2026-06-26) — MECH 1 READ-OBSERVER, first CODE slice SHIPPED (flag-gated, byte-identical OFF, defaults NOT flipped). The eighth-grid bare-line `m2int_m3stat`.** The cc+0 deferred FF41 read samples at its M-cycle START (dot ≡0 mod 4) and observes the mode-0 flip at the cc+2 phase (`ACCESS_PHASE`), so a read landing IN the boundary's M-cycle should read mode 0 iff the dispatch is at cc1/cc2. cc1 (dispatch == M-cycle start) is already caught by `line_render_done`; a **cc2 dispatch (dot ≡1 mod 4 ⇔ SCX&7 ∈ {3,7})** commits ONE dot past the start, so the whole-dot `line_render_done` left the same-M-cycle read at mode 3. Fix (`render/mode0.rs::m0_flip_events`): bare-line `early_lead = 1` when the predicted `dispatch_dot = self.dot + proj − lead ≡ 1 mod 4`, anticipating `vis_early` to the M-cycle start. cc3/cc4 KEEP el=0 (their same-M-cycle read precedes the boundary → mode 3 = correct: kernel m2int@252 dispatch 254≡2, lcdon's 253 read; the NEXT M-cycle reads mode 0 via `line_render_done`). The IRQ side (`mode_for_interrupt`/`prev_done`) keys on `line_render_done`, NOT `vis_early` (bare non-glitch lines), so the counter-pinned dispatch is untouched. **Restricted to window-free bare lines** (`clean_bare = !wy_latch && wy2!=ly && !win_stalled && !win_aborted`): the first two-bin exposed an A/B swap on `window/late_disable_*` (bare at flip but the two test reads land 1 cycle apart and COLLAPSE to the same slopgb read dot — SameBoy distinguishes them at sub-dot, BOTH siblings SameBoy-passing; the cc2 anticipation just flips which passes → would DROP the SameBoy-passing `_2`). `wy_latch`/`wy2==ly` persist across a window-disable, marking every window-involved line. **Full-suite two-bin (target/gbtr fix vs target/lint reverted): clean +8/−0** — fixed `m2int_scx3_m3stat_2` (DMG+CGB), `m2int_nobg_scx7_m3stat_2` (CGB), 5 `speedchange/*_m2int_m3stat_scx3_2` (CGB); ZERO SameBoy-passing dropped. Floored: `late_scx4_2` (DMG+CGB) = the same read-collapse as the window late_disable pairs (slopgb reads _1 and _2 both at dot 256; SameBoy 260/261 — a read-side sub-dot limit, not a boundary). Pin `tier2_m2int_m3stat_scx3_passes`; 11 tier2 pins; mooneye flag-on 91/91; gbtr+mooneye OFF byte-identical (exit 0); lib 659. `measurements/m2int-m3stat-eighthgrid-2026-06-25.md`.
      - **#11o (2026-06-26, `d524267`) — MECH 1 READ-OBSERVER, the ACCESSIBILITY-COUPLING slice SHIPPED (flag-gated, byte-identical OFF, defaults NOT flipped). OAM/VRAM read-accessibility `vis_early` release.** The same cc+0 deferred read that samples FF41 mode also samples OAM/VRAM read-accessibility — but the accessibility unblock keyed on `line_render_done` (the render-done dispatch) while the FF41 mode keyed on `vis_mode`/`vis_early` (one dot earlier on the cc2 boundary, #11n). On SameBoy the OAM/VRAM unblock is **COINCIDENT with the visible mode→0 flip** (`vis_early`), so the deferred read saw mode 0 yet OAM/VRAM still locked → accessibility reads rendered `3` where SameBoy reads accessible (`out0`). Fix (`ppu/blocking.rs`, tier2-gated): `oam_read_blocked += && !(tier2_reclock && vis_early)`; `vram_read_blocked` early-return-false `+= || (tier2_reclock && vis_early)`. **Full-suite two-bin (target/gbtr fix vs target/lint reverted): clean +6/−0** — fixed `vram_m3/postread_scx3_2` (DMG+CGB), `oam_access/postread_scx3_2` (CGB; DMG leg `xout1`-exempt), `dma/hdma_start_scx3_1` (CGB), `vramw_m3end/vramw_m3end_scx3_3` (DMG+CGB) — all SCX&7=3 cc2/el=1 boundary reads, ZERO SameBoy-passing dropped. Floored: `postread_scx2/5_2` (el=0 read-collapse — the OAM read lands at the boundary's dot but a sub-dot BEFORE its M-cycle start, which `vis_early`==dispatch-at-el=0 cannot release; the same read-side sub-dot limit as `late_scx4`). SEPARATE (not this fix, opposite direction): `oam_access/postwrite_2_scx3` (writes block LONGER — `oam_write_blocked`/`vram_write_blocked` boundary, own ground-truth). The OAM/VRAM `vis_early` release is the visible-flip↔accessibility COUPLING the #11n investigation predicted. Pin `tier2_oam_vram_postread_scx3_passes` (vram_m3 D+C, oam_access C); 12 tier2 pins; mooneye flag-on 91/91; gbtr 197/0 + mooneye 91/0 OFF byte-identical (exit 0); lib 659. `measurements/oam-vram-accessibility-2026-06-26.md`. **WRITE side (`d6a1b7f`, +4/−0): the SEPARATE write-direction target, SAME lever.** New SameBoy `SBOAMW`/`SBVRAMW` write tracers (`memory.c`) measured: SameBoy lands the write at `ly1 cfl260 blk=0` (mode 0), slopgb blocks it at `ly1 dot256 oam_write_blocked=true` — `vis_early` fires dot254 but `line_render_done` (dispatch) is later, so the write at dot256 sees `!line_render_done` → blocked. Fix (`ppu/blocking.rs`, `write_unblocked_early() = tier2_reclock && vis_early && !glitch_line`): release the mode3→0 END term of `oam_write_blocked` (DMG+CGB) and `vram_write_blocked` (non-glitch) on `vis_early`; glitch EXCLUDED so `lcdon_write_timing-GS` (line-start dots 80-83 gap) untouched. Two-bin (read+write vs read-only `d524267`): +4/−0 — `oam_access/postwrite_2_scx3` (D+C), `vramw_m3end/vramw_m3end_scx3_5` (D+C), zero SameBoy-passing dropped. Pin `tier2_oam_vram_postwrite_scx3_passes`; 13 tier2 pins; mooneye flag-on 91/91; gbtr 198/0 + mooneye 91/0 OFF byte-identical. **Read-observer accessibility family now CLOSED both directions** (postread/postwrite scx3); remaining accessibility = the floored scx2/scx5 read-collapse (el=0). Doc updated `measurements/oam-vram-accessibility-2026-06-26.md`.
      - **#11p (2026-06-26) — vis-HOLD TARGET EXHAUSTION (ground truth, NO code; both candidates REFUTED; mech-1 clean read-phase slices EXHAUSTED at +58 net).** The goal asked to build the vis-HOLD primitive (a tier2 mechanism EXTENDING CPU-visible mode-3 PAST the dispatch flip to fix an FF41 mode-3 read, the symmetric inverse of `vis_early`). Ground truth: it has **no viable clean tier2 target**. (1) **scx_during_m3/scx_m3_extend_1 (the designated "simplest START") is a PIXEL-RENDER test, not an FF41-read test** — SameBoy samples FF41 ZERO times for the output (1 ff41 read all run = ly1 cfl0 dc-372 spurious setup); the `out3` digit is the rendered BG gradient (OCR `03232323232301010101`) shifted by the REAL mode-3 length. **flag-OFF OCR == flag-ON OCR** (tier2 read phase irrelevant); slopgb's lone FF41 read (ly1 dot265 mode0) MATCHES SameBoy (cfl↔dot +3 → SameBoy ext cfl262 = slopgb dot259 < read 265 → both mode0). The under-extension is slopgb's shared fine-scroll comparator (`render.rs` hunt) latching `hunt_done` at the SCX=0 line-start match and never re-opening on the mid-mode-3 SCX write (SameBoy's comparator is live) → SameBoy extends +5 (cfl257→262), slopgb stays bare dot254. A visible-mode hold extends `vis_mode()` (FF41) but CANNOT move rendered pixels → **vis-HOLD structurally inapplicable; this is a SHARED render under-extension (prod==tier2), A/B-swept (baselined cluster) → C2.** (2) **Window mech #2 already refuted by #11g ATTEMPT 1** (a tier2 win-active vis-HOLD `m0_flip_dot`+`win_vis_hold()` IMPLEMENTED, measured 0/53, reverted): win-active reads are OPPOSITE-direction at the same ~dot261 boundary (want=3 late_wy too-SHORT-need-LATER vs want=0 m2int_wx_2 too-LONG-need-EARLIER), discriminated only by per-config read cycle → the S7 read-frame↔boundary atomic coupling, not a boundary lever; plus mech-3 (late_wy `wy_ok=false`) + mech-4 (late_disable `en=false`) render BARE (window never triggers in slopgb) → render-level WY-latch/abort = C2. Re-confirmed: `late_wy_FFto2_ly2_scx5_2` slopgb visflip win@261, SameBoy exit cfl268 (=263+SCX&7), read dot264 in the (261,268] gap — a vis-HOLD fixes THIS one but regresses the want=0 siblings. **Probe (233 baselined window+scx_during_m3 rows): 58 fail-OFF→pass-ON (read-phase wins already harvested #11n/#11o+prior), 0 tier2 regressions, 34 fail-BOTH render floors (only 7 in-scope DMG, rest CGB ds/lcdoffset).** **ATTEMPT 2 (this session, executed): the vis-HOLD primitive was BUILT** (a `vis_hold_until: u16` field, symmetric inverse of `vis_early`, set on `tier2 && win_active` dispatch to the refined ABSOLUTE law `263 + SCX&7`, consumed in `vis_mode` to hold mode-3 past the dispatch; byte-identical OFF) **and measured 0/233 alone — inert** because the want=3 rows render BARE (`win_active=false`, mechanism-3 WY-latch → hold unreachable) and the win-active fails are want=0 (need EARLIER). Own-measured confirmation of #11g with the correct law + exact root cause. **SHIPPED as a validated-inert foundation** (unit-test pin `vis_hold_extends_visible_mode3_past_the_dispatch`; `cycle_clock`/`mode_timeline` precedent) — the visible-mode half of the C2 parallel window-length model; the other half (the WY-latch render trigger) is the remaining C2 work. Gate green: gbtr 198/0 + mooneye 91/0 OFF byte-identical, mooneye flag-on 91/91, lib 660, 13 tier2 pins. **FULL read-observer sweep (238 rows, ALL families: scx_during_m3/enable_display/m0enable/bgtiledata/bgtilemap/scy/oam_access/vram_m3): the ONLY in-scope DMG fail across the entire mech-1 read-observer space is `scx_m3_extend` (the pixel floor) — every other fail is CGB lcd-offset/ds (mech-3 / Class-A) or window (render/S7). The proven #11n/#11o eighth-grid/accessibility levers have NO remaining DMG target (every want=0-got=3 anticipate-earlier row is CGB lcd-offset).** mech-1 DMG read-phase slices COMPREHENSIVELY EXHAUSTED. Remaining C-stage = big-infra only: mech-3 CGB lcd-offset (lcd_offset port — the largest/most concentrated batch, likely most tractable NEXT lever) · mech-2 halt wake-clock (S7) · C2 render rebaseline (hunt re-open / WY-latch). Defaults NOT flipped; final tree docs-only, byte-identical OFF. `measurements/vis-hold-target-exhaustion-2026-06-26.md`.
      - **#11q (2026-06-26, `457955e`) — MECH 3 CGB LCD-OFFSET, the accessibility-class line-start OAM-read window SHIPPED (first C-stage lcd-offset CODE; flag-gated, byte-identical OFF, defaults NOT flipped). +1/−0.** The 43-row lcd-offset batch's clean local lever. **What lcd-offset is:** on CGB the PPU's internal `display_cycles` phase is offset from the M-cycle line counter; SBMODE for `preread_lcdoffset1_1` shows the mode timeline shifted — every line `cfl=0 dc=2 vis=0` (mode-0 tail) → `cfl=0 dc=8 vis=2` (mode-2 a few sub-dots in), mode-3 at **cfl=84** (not 80, +4), mode-0 at **cfl=257**; slopgb does NOT model the offset (un-shifted DMG grid) so the deferred cc+0 read/dispatch mis-samples. **SHIPPED slice:** `oam_access/preread_lcdoffset1_1` [Cgb] want0 got3. Clean isolation — the non-offset base `preread_1` [Cgb] PASSES flag-on (slopgb reads `ly1 dot452`, real mode-0 → accessible); only the offset variant fails. SameBoy keeps `oam_read_blocked=false` for the first ~3 T-cycles of each CGB single-speed line (`display.c:1805-1810`: the mode-0/HBlank tail runs 2+1 `GB_SLEEP` cycles before the mode-2 OAM lock engages at state 7, where `oam_read_blocked = !cgb_double_speed`). The lcd-offset shifts slopgb's read 6 dots later (`ly1 dot452` → `ly2 dot2`), across the line boundary into mode-2, where slopgb (locking OAM from dot 0) reads blocked; SameBoy reads in the line-start window (`ly2 cfl0 blk=0`). Fix (`ppu/blocking.rs::cgb_linestart_oam_open`, tier2-gated): release `oam_read_blocked` for dots `0..CGB_LINESTART_OAM_OPEN`(=4) on CGB single-speed, line!=0. Two-bin (654 CGB baseline rows flag-on): +1/−0, zero SameBoy-passing dropped. Pin `tier2_oam_preread_lcdoffset1_passes` (gbtr 199/0). **SECOND SLICE (`e8c1257`, +2/−0): m3-start palette-RAM window.** SameBoy keeps `cgb_palettes_blocked=false` for 3 T-cycles INTO mode 3 (`display.c:1867` false, 3-cycle SLEEP, `:1877` true) — palette RAM accessible at the mode-3 entry before the lock. The offset shifts the `cgbpal_m3/*_m3start_lcdoffset1_1` access `ly1 dot80` to `ly1 dot86` past slopgb's sharp dot-84 palette anchor (SameBoy locks ~cfl87). Fix (`pal_ram_blocked`, tier2-gated): extend the mode-3 lock by `PAL_M3START_OPEN`(=3) to dot 87, CGB single-speed; `pal_ram_blocked` gates BOTH read+write so both legs land (`cgbpal_read` out00 + `cgbpal_write` out01). Pin `tier2_cgbpal_m3start_lcdoffset1_passes`; **15 tier2 pins**; gbtr 200/0 + mooneye 91/0 OFF byte-identical; mooneye flag-on 91/91; lib 660. **Session lcd-offset accessibility total +3/−0** (both = SameBoy mode-boundary accessible windows slopgb lacked; the offset moves the deferred access into them). VRAM has NO m3-start window (`display.c:1865` blocks VRAM at mode-3 entry, unlike palettes) so `vram_m3/preread_lcdoffset2` stays a render floor. The 24 residual lcdoffset fails (`m0enable/m1/lycEnable/miscmstatirq late_enable*` engine-dispatch IRQ swaps, `window` WY-latch, `_ds_` S6, `dma/hdma` S6) all need the full lcd_offset PPU-phase port = C2. **FLOORED (measured, not clean levers):** (1) `vram_m3/preread_lcdoffset2_1` — render/readback floor (flag-OFF OCR == flag-ON OCR, like scx_m3_extend; slopgb `ly1 dot84` mode-3 blocked == SameBoy `ly1 cfl87 blk=1`, both block → out0 is render-level). (2) `oam_access/prewrite_lcdoffset1_1` (write) — GENUINE floor: out1 ⟺ write lands (calibrated on the passing base), slopgb writes `ly2 dot2 blk=true` (drops) and SameBoy SBOAMW also blocks (`ly2 cfl0 blk=1`); per FSM CGB writes ARE blocked from line-start (`display.c:1802`, no read-style 3-cycle window) → slopgb correctly blocks, both drop, want1 is real-HW SameBoy misses too → not SameBoy-passing, leave baselined. **DISPATCH-CLASS (engine core, NOT a local slice):** `m0enable/m1/lycEnable late_enable_lcdoffset1_1` (want2 got0 — slopgb delivers `if=00`, no STAT IRQ, where SameBoy fires); clean offset-isolation but the late enable lands near the offset-shifted mode-2/mode-0 edge (cfl84/cfl257) that slopgb's un-offset `stat_update_tick` engine doesn't catch → needs the full lcd-offset PPU-phase model in the shared engine/render grid (the "lcd_offset port") = C2/mech-3 engine reclock. **VERDICT: the clean local accessibility levers in the lcd-offset batch are the two sub-dot mode-boundary windows — OAM line-start read (+1) + palette m3-start read+write (+2) = +3/−0 shipped+pinned; write OAM=genuine floor, VRAM=render floor, dispatch/window/ds/HDMA=engine/render/S6 needing the shared-grid offset port.** `measurements/lcdoffset-accessibility-2026-06-26.md`.
    - **`hblank_ly_scx_timing-GS` ×4 — [SUPERSEDED by C1.3 above] was the LONE residual, diagnosed as the L2/S7 read-grid QUANTIZATION (not a boundary, not a uniform offset).** Probed (el=0, hh=(0,4)): SCX=0-delay_a and SCX=1-delay_b BOTH land at **line 66 dot 0** (the LY wrap) yet want OPPOSITE LY (65 "before" vs 66 "after"). On hardware the per-SCX mode-0 IRQ dot (mode-3 length) separates them across the wrap; the deferred M-cycle-quantized halt-wake collapses the sub-M-cycle SCX phase to the same dot, so no `vis_ly` back-date fits both (off=1 fixes SCX=0-delay_a, breaks SCX=1-delay_b — measured, the same dot wants both values). int_hblank passes (the wake is right for the TIMA count), so this is purely the LY-read sub-M-cycle phase. The fix is sub-M-cycle wake/read resolution preserving the SCX phase (the half-dot grid, port Stage S7) OR a grid-snap on the bare-line mode-0 dispatch like L2-sprites (but that dispatch is shared with the kernel/int_hblank — must hold them). The S7 finer-grid residual; multi-session.
    - **`lcdon_timing-GS` — superseded recipe (#11a, kept for history):** Fails OFF=PASS, **LE=FAIL** too (pre-existing — the long-standing lone LE-flag-on failure, the glitch-line dot-4 residual), not new to tier2. Decoded via HRAM `ff98-ff9c` (fail_round/expect/actual/str): the failing legs are all glitch-line back-dates calibrated −4 for LE that the deferred frame should NOT take (same pattern as C1.1). **Leg 1 (round 1, STAT mode entry): glitch `start = GLITCH_MODE3_START − 4` (`stat_irq.rs:47`) → gate `&& !tier2_reclock` (no back-date under tier2). Leg 2 (round 3, LYC coincidence bit): glitch `compare_ly` last-4-dots back-date (`lyc.rs:21`) → gate `&& !tier2_reclock`.** Each leg fixed advances the failure (round 1→3→5), ZERO regressions to the 5 passing tier2 pins (verified full mooneye matrix). **Leg 3 (round 5, cycle 174 ≈ line-1 dot 244): expect $83 mode 3, got $80 mode 0 — a line-1 mode-3 EXIT ~8 dots early** (NOT glitch-line; touches the shared bare-line `early_lead`/exit, so it must be ground-truthed vs SameBoy before touching — intr_2_mode0 passes with early_lead=1, so the lcdon read frame differs). Legs 1+2 are 2-line trivial diffs (the recipe above); the next session applies them with leg 3 + the OAM/VRAM accessibility legs once lcdon fully passes (don't bank partial — no passing test to pin the intermediate, and it perturbs the C2 gambatte rebaseline).
    - **`hblank_ly_scx_timing-GS` — DEEP deferred-halt residual, no clean lever.** Fails ONLY tier2 (OFF+LE PASS), so the deferred MACHINE. Measures mode-0-STAT-IRQ-halt-wake → LY-increment gap. **`int_hblank_halt` passes all 8 SCX (TIMA-counted wake correct) → the mode-0 IRQ dispatch + wake are right; the LY read is the issue.** Failing read (SCX=0, delay_a=2) lands at **line 66 dot 4** (4 dots past the line-65→66 wrap), reads 0x42, want 0x41 — the post-wake LY read sees the wrap ~1 M-cycle early. NOT fixable by: (a) `m0_halt_hold` (sweep `SLOPGB_HHBASE`/`HHCC` — hblank FAILS at every value; the int_hblank-calibrated wake is fixed); (b) a `vis_ly` read-mask back-date (a uniform offset breaks the delay_b after-increment reads AND shifts WHICH SCX fails 0→1 — per-SCX, no uniform fit, and corrupts the setup poll). Entangled with the dispatch −2 retime + `m0_halt_hold` mask + per-SCX deferred mode-3 length (the L2-sprites class, but for SCX-shifted bare lines). REFUTED #11: flush-at-wake (`bus.flush_pending()` after `set_halted(false)`, before dispatch — `SLOPGB_WAKEFLUSH`) has ZERO effect → the post-wake parked debt is NOT the issue. The clean fix is the deferred halt-exit clock state / post-wake PPU-advance frame ported exactly to SameBoy `GB_cpu_run` — multi-session.
    - **UNIFYING DIAGNOSIS (hblank + lcdon leg 3, #11):** both = the deferred read frame lands ~1 M-cycle LATE following a frame DISCONTINUITY (lcdon = the LCD-enable→glitch-line start; hblank = the halt-wake). lcdon leg-3 PROOF (probed): line-1's mode-3 EXIT boundary is CORRECT (`vis_early` at dot 253, kernel-calibrated) — the failing idx5 read (cycle 174 post-enable) lands at dot 253+ (reads mode 0) when it should land ≤252 (mode 3), so the READ is late, NOT the boundary early. Steady-state reads (kernel/intr_2_mode0/mode3, post-C1.1) are correct; only the post-discontinuity reads drift late. The next lever is the deferred-frame ENABLE/WAKE→read dot mapping vs SameBoy `GB_cpu_run` (dot-level ground truth needed), NOT a per-register read back-date (those break siblings: vis_ly per-SCX-inconsistent, breaks delay_b — measured).
    - **C-stage gate status: mooneye NOT yet zero-SameBoy-drop (hblank+lcdon fail tier2; both SameBoy-PASS → must converge BEFORE the flip). C2 (gambatte 594/233 rebaseline), C3 (flip), C4 (golden) all blocked on C1 completing. Tooling unchanged (`/tmp/slopgb-flip` worktree has the env probes: `SLOPGB_M3ENTRY`/`LYBACK`/`HHBASE`/`HHCC` + glitch tier2-exclusions applied; `SLOPGB_HRAMDUMP` in `run_mooneye` dumps `FF80-FF9F` = mooneye `regs_save`/`fail_*`).**
  - **BEFORE flag-on baseline re-measured this session (worktree `/tmp/slopgb-before` @HEAD+both flags): gambatte 559 regr / 223 stale = net −336; mooneye 90/91 (only `lcdon_timing-GS` ×4).** `before_regr` (`/tmp/before_regr.txt`, the must-stay-⊆ subset) cluster split: render-frame rebaseline (window 105 / scx_during_m3 37 / scy 32 / bgtilemap 30 / bgtiledata 29 ≈ 233) vs dispatch-entangled STAT (halt 55 / lycEnable 34 / m1 27 / ly0 22 / m2int 17). The real convergence gate is mooneye (SameBoy-agreeing; gambatte is rebaselined); render-frame regr resolve via the C2 SameBoy-ground-truthed rebaseline.
  - **A3 SHIPPED this session (`5238bb6`) — per-model write-conflict clock-phase table (byte-identical scaffold, the ONLY genuinely-bankable Group-A piece; A1/A2/A4 are Tier-2-entangled = net-negative until the reclock).** Ported SameBoy's `cycle_write` map (`sm83_cpu.c:31-318`): 15 classes → 5 clock phases (added `EarlyTwo` P-2/repark-6, `WxHold` commit-at-leading-edge/hold-1/repark-3); `Interconnect::write_conflict(addr)` keyed on model; `Bus::write` routes through it. Commit position discarded ⇒ byte-identical; consumed at S6 (B6). Plan + dep-graph: workflow synthesis (`wf_dd561556` result) + [`docs/sameboy-port/PORT-PLAN.md`](../sameboy-port/PORT-PLAN.md).
- **Increments 1-2 (cc+2 OAM + VRAM read)** done: the mode-3→mode-0 OAM/VRAM accessibility unblock trails the m0 IRQ rise by one half-dot (gambatte m0Time xpos+7 vs IRQ xpos+6); a CPU OAM/VRAM read samples at the cc+2 MID phase (2 dots before the tick-then-access end view), so it still reads $FF when the unblock lands in the M-cycle's 2nd half. `Ppu::m0_access_flip` (set in `m0_flip_events` for STEADY-STATE BARE-line flips only — sprite/window/glitch-line phases deferred) → `Interconnect::m0_access_edge` (an `Option<u8>` edge-stamp of the flip's dot-END commit eighth; `stamp_blocks(stamp, obs_phase(addr))` vs the MID observer = the old half-split) overrides ONLY the FE00-FE9F OAM read + CGB FEA0-FEFF mirror (INC1) and the 8000-9FFF VRAM read (INC2, suppressed while an HDMA is armed — the HDMA service seam writes VRAM at the same mode-0 entry, deferred to INC5) and the OAM/VRAM WRITE (dropped at cc+2 MID, same gate); everything else keeps the end view (net-zero except the straddle). A SECOND edge (`Ppu::pal_access_flip` → `Interconnect::pal_access_edge`) anchors the CGB palette-RAM unblock at the pipe end (`render_finished`, 1 dot after the m0 flip / the hdma_lead trigger), routing the FF69/FF6B read at MID. Lifted gambatte oam_access read+write + vram_m3/postread_scx2/scx5_1 (+ds) + vramw_m3end + cgbpal_m3end_ds_1/scx3_1. The clean 2-phase read/write accessibility wedges are largely EXHAUSTED (residual cgbpal_m3end rows want render_finished+2).
- Remaining ladder (each its own TDD+matrix+revert-on-any-non-target-green-regression round): INC3 STAT mode-bit read at MID — DEFERRED (a naive FF41|0x03 at MID lifts ~48 incl. wilbertpol intr_2_mode0_nops but regresses ~32: FF41 is read by many chains with different sub-cycle offsets, so it's the PARKED CPU↔PPU-phase problem, not a clean read analog); INC4 cgbpal/dmgpalette/scx/scy during_m3 (cgbpal_m3 is hdma_start-anchored → needs the g-anchor first); INC5 (atomic) CGB/AGB halt-wake whole-cycle mask + the g-anchored HDMA service-seam re-flag (AXIS-1, see [cpu-interrupts.md](cpu-interrupts.md) halt wake — the 13-row dma trap is the guard; gate to single speed, the 9 ds regressions are class-A/INC6); INC6 the class-A DS half-dot offsets, one cluster at a time. The clean accessibility-read wedges (OAM/VRAM) are EXHAUSTED — INC4/5 both need the HDMA-seam g-anchor foundation.
- **INC-DS-1 (class-A DS, the biggest cluster) done**: the CGB double-speed STAT mode-bit (FF41 bits 0-1) read at cc+2 MID — a third edge `Ppu::m0_stat_flip` (set in `m0_flip_events`, gated to SPRITE-extended lines `r.fetched != 0`, the complement of `m0_access_flip`'s bare gate) → `Interconnect::stat_mode_edge` (the flip's dot-END commit eighth; `stamp_blocks` vs the MID observer = the old half-split) forces the FF41 mode bits to 3 (old mode) when a sprite-line m3→m0 flip lands in the read M-cycle's second half AND `double_speed` AND `lcd_enabled`. Lifted 43 gambatte sprites `m3stat_ds_1` rows (961→918), zero regressions. Two looser axes A/B-rejected: dropping the half-split (whole-M-cycle override) lifts ~84 more but regresses 21 green DS reads wanting mode 0; dropping the sprite gate regresses 5 bare-line reads (dma gdma/hdma_cycles_scx5_ds_2, lcd_offset m0stat_count) — both reach FF41 through the m2-dispatch/DMA/lcd-offset chains at a different sub-cycle offset (the parked multi-chain problem). Remaining m3stat_ds: 9 window (`wx`) cases + the m2int/speedchange dispatch reads + the dropped 21/5.
- **INC-DS-2 (the 9+2 window/scx bare m3stat cases) probed + PARKED 2026-06-14**: no `m0_stat_flip` gate/half-split variant lifts them (drop sprite-gate = 0 lift / 2 regress; whole-M-cycle = 0 lift / 2 regress). Their read line is BARE (no sprite/window active at the flip), the flip fires via the SILENT `advance_lx` pipe-end path (so `m0_stat_flip` is never set), and the targets need the flip a half-dot LATER while the 5 known regressors are already 2nd-half but want mode 0 — an INVERSE relationship only the read-chain sub-cycle phase resolves. The window `m2int_wx*_scx*_m3stat` cluster fails in BOTH SS and DS (not a DS-only sub-dot issue).
- **INC-G1 (eighth-grid scaffold) done 2026-06-14**: the four dot-loop `2*(i+1)>dots` half-splits are now `obs_pre_edge(MID_PHASE, edge_eighth(i, dots))` (interconnect.rs; MID_PHASE=4 of 8 eighths/M-cycle, dot-END edge commit), provably bit-identical (`eighth_grid_predicate_matches_half_split`) — net-zero. Worth: converts the parked one-boolean conflation (when the edge committed vs the single cc+2 observer) into per-edge sub-dot offsets + per-read-chain observer phases. **Honest ceiling (design workflow wnbtdfr1m)**: granularity was never the constraint, CALIBRATION is — the ~100-row sprites+window m3stat cluster is NOT cleanly solvable; addressable pool ≈ low TENS of rows and some chains may be PPU-phase-dependent (no constant offset → net-worse → revert). PARKED (no grid at any resolution lifts): the FF0F-vs-FF0F m2int_m0irq double-read IF-flop race + win*_b/ppu_sprite0 + STAT-mode through non-direct read chains.
- **INC-G2-PROBE done 2026-06-14**: instrumented vs gambatte, the m2-dispatch (interrupt-ISR) FF41 STAT read has a CLEAN CONSTANT observer offset **O=2 eighths (cc+1)** vs the direct-poll/DS MID=4 (the flip lands inside the read M-cycle at edge_eighth∈{4,6,8}; O=2 blocks all, MID leaves edge=4 unblocked) — confirmed across m2int_m3stat base+scx2+scx5 × DMG+CGB. Blocker: m2-dispatch and direct-poll SS reads are bus-indistinguishable yet want OPPOSITE results at edge=4, so the lift needs CPU→bus chain tagging (the dispatch tags its ISR FF41 reads O=2, gate dropped for tagged reads only).
- **INC-G2a+b (edge-stamp + observer-phase plumbing) done** (commit 323b327, net-zero): the three `*_mid_blocked` booleans are now `Option<u8>` edge-stamps (`m0_access_edge`/`pal_access_edge`/`stat_mode_edge`) storing the flip's `edge_eighth`, consumed via `stamp_blocks(stamp, self.obs_phase(addr))`; `obs_phase` returns MID for every chain (`stamp_blocks_matches_half_split` + `default_observer_phase_is_mid`). **THIS is the eighth-grid scaffold's terminal deliverable.**
- **INC-G2c (dispatch FF41 tag) IMPLEMENTED + REVERTED 2026-06-14**: built the full mechanism — `isr_ff41_obs` countdown armed by `ack(STAT bit 1)`, `obs_phase(0xFF41)`=O2 while live; a bare-line stamp `stat_mode_edge_bare` (PPU `m0_stat_flip_bare`) consulted ONLY by tagged reads (sprite stamp kept MID+DS-only, so untagged dma/lcd_offset bare polls stay green); dispatch override single-speed-gated. Calibration: the m2int ISR pads ~60 M-cycles from dispatch to its `ldh a,(FF41)` read at the mode-0 flip, so the tag needs a magic lifetime L≈64 — NOT a hardware constant, test-ROM-specific = curve-fitting. Authoritative full gbtr = **+57 lifts / −34 regressions**: the lifts are the wilbertpol `intr_2_mode0_scxN_timing_nops` chains (the documented A/B swaps the mode-0 grid note says DON'T chase) + gambatte m2int_m3stat `_1`; the regressions BREAK the canonical `intr_2_mode0_timing(+sprites)` mooneye test in BOTH wilbertpol and mooneye2022 (×6 models) — the dispatch tag broadly perturbs the whole STAT-dispatch read chain, flipping the parked mode-0 A/B. REVERTED — the honest-ceiling "PPU-phase-dependent → net-worse → revert" outcome. Don't re-attempt the FF41 dispatch tag without a real sub-cc CPU↔PPU phase model (not a magic ack-countdown).
- **INC-G3 (real sub-cc CPU↔PPU phase model) — FEASIBILITY + TDD-PLANNED, NOT IMPLEMENTED 2026-06-14**: the model G2c lacked. **POSSIBLE in a scoped form**: cc-exact boundary-event positions (from the gambatte xpos formulas already encoded as dot-leads in render.rs `m0_flip_events`: m0 IRQ/flip lead pipe-end by 2 dots / 1 in DS; m0Time=xpos lcd_hres+7, IRQ +6) + a SINGLE fixed CPU-access observer phase (NOT per-chain — G2c's per-chain O=2 was the wrong premise; all accesses sample at the same M-cycle cc-offset since M-cycles are dot-aligned to the PPU) + an IF flip-flop set/read sub-cc RACE (the m2int_m0irq double-read + wilbertpol intr_2_mode0_*_nops cluster). **Hard ceiling**: lifts ONLY geometries where the dot-clocked projection already places the pipe-end DOT correctly; geometries with the dot ±1 off (speedchange `_2`, some scx/sprite/window combos) need a full cc-granular pixel-pipe RECLOCK — OUT OF SCOPE (would recalibrate ~5941 green dot-level cases incl. mealybug photos, mode-3 fetch grid, window machine). TDD plan = net-zero `EdgeKind`/`event_phase` + single `ACCESS_PHASE` scaffold, then per-cluster calibration (cgbpal palette-offset, m3stat, IF-flop race), each full-gbtr-gated with revert-on-regression. **cgbpal probe (reverted)**: a whole-dot `pal_access_delay` (render_finished+N) is A/B — N=1 lifts scx2_1/scx5_ds_1 but regresses ds_1 (an INC4-partial green); ds_1(scx0) wants +0, scx5_ds_1 wants +1 → the palette unblock's sub-dot position depends on SCX%8, only a per-SCX sub-dot offset (the real model) could condition it, and that is itself A/B-risky. Honest-ceiling holds.
- **INC-G3 scaffold (TDD-plan tasks 2-4) done — net-zero**: the `event_phase`/`ACCESS_PHASE` foundation, before any lift. `EdgeKind` enum (`M0Rise`/`M0Access`/`PalAccess`/`StatMode`) + `event_phase(kind,i,dots)` returning `edge_eighth(i,dots)` for every kind (the dot-loop now stamps `Some(event_phase(EdgeKind::X, i, dots))`; the `kind` arg is the seam a per-event sub-dot lead/lag hangs on later — e.g. m0Time=xpos+7 vs IRQ+6) + `const ACCESS_PHASE = MID_PHASE` replacing the per-address `obs_phase(addr)` method at all 7 read/write consumer sites. This bakes in INC-G3's corrected premise: every CPU bus access samples at ONE fixed phase (`ACCESS_PHASE`), so the discriminator between read chains is the EVENT's sub-dot position (`event_phase` per kind), NOT a per-chain observer phase (G2c's reverted mistake). Tests `event_phase_net_zero_except_pal` (the scaffold kinds ≡ `edge_eighth`, both speeds, every dot) + `access_phase_is_single_constant` (`ACCESS_PHASE == MID_PHASE`); the removed `default_observer_phase_is_mid` no longer applies. clippy/fmt/`/rust-diff-review` clean. **NEXT** = the per-cluster lift tasks (5 cgbpal / 6 m3stat / 7 IF-flop race), each full-gbtr-gated, revert-on-regression — honest pool low tens of rows, real A/B risk; the scaffold is the safe foundation, the lifts are the gamble.
- **INC-G3 task 5 (whole-M-cycle CGB palette block) done — +3 rows, zero cross-suite regression**: the first lift on the `event_phase` seam. `event_phase(EdgeKind::PalAccess, ..)` returns `END_PHASE` (8 = cc+4 = M-cycle end) instead of the dot-END `edge_eighth`, so the CGB FF69/FF6B palette read stays $FF for the ENTIRE straddle M-cycle (readable only next M-cycle) — one observer grid later than OAM/VRAM's dot-split, because the palette unblock physically lags the pixel-pipe end (gambatte cgbpAccessible vs m0Time). The earlier half-split under-blocked the geometries where the lx==160 pipe-end falls in the M-cycle's FIRST half. Lifted gambatte cgbpal_m3end `scx2_1`/`scx5_1`/`scx5_ds_1` (out7); NOT an A/B swap — full gbtr confirmed zero cross-suite regression (mealybug photos, age/wilbertpol, every CGB palette read green). Probe path: env-gated `SLOPGB_PALOFF` eighth-offset sweep over the cgbpal glob (`/tmp/calib_g2c.py`) found off≥3 = always-block saturates at +3 lifts/0 cgbpal-regress; off≤2 (dot-dependent half-split variants) only reach +2 → scx2_1's pipe-end lands in the M-cycle's 1st half, so ONLY whole-M-cycle blocking catches it. 599 lib + 439 mooneye + 184/184 gbtr. Residual cgbpal floored: the m3end `_3` (out0) rounds + plain `_1/_2/_3` + m3start/read/write_m3start (different read offset / write cell, not the straddle block). Tests `pal_access_blocks_whole_mcycle` + the updated `cgb_palette_read_mid_override_returns_ff`.
- **INC-G3 task 6 (single-speed m3stat) probed + BLOCKED, task 7 out of scope 2026-06-14**: dropping the `double_speed` gate on the FF41 STAT-mode override (to extend INC-DS-1's block to single speed) measures **LIFT=0 / REGRESS=30** over the m3stat suites (env probe `SLOPGB_STATSS` + `/tmp/calib_g2c.py`, reverted, nothing committed). Single- and double-speed FF41 reads share ONE `stat_mode_edge` stamp and `event_phase` is speed-agnostic, so the green single-speed direct-poll reads (enable_display, sprite-count, m0int_m3stat, ine_m3stat) — which want mode 0 — get wrongly forced to mode 3. Isolating single speed needs a speed/chain-conditioned phase = the per-read-chain CPU→bus tagging G2c proved breaks the canonical `intr_2_mode0_timing` mooneye test. This is the SINGLE-speed sub-axis of task 6; it stays blocked. The DOUBLE-speed sub-axis was the actual lift — see the next bullet.
- **INC-G3 task 6 (double-speed m3stat whole-M-cycle block) DONE — +84 rows 2026-06-14**: `event_phase(EdgeKind::StatMode)` now returns `END_PHASE` (like `PalAccess`/task 5) instead of the dot-END `edge_eighth`, so a sprite-line m3→m0 flip holds the cc+2 FF41 mode bits at the pre-flip mode 3 for the WHOLE straddle M-cycle, not only when the flip lands in the M-cycle's 2nd half. INC-DS-1's half-split caught only the +43 2nd-half-flip rows; the whole-M-cycle block adds the +84 residual sprite `m3stat_ds_1` (out3) rows whose flip lands in the FIRST half. Full-gbtr ratchet = **+84 / −3** (gambatte floor 915→831, total gbtr floor 1103→1019; net −84): the only regressions are the 3 `late_sizechange_sp00/01/39_ds_1` (out0), a net-neutral in-cluster A/B swap — their `_ds_2` siblings (out3) are in the lift, and whole-M-cycle forces both same-line size-change reads to mode 3 (the `_2` want it, the `_1` do not; no `event_phase` offset separates two reads in one M-cycle). ZERO cross-suite cascade (age/mealybug/gbmicrotest/mooneye2022/blargg/wilbertpol green; 439 mooneye + 600 lib green). The half-dot-grid branch's net-positive-trades policy is what makes this commitable (INC-DS-1 had MEASURED the same +84 but kept the zero-regression endpoint for main's hold-floor). Kept gates: sprite-line (`m0_stat_flip = r.fetched != 0`) and **`double_speed` (load-bearing — single-speed shares this stamp but wants mode 0, the blocked sub-axis above; pinned by `stat_mode_override_requires_double_speed`)**. Residual m3stat_ds floored: window `wx` (silent advance_lx flip path, `m0_stat_flip` never set), m2int/speedchange dispatch reads, speedchange `_2` (dot-±1-off).
- **CC-RECLOCK foundation (net-zero cc-granular tick) DONE 2026-06-14**: the documented class-A lift mechanism ("re-clock observable event commits to a half-dot (8 MHz) grid", gambatte.txt floor-class A) — its safe scaffold. The dot loop (`interconnect/tick.rs`) was `for i in 0..dots` (dots=4 single / 2 double speed), which LOCKS the CPU↔PPU phase: in double speed PPU dot boundaries land only at even cc within the M-cycle ({2,4}, eighths {4,8}), so events can never sit at odd cc. The reclock makes the M-cycle advance one CPU cc at a time — `for cc in 1..=4` gated by `dot_ticks_on_cc(cc, ds, dot_phase)` (single speed ticks a dot every cc; double speed ticks 2 dots, the even cc {2,4} at phase 0, the odd cc {1,3} at phase 1) — each dot stamping its `cc_eighth(cc)` (= the single-speed dot-END `edge_eighth`, so the cc grid IS the single-speed dot grid). `event_phase(kind, cc)` replaces `event_phase(kind, i, dots)`; a new `Interconnect::dot_phase: u8` field (default 0, not yet set anywhere) carries the offset. **Net-zero proof**: phase 0 reproduces the old loop's dot-tick set + eighth sequence exactly (`cc_grid_matches_dot_loop`); full gbtr 184/184 ZERO baseline delta + 439 mooneye + 602 lib + clippy `-D` + fmt clean. `/rust-diff-review` (mine + independent cavecrew-reviewer, which re-traced the SS/DS mapping by hand) = 1 🔵 (cc helpers lacked `edge_eighth`'s `debug_assert!` contract guard — `cc_eighth(0)` would underflow) → fixed; re-review clean. Net-zero confirmed on commit 22ee6fc.
- **CC-RECLOCK Phase-0 (golden net) + R3 (single-speed FF41 MID read) MEASURED 2026-06-15**: pursuing SameBoy parity (the 79 wilbertpol rows — 66 intr_2_mode0_*_nops + 8 hblank + 4 ly_*-C + 1 defect; mooneye 439 + blargg already at SameBoy's bar). **Phase-0 committed (a77a011)**: `tests/gbtr/golden.rs` — a per-ROM frame-hash (FNV-1a) + audio verdict for all 9020 (rom×model) cases under the 16-frame protocol; `SLOPGB_GOLDEN=capture` writes the snapshot, default diffs + fails on drift; deterministic. The drift net the reclock needs (baselines catch pass/fail; this catches silent pixel drift). **Spike (task 0, env `SLOPGB_STATK`, reverted) RESOLVED the architecture crux**: sweeping the FF41 mode read's intra-M-cycle dot K on DMG — K=0/4 (end view) = 2/8 scx_nops, K=1 = 8/8 but BREAKS the canonical, **K=2 (cc+2 MID) = 6/8 with the canonical safe**. So the cc-event/access-phase model is net-positive in principle (frames untouched → golden green); NOT the feared full pixel-RENDER reclock. **R3 (single-speed bare-line FF41 mode read at cc+2 MID via the existing `m0_access_edge`, bare-gated so `intr_2_mode0_timing_sprites` stays green) MEASURED on full gbtr = +19 / −23 A/B swap** (reverted). The lifts are the well-positioned `_1` families (m2int_m3stat, scx_during_m3 — clean, no sibling regress); the −23 are speedchange/lcd_offset/window `_2`/`_3` rows wanting mode 0 that now read mode 3 — because **our mode-0 flip lands one M-cycle too late in those geometries**, so the `_2` read (one M-cycle after `_1`) wrongly hits the flip's M-cycle. **VERDICT: the MID read is necessary but not sufficient — full parity needs the cc-exact flip POSITION (the pixel-pipe-end reclock) so `_1`/`_2` separate by their M-cycle, with the MID read on top.** The MID read EXPOSES pre-existing flip-position errors (masked by the end-view read) in speedchange/lcd_offset/window/CGB. **CLOSED 2026-06-20 — R3 re-measured + classified END-TO-END (global spike + a DMG-gated attempt, both reverted; nothing shipped). Not a harvestable lift on EITHER model:** the +19/−23 gambatte swap splits cleanly by model (model IS a runtime read-site discriminator — `self.model.is_cgb()`; the same ROM `late_wy_FFto2_ly2_scx5_2` lifts on Dmg / regresses on Cgb precisely because `m0_flip_events` already model-conditions the flip geometry, mode0.rs). So a `!model.is_cgb()` gate isolates a DMG slice that is gambatte +4/−1 (m2int_scx{2,5}_m3stat_1 + scx_m3_extend_1 [Dmg] lift; a net-neutral `late_wy_scx5_2/_3` [Dmg] sibling swap rides along), CGB byte-identical. **But the DMG slice is the CROSS-ORACLE swap the project deliberately rejected**: measured on the FULL battery it ALSO lifts +16 wilbertpol `intr_2_mode0_scx{1,2,5,6}_timing_nops` ([Dmg]/[Mgb]/[Sgb]/[Sgb2] — class-E 2016-era expectations) while REGRESSING 4 currently-GREEN gbmicrotest `ppu_sprite0_scx{1,2,5,6}_b` [Dmg] (class-H one-dot conflicts the mode-0 grid pins — gbmicrotest.txt "don't chase one-sidedly"). Dropping SameBoy-aligned hardware rows to gain the rejected wilbertpol side violates the oracle policy (CLAUDE.md: never drop a test SameBoy passes). **The CGB slice** is the −22 flip-POSITION swap (class-A `lcd_offset` + class-B `speedchange` + window + display-start): `m0_flip_events` projects the flip from render state ALONE and carries NO state distinguishing those geometries, so the correction IS the out-of-scope pixel-pipe reclock (the dead-end below). Golden drift on both runs was CONFINED to the STAT/mode-0/sprite0 cluster (intr_2_mode0_nops, ppu_sprite0_b, late_disable, spsw-mode0, + the lift/regress ROMs) — ZERO mealybug-photo / game / mode-3-fetch-grid drift. So R3 is irreducibly closed: the DMG slice by the class-E↔H cross-oracle tie-break (same wall as INC-G3 task 7), the CGB slice by the pixel-pipe reclock. This converts the prior "out of scope by assertion" into "out of scope by end-to-end measurement". Pinned closed by `single_speed_ff41_keeps_end_view_under_every_edge` (both models; no single-speed FF41 sub-dot override ships). mooneye stayed 439 green throughout; 603 lib.
- **CC-RECLOCK LIFT (event-layer phase) PROBED + DEAD-END 2026-06-14 (probe reverted, nothing committed)**: the gating measurement (TDD-plan task 4). Wired `dot_phase` at the entering-DS STOP path (env probe `SLOPGB_DOTPHASE` = const-1 / `cycles`-parity / `cycles/2`-parity) and swept run_gambatte + a baseline-aware classifier over the speedchange/`_ds`/m3stat/wx CGB rows. **RESULT: LIFT = 0 across ALL phase candidates** (287 baselined rows), **REGRESS = 0** over 1002 currently-passing DS rows. The probe IS active (verified: it fires at the switch and *does* change the speedchange `_2` output — e.g. `_outC0` becomes `C3…`, the FF41 read returns mode 3 when mode 0 is wanted), but never to the CORRECT value. Root cause (the honest ceiling, now confirmed end-to-end): the m3stat / speedchange `_2` reads are served by the **cc-invariant `END_PHASE` StatMode/PalAccess overrides** (tasks 5/6), so shifting WHICH cc the M-cycle samples doesn't touch them; and the discriminator (`_2` wants mode 0, the dot-clocked projection gives mode 3) is the **pixel-pipe END dot position**, which `dot_phase` does NOT move — it only shifts when the M-cycle samples the (unchanged) pipe. Moving the pipe-end dot is the **full pixel-pipe cc-granular reclock** — recalibrates ~6028 green dot-level cases (mealybug photos, the mode-3 fetch grid, the window machine) — a separate architecture, NOT a revert-on-regression increment. **The event-layer reclock (the committed foundation) is the terminal deliverable; the EVENT-phase lift vein is empty. The pixel-pipe reclock remains the only lever and is out of scope.** The IF-flop intr_2_mode0_nops / m2int_m0irq cluster stays baselined regardless (cross-oracle irreducible — siding with gambatte forfeits 46 gbmicrotest + 117 gambatte rows; pinned by `stat_mode_override_requires_double_speed`).
- **INC-G3 task 7 (IF-flop set/read race) OUT OF SCOPE — cross-oracle irreducible, ceiling pinned 2026-06-14**: the single-speed m2int_m0irq / wilbertpol intr_2_mode0_*_nops cluster is a fully-characterized cross-oracle swap, not a missing calibration — gbmicrotest hblank_int_scx*_if pins the very FF0F dots gambatte's m2int reads contradict (siding with gambatte forfeits 46 gbmicrotest + 117 gambatte + 2 mealybug + 2 age rows, see wilbertpol.txt intr_2_mode0 note), and G2c proved the ack-countdown tag that lifts the gambatte side breaks the canonical `intr_2_mode0_timing` mooneye test. It needs a real sub-cc IF flip-flop race model or a full cc-granular pixel-pipe reclock (recalibrates ~5944 green dot-level cases incl. mealybug photos), NOT an `event_phase` offset. Not attempted; the executable ceiling pin is `stat_mode_override_requires_double_speed` (the same single-stamp/opposite-wants ceiling task 6's single-speed axis hits). **The `event_phase` lift vein: task 5 (+3 cgbpal) + task 6 DS (+84 m3stat); the single-speed/IF-flop axes hold at the cross-oracle ceiling.**
- **PIXEL-PIPE RECLOCK (event-layer hybrid) — IN PROGRESS 2026-06-20 (user: implement "by any means")**: the prior "out of scope" verdict reopened. Architecture (design workflow `wf_01ff60a1`, 5 mappers + 4 architects + judge, chose Architect-D's hybrid): eighth-denominate ONLY the EVENT/flip layer (mode-0 flip / accessibility / palette / STAT commit positions); keep every pixel pop (`advance_lx`/`fetcher_step`/`output_pixel`) WHOLE-DOT. Pixels never move, so the golden 9020 frame-hashes + the 55 mealybug HARDWARE photos stay byte-identical BY CONSTRUCTION (not recapture) — collapsing the documented ~6028-case blast radius to CPU read-results of FF41/OAM/VRAM/palette only. The full-pixel reclock (Architect C) was rejected: it moves pixel pops → breaks mealybug PNGs (external hardware truth, NOT recapturable) → forbidden by the never-drop-a-SameBoy-pass rule. Stages, each full-gbtr+golden+suite-gated, revert-on-any-non-target-regression: **S0 DONE (net-zero, this commit)** — `event_phase(kind,cc)` → `event_phase(kind,cc,lead_eighths:i8)` (signed sub-dot offset, clamped 0..=END_PHASE; lead 0 = identity, pinned `event_phase_lead_shifts_and_clamps` + `event_phase_net_zero_except_pal_and_stat`); gbtr 183/183 + golden byte-identical + mooneye 439. S1 = eighth-denominate `m0_flip_events` proj/lead + thread a per-flip `lead_eighths` (net-zero). S2 = PalAccess per-SCX lead (cgbpal residue probe). S3 = M0Access/StatMode flip-position lead (m3stat). S4 = speed-switch/lcd_offset state (speedchange `_2`, class B). Kill criterion (from the plan): if the isolated S2 probe lifts 0 with golden byte-identical, the floor is a geometry-STATE gap not a grid-RESOLUTION gap — but per the "by any means" mandate, probe the m3stat vein (S1+S3) before concluding, and keep the S0/S1 scaffold regardless.
- **PIXEL-PIPE RECLOCK — OUTCOME 2026-06-21 (scaffold DONE, lift vein EMPTY, ceiling re-confirmed by measurement):** S0+S1 shipped net-zero on branch `pixel-pipe-reclock` (commits 050eedb, 126b924): the `event_phase(kind,cc,lead_eighths)` hook + per-flip `lead_eighths` threading (the 3 flip flags are `Option<i8>`). Every lift lever was then exhaustively SWEPT (env-tunable probe + run_gambatte, reverted): **S2 (PalAccess per-SCX lead) = 0 lift** — cgbpal `_3` reads the wrong palette BYTE (read-VALUE/auto-increment artifact, not a sub-dot unblock-timing artifact; constant/per-SCX lead can't touch it). **S3-flip-lead (StatMode/M0Access sub-dot lead over m3stat/scx/speedchange/sprites, 900+ ROMs) = 0 lift** (probe proven to reach the StatMode edge — FLIP_D=-5 correctly regresses the task6-lifted sprites m3stat_ds): the floored class-A/B reads are the PPU **END-VIEW** (the mode-3→mode-0 boundary dot is whole-dot-late), which a sub-dot lead cannot move. **S3-boundary-event (decouple `line_render_done`+`m0_access_flip` from the IRQ via `lead_mode=lead-ds`, design workflow `wf_30414d3b`) = +5 / −12, REVERTED**: it lifts +5 DS `m2int_m3stat_ds_1` (mode-3 held one dot longer) but regresses −12 green DS rows — the bare-line DS FF41 chains (`dma/gdma/hdma_cycles_scx5_ds_2`, the parked multi-chain: the +5 m2int read and the −4 dma/hdma read are the SAME bare-line DS FF41 mode read via different CPU dispatch chains, wanting OPPOSITE modes — inseparable at the PPU, and G2c proved a CPU-side chain tag breaks the canonical mooneye `intr_2_mode0_timing`) + INC1/INC2 DS OAM/VRAM accessibility (`oam_access/vram_m3` postread/prewrite/`vramw_m3end` `_ds`). mealybug 55 photos + gbmicrotest + age + wilbertpol + mooneye 439 all stayed GREEN (the hybrid is photo-safe by construction — confirmed). **CONCLUSION (measured this session + 3 design workflows + all prior sessions agree): the class-A (~397) + class-B (~165) floor is the true CROSS-ORACLE / MULTI-CHAIN / IRQ-dot-pinned ceiling — NOT liftable photo-safe.** Lifting it needs either (a) a CPU→bus read-chain tag (breaks the canonical mooneye intr_2_mode0, the gbmicrotest FF0F oracle — forbidden), or (b) a full pixel-pop reclock that moves the mealybug HARDWARE photos (non-recapturable hardware truth — forbidden). The `event_phase`/`lead_eighths` scaffold (S0+S1) is the terminal deliverable: the correctly-built event-phase reclock foundation, ready for any future per-event sub-dot calibration that a new (non-multi-chain) geometry might admit; all leads are 0 today, so it is net-zero. The eighth-grid sub-dot model is at its true ceiling.
- **HALF-DOT GRID — IRREDUCIBILITY PROOF 2026-06-21 (the major-rewrite attempt + numeric "cannot"):** the mandated "real sub-cc CPU↔PPU phase model" (the last untried major rewrite — design workflow `wf_00fecbf4`, 9 agents, instrumented) was reduced to a numeric impossibility, VERIFIED here by direct run. The class-A/B FF41 floor's irreducible kernel is the pair `gambatte/m2int_m3stat/m2int_m3stat_1` (wants **out3** = mode 3) and `gambatte/m0int_m3stat/m0int_m3stat_2` (wants **out0** = mode 0). BOTH are the SAME opcode `ldh a,(FF41)` reaching `Interconnect::read`→`tick_machine`→`read_no_tick`, and two independent probes + a direct `run_gambatte` measured them landing on the **byte-identical modeled bus M-cycle** (cyc=772, ly=1, dot=256, boundary eighth E=4) — our emulator returns the SAME value to both (verified: both print `0`; m0int passes, m2int is the baselined fail). ANY sub-cc timing model computes `observed = (O < E ? mode3 : mode0)` where `O` is the read's bus sub-cc offset (a per-OPCODE property → identical for two identical `ldh`) and `E` is the boundary's sub-dot eighth (a per-PPU-STATE property → identical for the same bus M-cycle) — exactly `stamp_blocks(stat_mode_edge, ACCESS_PHASE)`. Imposing both wants: m2int(mode3) ⟹ O<4; m0int(mode0) ⟹ O≥4. **O<4 ∧ O≥4 is UNSATISFIABLE** — a pure function cannot return two values for one input. The only discriminator is *which ISR is on the call stack* (mode-2 OAM vs mode-0 HBlank) = CPU control-flow context, which is **neither a bus offset nor a PPU sub-dot → outside the entire half-dot/sub-cc model space**. Tagging that context is the reverted G2c, which broke the canonical mooneye `intr_2_mode0_timing` ×6 + the gbmicrotest FF0F oracle. **The contradiction is INTERNAL to gambatte** (not cross-suite): gbmicrotest (cc+4 accessibility) + mooneye (cc+4 FF41 vs the dot-254 flip) + mealybug (the pixel-pinned flip dot) are all mutually consistent + green, and any move of `O` (R3 +19/−23) or `E` (the flip dot, pinned by mealybug HARDWARE photos + 46 gbmicrotest + 117 gambatte) regresses them. **CONCLUSION: the half-dot grid is at its mathematically-proven ceiling.** The event-phase scaffold (S0+S1) is the complete, correct implementation of the model; the residual class-A/B floor is not a model gap or an effort gap — it is a CPU-context discriminator no timing model (half-dot, eighth, or finer) can represent. Falsification (would reopen it): a re-instrumented FF41 probe showing the pair on DIFFERENT bus M-cycles (distinct cyc/dot or E) — measured identical (cyc=772/E=4), so the proof stands.
- **FINAL (2026-06-21) — the floor is a DOCUMENTED CROSS-ORACLE contradiction, lift-condition is an unproven from-scratch sub-dot IF-flop CPU re-architecture, one-sided lift is FORBIDDEN.** Refining the numeric proof above with the upstream evidence: the kernel reads ARE separable in principle by the CPU-ISR-context observer phase (the m2-OAM-source ISR's `ldh a,(FF41)` observes at cc+1 / O=2 per the G2-PROBE measurement, vs the m0-HBlank-source ISR / direct poll at cc+2 / O=4 — so O is per-DISPATCH-CONTEXT, not per-opcode; the proof's "O identical" holds only within our single-`ACCESS_PHASE` model). BUT lifting the gambatte side forfeits the modern corpus: `baselines/wilbertpol.txt` header — "whole file is class E (2016-era expectation chains REJECTED in favor of the modern corpus — lift condition: a sub-dot IF-flop model, class H's precondition, MAY reconcile both grids; re-probe every row then) ... Do not chase any -C or _nops row one-sidedly: each cluster names the modern rows that pin the opposite side of its trade." The `intr_2_mode0` family (gambatte 2016) and the gbmicrotest hblank_int/FF0F grids + mooneye2022 (the modern, CPU-CGB-04-verified corpus the project sides with) measure the SAME mode-0/STAT read and CONTRADICT — an upstream test-suite disagreement, not our bug. G2c implemented the dispatch-context tag and EMPIRICALLY confirmed it: +57 gambatte / −34 that BREAK the canonical mooneye `intr_2_mode0_timing(+sprites)` ×6 models + the gbmicrotest oracle. So lifting the class-A/B/E/H floor would DROP SameBoy-passing hardware tests — forbidden by the project's load-bearing invariant ("never drop a test SameBoy passes"). The documented lift-condition ("a sub-dot IF-flop model MAY reconcile both grids") is (a) UNPROVEN (the docs say "may", "re-probe every row then") and (b) a from-scratch instruction-level CPU IF-flip-flop / sub-cc bus-access re-architecture replacing the tick-then-access (cc+4) contract — recalibrating all ~7000 cases, all-or-nothing, not a revert-on-regression increment. **DELIVERABLE: the event_phase/lead_eighths half-dot scaffold (S0+S1) is the complete, correct, net-zero implementation of the event-phase sub-dot model. The residual floor is the GB-emulation frontier — a cross-oracle contradiction whose only (unproven) reconciliation is a from-scratch sub-dot IF-flop CPU re-architecture; until that lands and is proven to reconcile both grids, the project correctly sides with the modern hardware corpus and baselines the gambatte 2016 rows.** Re-open ONLY by implementing the sub-dot IF-flop CPU model as its own multi-phase work package and re-probing every class-E/H row (per wilbertpol.txt).
- **EMPIRICAL CONFIRMATION 2026-06-21 (the dispatch-context lift IMPLEMENTED + measured, not just cited):** the refined source-gated lever was BUILT and run on the full battery (reverted, nothing shipped). Mechanism: `Ppu::last_stat_rise_oam` (set in `stat_events_tick` when the mode-2/OAM source raises STAT IF) → `Interconnect::in_m2_stat_isr` (captured at the STAT `ack`, cleared at `RETI` via a new `Bus::on_reti`) → the FF41 mode read observes at cc+1 (`M2_ISR_OBS_PHASE`=2) instead of MID inside the OAM-source ISR. This DID separate the kernel pair (verified: `m2int_m3stat_1`→3 lifted, `m0int_m3stat_2`→0 held) — so the discriminator IS trackable state, refuting the "no model can represent it" framing. But the full-gbtr ratchet measured **+28 gambatte / −21 gambatte / + the 2016 wilbertpol `intr_2_mode0_*_nops` family / − the MODERN CANONICAL `mooneye-test-suite/acceptance/ppu/intr_2_mode0_timing` ([Dmg]+[Cgb]) + mooneye2022 + `intr_2_mode0_timing.gb` wilbertpol**: (a) the `in_m2_stat_isr` flag spans the whole ISR, so it cannot separate the `_1` read (wants out3) from the `_2` read (wants out0) — −21 `_2` siblings flip to mode 3; and (b) `intr_2_mode0_timing` is ITSELF a mode-2-source ISR, so the cc+1 phase shifts its reads and BREAKS the canonical — the exact wilbertpol.txt "siding with gambatte forfeits the modern corpus" trade, dropping SameBoy-passing tests (FORBIDDEN). This is the cross-oracle contradiction confirmed by construction: the gambatte 2016 `m2int_m3stat`/`intr_2_mode0_nops` grids and the modern mooneye/mooneye2022 `intr_2_mode0_timing` canonical want OPPOSITE results from the same mode-2-ISR FF41 read; no observer phase satisfies both because the same `in_m2_stat_isr` value covers both ROMs' reads. The lift is real but inseparable from the canonical regression → the project correctly baselines the gambatte rows and keeps the modern canonical green. **This closes the half-dot grid: every lever — event-lead, dot_phase, whole-dot boundary, sub-cc CPU dispatch-context — has now been IMPLEMENTED and measured; all either lift nothing or forfeit a SameBoy-passing oracle. The scaffold is the terminal deliverable.**
- **CORRECTION 2026-06-21 — the "irreducible / cross-oracle contradiction" verdict above is WRONG; the floor IS liftable.** Built SameBoy's tester + a gap-finder and MEASURED: SameBoy PASSES ~420 of our baselined gambatte rows (m2int_m3stat, cgbpal, scx_during_m3, vramw, speedchange, sprite_late, lcd_offset), AND passes BOTH m2int_m3stat (out3) and mooneye intr_2_mode0_timing — so it is NOT a contradiction, just our model being too coarse. SameBoy resolves it with a cycle-exact (T-cycle) timing model. The kernel pair m2int_m3stat_1 / m0int_m3stat_2 both read FF41 at the identical dot 256 in our model (collapse); SameBoy lands them in different M-cycles. The path to SameBoy parity is to port SameBoy's cycle-exact timing core (deferred-commit reads + decoupled mode/mode_for_interrupt + cycle-exact STAT-IRQ dispatch + write-timing) — a multi-session re-architecture, FEASIBLE (SameBoy's model passes everything incl. mealybug + mooneye). Full validated finding, diagnosis, staged plan + tooling: **docs/sameboy-parity-plan.md**. The event_phase/lead_eighths scaffold (S0+S1) is a correct-but-insufficient half-dot approximation; the cycle-exact core supersedes it.
- **CYCLE-EXACT PORT BEGUN 2026-06-21 (spec + foundation shipped, branch-green; lift confirmed atomic/multi-session).** Acting on the user's "begin the cycle-exact core port" mandate: (1) **Re-verified the finding** independently — SameBoy's tester renders `3` (non-blank OCR `300000`) for the kernel `m2int_m3stat_1`, and 332/456 gap rows have non-zero expectations (real signal, not blank-screen false-positives); the OCR gap-finder's out0 rows (124) are the only false-positive-risk subset. (2) **Wrote the full source-grounded port spec** (3 parallel opus agents over SameBoy 1.0.2): `docs/sameboy-port/cpu-timing-map.md` (deferred-commit `pending_cycles`, leading-edge cc+0 sample, the `pending-=2` dispatch retime, the conflict-write table), `ppu-timing-map.md` (the kernel resolver = decoupled `mode_for_interrupt` + mode-2-IRQ-1-dot-before / mode-0-IRQ-1-dot-after, NOT call-stack), `slopgb-core-map.md` (our cc+4 seams), `PORT-PLAN.md` (staged S0–S7 roadmap) + a `/tdd-test-plan` task list. (3) **Adversarial opus judge** confirmed no single-session green LIFT exists and ranked a *passing* shadow clock as the best foundation. (4) **Measured the last untried lever** — single-speed mode-0 IRQ +1-dot split (`SLOPGB_M0LAG` probe, reverted): full gambatte_matrix = **+14 / −25** (net −11 A/B swap), joining R3 (+19/−23) and S3-boundary (+5/−12); the boundary move without the read-phase change regresses 25 green rows, confirming the lift is the atomic foundation, not a lever. (4b) **Measured the COMBINED kernel fix** (`SLOPGB_COMBO`: IRQ-split + single-speed leading-edge/MID FF41 read TOGETHER, reverted) = **+33 / −48 gambatte that ALSO cascade-breaks mooneye2022 + gbmicrotest_dmg + wilbertpol + age + drifts 146 golden frames** — conclusive: the lift moves the IRQ-dispatch dot (pinned by COUNTER-based gbmicrotest/mooneye2022 tests an FF41-read-phase change can't compensate) AND drifts rendered frames, so it is inseparable from the global cc-exact reclock. No event-layer lever or combo lifts green. (5) **Shipped the validated, inert deferred-commit clock** `src/cycle_clock.rs` (+5 tests pinning `sm83_cpu.c` §2.1/§3/§6), byte-identical (unwired; wired at Stage S2+S3). The remaining lift = the atomic multi-session Stage 1 per `PORT-PLAN.md`.
- **PORT EXECUTION S1→S2d 2026-06-21 (S0–S2c shipped green; S2d measured the atomic boundary with hard numbers).** Executed the staged port via `/tdd-test-plan`, each stage rust-diff-review-clean + committed + pushed, branch green throughout: **S1** wired `cycle_clock` into the live `Bus` (5 M-cycle methods + `flush_pending` at the `step` boundary; write-only, net-zero gate confirmed byte-identical); **S0** the `#[ignore]`'d kernel-pair acceptance test (m2int→3 ∧ m0int→0) — note **m0int already passes**, only m2int is the floor row, so the lift is *directional*; **S2a** the leading-edge (cc+0) FF41 read path behind `leading_edge_reads` (default off → byte-identical); **S2b** the decoupled `mode_for_interrupt` PPU field (inert, the mode-2 lead at dot 3 / mode-0 lag via a 1-dot-delayed `line_render_done` mirror); **S2c** validated the cycle-exact mode-3 length (`ModeTimeline`, 247+SCX&7) against the live flip and **measured the reclock magnitude: live flip = SameBoy + 7 dots, CONSTANT across SCX** (a +4 line-start mode-0-window offset + a +3 longer mode 3). **S2d MEASUREMENT** (`SLOPGB_LEADING_EDGE` probe over the live Bus, reverted): leading-edge reads ALONE flip BOTH kernel reads 0→3 — **m2int lifts to 3 (correct) but m0int ALSO flips to 3 (regression, wants 0)** — they cannot separate without the `mode_for_interrupt` anchor swing driving the IRQ-dispatch dots. Full `gambatte_matrix` with leading-edge on = **+107 lifts / −320 regressions (net −213**, 4228/5272 pass vs ~4441 off): a strongly net-negative A/B swap, even after rebaseline the new frame passes FEWER gambatte rows. This + the prior COMBO (+33/−48 + breaks mooneye2022/gbmicrotest/wilbertpol/age + 146 golden) is the conclusive empirical proof that the kernel-pair lift is the **whole cc-exact reclock** (leading-edge read + anchor swing + the +7 boundary shift + ~7000-case rebaseline + golden recapture, all landing together), NOT any single lever — exactly the atomic multi-session Stage S2+S3. **Continuation (next session):** wire S5 (`GB_STAT_update` rising-edge on `mode_for_interrupt`|LYC) + the leading-edge read + the +7 reclock TOGETHER, then S3 rebaseline gambatte + recapture the 146 golden + verify no SameBoy-passing oracle (mooneye/wilbertpol/age/mealybug) drops; S4/S6/S7 follow the back-dated frame.
- **PORT EXECUTION S2+S3 SPIKE MEASURED 2026-06-21 (the engine swap wired end-to-end for the first time; reverted, branch stayed green; TDD plan saved).** Acting on the "implement remaining tasks via a measured spike, stay green" mandate. Wrote the remaining-stage TDD plan (`docs/sameboy-port/TDD-PLAN.md`, S2+S3/S4/S6/S7). Then ran ONE atomic spike in two env-gated configs (`SLOPGB_SPIKE`, fully reverted — nothing shipped): **Config 1 = leading-edge (cc+0) FF41 read + the +7 mode-0 boundary reclock** (`lead += 7` in `m0_flip_events`, gambatte STAT engine kept) = **gambatte +110 / −533 (net −423**, 4018/5272 pass) — *worse* than the S2d leading-edge-alone (+107/−320). The kernel `m2int_m3stat_1` reverts to **0** (not 3): the reclock moves the visible mode→0 boundary earlier, but Config 1 leaves the mode-2 IRQ dispatch on the OLD engine's dots, so the read lands on the wrong side of the moved boundary — the same atomic desync, now in the read↔boundary direction. **Config 2 = Config 1 + the SameBoy `StatUpdate` (`GB_STAT_update`) rising-edge engine** driven off the decoupled `mode_for_interrupt` (replacing `stat_events_tick`) — the FIRST end-to-end measurement of the engine swap (prior sessions only measured leading-edge alone): kernel m2int STILL 0 (the bare engine never fires the m2 IRQ for that ROM); **gambatte +149 / −784 (net −635**, 3806/5272); **mooneye −51** including the CANONICAL `intr_2_mode0_timing`/`_sprites` + `intr_2_mode3_timing` + `intr_1_2_timing` + `intr_2_0_timing` + `intr_2_oam_ok_timing` + `hblank_ly_scx_timing` + `vblank_stat_intr`; **mooneye2022 387/439 (−52)**; **mealybug (55 hardware photos), wilbertpol, age, gbmicrotest_dmg, scribbltests ALL fail**; **golden = 1549 frame drifts**. **Verdict (corroborated with the engine wired this time, not just inferred from leading-edge-alone):** in THIS partial wiring the three pieces (leading-edge read + boundary reclock + STAT-engine swap) are NOT additive toward green — the bare `StatUpdate` engine without its surrounding SameBoy machinery (cycle-exact `ly_for_comparison`/LYC delays, the S6 per-model write-conflict table, the full `mode_for_interrupt` line-start/VBlank pulse scheduling, the halt-late commit masks, the STAT-write glitch triggers) drops the entire STAT-timing oracle set. A single INCOMPLETE-port spike cannot by itself prove a *faithful* port can't converge (SameBoy's full model demonstrably passes everything, incl. mealybug + mooneye — the port is feasible); what it does is **corroborate** the prior multi-session finding (S2d −213, COMBO +33/−48, M0LAG +14/−25) with the engine actually in the loop, and pin *exactly which machinery must land together* with the engine. (Config-1 numbers are from the reverted env-gated run — re-derive via the `SLOPGB_SPIKE` harness below; only the captured Config-2 outputs persist.) **Shipped net-zero this session:** the TDD plan + the **S5-refine line-0 OAM pulse** in `update_mode_for_interrupt` (line 0 pulses `mode_for_interrupt=2` AT the visible mode→2 edge / dot 4 — `display.c:1792`'s unconditional set, no early lead per `:1778` — matching `ModeTimeline::mode2_irq_offset(0)==0`; the field is still inert / `#[cfg(test)]`-read only → byte-identical gate). The remaining S5-refine (VBlank-entry mode-2 source, `display.c:2138`) and the full S2+S3 convergence stay the documented multi-session continuation.
- **PORT EXECUTION Phase-A 2026-06-21 (3/5 flag-gated foundation pieces SHIPPED green + net-zero; fresh kernel measurement pins the reclock as the separator).** Continuation of the S2+S3 atomic convergence: built the surrounding SameBoy machinery behind the `leading_edge_reads` master flag, each commit byte-identical flag-OFF (gbtr+mooneye gate green, exit 0), adversarial-review-clean, pushed. (A2, `afe64c4`) `update_mode_for_interrupt` now carries the OAM (mode-2) IRQ source high across the whole visible line-start window (dots 0-3 = 2 on lines 1-143, the `display.c:2138` end-of-line set + `:1781` re-set), not only the dot-3 pulse — the "OAM int 1 T-cycle before STAT" lead as a sustained carryover; + a regression test pinning the VBlank `mode_for_interrupt` timeline (line 144 HBlank carryover→mode 1 at dot 4; 145-153 hold 1; the per-line DMG OAM pulses + line-144 OAM IF pokes are direct `IF|=2` writes, not `mode_for_interrupt` transitions). (A1, `fa2c1a2`) `ly_for_comparison()` — SameBoy's delayed LYC-compare schedule (`display.c`): visible lines prev-line carryover (dots 0-2 = N−1) → −1 (dot 3) → N (dots 4+); vblank −1 (dots 0-3) → N; line 153 the model-specific micro-sequence (DMG/CGB-C SS: −1→153→−1→0 at dots 6/8/12; AGB `model>CGB_C`: −1→153→0 at dots 4/12). Single speed exact + tested; DS line-153 dot-doubling a documented inert placeholder (S7). (A4, `9a0b285`) wired the `StatUpdate` (`GB_STAT_update`) rising-edge engine into the live PPU on the flag-ON path (`stat_update_tick`), replacing `stat_events_tick`: drives the line off `mode_for_interrupt` | the `lyc_interrupt_line` latch (re-eval only when `ly_for_comparison != -1`, held across the −1 gaps), IF on the 0→1 rise; LCD-off = the tick's `!enabled` early-return (engine held low); the OAM-DMA guard clears only visible mode bits (not the IRQ line) so it is irrelevant to the edge; `refresh_cmp` still runs for the readable FF41 bits so reads stay identical. **The halt-late commit masks (`stat_late`/`stat_halt_late`/`m0_rise`) have NO `GB_STAT_update` equivalent and are deferred to the flip.** **FRESH KERNEL MEASUREMENT (current branch, env-temp default-on flip of BOTH `leading_edge_reads` defaults, run_gambatte cgb, reverted): `m2int_m3stat_1` (wants 3) → `3` LIFTED; `m0int_m3stat_2` (wants 0) → `3` REGRESSED — the pair does NOT separate, BOTH read 3.** This is the leading-edge-FF41-read-ALONE behaviour (S2d: "leading-edge flips BOTH 0→3") now confirmed WITH the StatUpdate engine + the refined `mode_for_interrupt`/`ly_for_comparison` inputs in the loop: the engine's anchor-swing moves the IRQ *dispatch* dot but does not by itself separate the reads. **A5 +7-reclock MEASURED (same env-temp flip, `lead += 7` in `m0_flip_events` on the flag-on path — a back-date of the mode-0 flip/IRQ/accessibility events 7 dots, photo-safe by construction since `advance_lx`/the FIFO are `lx`-based; reverted): `m2int_m3stat_1` (wants 3) → `0` (REVERSED from the no-reclock `3`); `m0int_m3stat_2` (wants 0) → `3` — the pair STILL does not separate, now both wrong in OPPOSITE directions.** So neither config separates: no-reclock = both read 3 (boundary +7 late, both reads land before it); +7-reclock = m2int reads 0 / m0int reads 3 (boundary now BETWEEN the reads but in the wrong order — m0int's read lands EARLIER than m2int's, the opposite of the anchor-swing's intent). **CONCLUSION: the kernel separation is NOT a blanket boundary shift — it needs the IRQ-dispatch dot and the leading-edge read phase coordinated so m2int's read lands one M-cycle BEFORE the back-dated boundary and m0int's one AFTER. The blanket +7 moves the visible boundary but leaves the two ISRs' read dots in the wrong relative order; the StatUpdate engine's rising-edge does not place them as the `mode_for_interrupt` anchor model predicts. This is the multi-session coordination work (every blanket lever is a measured A/B swap, as before).** **Remaining (next session): A5 coordinated — pair the reclock with the leading-edge read phase + the IRQ-dispatch dot (the mode-2-IRQ-before / mode-0-IRQ-after swing must actually move the dispatch dots, verify against the engine), + leading-edge OAM/VRAM/palette accessibility, + the halt-late masks; A3 (write-conflict table, lands with the flip); then Phase B (flag default-on, rebaseline ~7000 gambatte, recapture 146 golden, hold every SameBoy-passing oracle).** 642 lib tests green. The branch is the clean flag-gated foundation: A1/A2/A4 inert in production, validated flag-on; the kernel does NOT yet separate under either the no-reclock or the blanket-+7 config — the gap is the dispatch/read-phase coordination, not the boundary magnitude.
- **A5 INSTRUMENTED + KERNEL SEPARATED 2026-06-21 (the prior "does NOT separate" verdict is REVERSED; lever measured, not bankable standalone, all probes reverted — branch clean/net-zero).** Per the goal's "INSTRUMENT FIRST" mandate, added env-gated `SLOPGB_KPROBE` prints (FF41 leading-edge read dot/value, `StatUpdate` IF-rise dot, the visible mode→0 flip dot, `mode_for_interrupt` transitions) and ran flag-on (`leading_edge_reads` default temp-flipped true), CGB, both kernel ROMs. **Pinned the exact dots** (line 1 of each frame, SCX 0): m2int reads FF41 at **dot 248**, m0int at **dot 252** (a 4-dot gap — the mode-2-source vs mode-0-source dispatch difference + the `_1`/`_2` NOP pad), the **visible mode→0 flip at dot 254**, `mode_for_interrupt` 3→0 at **dot 255**. With leading-edge ON and no reclock both reads are < 254 → both mode 3 (m2int ✓ wants 3, m0int ✗ wants 0). **CRUCIAL: the two reads are already in the CORRECT order** (m2int 248 < m0int 252) — separation only needs the *visible* boundary slotted into the open interval **(248, 252]**. The prior "+7 reverses the order" finding is explained: the blanket `lead += 7` moves m0int's *dispatch* (its mode-0 IRQ fires off the very flip being moved), so m0int's read tracks the boundary and stays mode-3 while m2int (mode-2-dispatched, independent) gets caught — self-defeating coupling, NOT a true order reversal. **THE LEVER THAT SEPARATES (measured): decouple the visible flip from the dispatch.** Probe = (1) in `m0_flip_events`, fire `line_render_done` (the visible/`vis_mode` flip + `m0_access_flip`) at `proj <= lead + VISLEAD` while `m0_src`/`m0_rise_dot` (the dispatch) stay at `proj <= lead`; (2) retie `mfi_m0_prev` to `m0_src` (not `line_render_done`) so `mode_for_interrupt`'s mode-0 transition tracks the dispatch, not the moved visible flip. **`SLOPGB_VISLEAD` sweep (CGB, both ROMs): VISLEAD 0 → m2int 3 / m0int 3 (no sep); VISLEAD 2/3/4/5 → m2int 3 ∧ m0int 0 (BOTH CORRECT — kernel SEPARATES); VISLEAD 6 → m2int 0 / m0int 0 (visible boundary passed m2int's read).** At VISLEAD=3 the visible flip lands at dot 251 (= 254−3, strictly between 248 and 252); dispatch held at 254 so neither read dot moved. This is the first config in the whole port that lands m2int=3 ∧ m0int=0 simultaneously. **WHY IT IS NOT A STANDALONE GREEN FLIP (the blocker, also measured): the dispatch is mis-framed for leading-edge reads.** Leading-edge sampling is cc+0; our `m0_src` dispatch dot (254) is calibrated for cc+4 trailing reads and for SameBoy that dispatch sits ~6 dots earlier (visible 247 / IRQ 248, `ModeTimeline`). With leading-edge reads ON but the dispatch left at 254, the canonical mooneye `intr_2_mode0_timing`(+`_sprites`) breaks at **VISLEAD=0 already** (DMG+CGB, measured with `run_mooneye`) — i.e. the canonical regression comes from the dispatch mis-framing, **not** the VISLEAD boundary move. So the VISLEAD lever is frame-inconsistent (visible 3 dots *before* our dispatch, vs SameBoy's visible 1 dot *before* its IRQ): it patches the visible read to separate the kernel but leaves the dispatch in the wrong frame, so it is a partial hack, **NOT bankable as a foundation lever** (would violate "never bank a miscalibrated lever"). **THE NEXT COORDINATED LEVER (sharper than the prior handoff): move the DISPATCH (`m0_src`/the mode-0 IRQ) to SameBoy's frame (~248, `ModeTimeline::mode0_irq_dot`) together with the leading-edge read, so the canonical's dispatch-pinned grids pass at cc+0; THEN the visible/dispatch decouple (VISLEAD≈3) separates the kernel on top.** That dispatch move is the global reclock that needs the ~7000 gambatte rebaseline + 146 golden recapture (it shifts every mode-0 IRQ dot) — the documented atomic S2+S3 boundary, still multi-session. **Net-zero this session:** all probe code reverted, branch byte-identical to `275c07e`; the deliverable is the pinned mechanism + dot table above (separation is real and reproducible; the residual is the dispatch reclock, not a missing coordination). Tooling to re-derive: temp-flip both `leading_edge_reads` defaults true, add the `SLOPGB_KPROBE`/`SLOPGB_VISLEAD` probes (FF41 read in `interconnect/cycle.rs`, RISE in `stat_update_tick`, VISFLIP/M0FLIP in `m0_flip_events`, the `mfi_m0_prev` retie + the split flip), `cargo run --example run_gambatte -- <rom> cgb`, `git checkout` to revert.
- **S4 MEASURED-NET-NEGATIVE 2026-06-22 (the OAM/VRAM accessibility back-date — the goal's "route OAM/VRAM/palette leading-edge not just FF41" task; +8 / −14 bracket A/B swap; IMPLEMENTED + measured + REVERTED). The S4 access-read back-date is dispatch-frame-entangled, NOT a contained lever — do NOT re-chase without the reclock.** The flag-on `oam_access/vram_m3 postread_2` rows read BLOCKED (out3) where SameBoy renders UNBLOCKED (out0): on the leading-edge path the FF41 visible mode→0 boundary back-dates (`vis_early`, 3-4 dots ahead of the dispatch `line_render_done`) but the base OAM/VRAM accessibility (`oam_read_blocked`/`vram_read_blocked`, `blocking.rs`) still unblocks at `line_render_done`. **Implemented S4: `oam_read_blocked`/`oam_write_blocked`/`vram_read_blocked`/`vram_write_blocked` unblock at `(line_render_done || vis_early)`, mirroring `vis_mode` (byte-identical OFF — `vis_early` always false flag-off).** Spot-checked: the `_2`/post reads flip to the SameBoy-passing unblocked value. **MEASURED full gambatte_matrix (both flags flipped, reverted): 4105→4100 = NET −5** (+1 stale): RECOVERS the `_2`/post reads (`oam_access/postread_scx3_2`, `postwrite_2_scx3`, `vram_m3/postread_scx3_2`, `window/m2int_wxA6_*busyread_2/_3` — 8) but REGRESSES the `_1`/PRE reads (`oam_access/postread_scx5_1`+`10spritesprline_postread_1`, `vram_m3/postread_scx5_1`+`10spritesprline_postread_1`, `vramw_m3end_2/_4`, `window/m2int_wxA6_*busyread_1` — 14, all out3 BLOCKED, SameBoy-PASSING). **Root cause: the `_1`/`_2` reads BRACKET the accessibility unblock dot (SameBoy renders `_1`=blocked ∧ `_2`=unblocked — its unblock is BETWEEN them), but the OAM read is NOT leading-edge-routed (still cc+4) while the boundary moved cc+0 — so the cc+4 read sees the back-dated boundary 3-4 dots too early and unblocks the PRE read too. The postread reads are timed against the mode-0 IRQ DISPATCH (A8's 254 frame), so aligning them needs the cc+0 read + the dispatch reclock TOGETHER, not a boundary back-date alone — a smaller separate accessibility offset (A5a's "visible-vs-accessibility 3-dot window") is itself the bracket-calibration the global reclock resolves.** The OAM/VRAM access-read analogue of the A16/dot-4/dispatch forbidden levers. HARD-RULE compliant: measured, reverted, not banked. The cgbpal palette leg (a DIFFERENT `render_finished` anchor) was not attempted — same bracket class expected. **This closes the goal's listed pre-Phase-B levers: engine completion (A15 banked, A16 forbidden), S4 (net-negative). Every contained lever is exhausted by measurement; the residual −336 is the atomic Phase-B reclock.**
- **A16 MEASURED-FORBIDDEN 2026-06-22 (the line-153 LYC cc+4 alignment — the "tempting but forbidden" A/B swap; +42 gambatte but drops ≥4 SameBoy-passing rows; IMPLEMENTED + measured both symmetric & asymmetric, REVERTED, nothing shipped). The LYC under-fire group the A15 handoff named as the next lever is read-frame-entangled — do NOT re-chase without the reclock.** The flag-on `stat_update_tick` LYC latch fires `lyc_interrupt_line` off `ly_for_comparison`, which sets the line-153 match at the SameBoy GB_SLEEP **dot 6** (`display.c:2227`, after GB_SLEEP(19,2)+(14,4) — VERIFIED from source), while every OTHER line latches LYC at dot 4 and `stat_events_tick` fires line-153 LYC at dot 4 (our cc+4 frame). FF0F is NOT leading-edge-routed (only FF41 is, `interconnect/cycle.rs`), so the cc+4 FF0F read straddles the dot-6 fire's M-cycle → the bare engine MISSED `lycint152_lyc153irq_2` (out E2, gave E0) AND over-caught `_ifw_2` (out E0, gave E2). **Hypothesis (A8 precedent — align the update-engine IRQ to OUR cc+4 frame, defer the SameBoy move to Phase B): back-date the line-153 LYC fire to dot 4.** Two implementations measured (flag-on, both default-flipped, reverted): **(a) SYMMETRIC** (fill line-153 dots 4-5 with 153 → latch re-evaluates) = **+41 gambatte / 1 NEW regression (`lyc153_late_ff45_enable_4` [Cgb], SB=E0) / −6 stale**; **(b) ASYMMETRIC** (only RAISE `lyc_interrupt_line=true` when LYC==153, preserve the `-1`-gap hold) = **+42 gambatte / 0 new regression / −6 stale (8 lost − 2 gained)**. The asymmetric version FIXED the new regression but STILL drops the SameBoy-PASSING `dma/gdma_cycles_short_2`+`_scx5_2`+`gdma_weird_2` (SB=0=want) and `window/arg/late_wy_FFto2_ly2_wx00_2/_3` (SB=0=want) — verified via the SameBoy 1.0.2 tester. (The other 3 lost — `lcd_offset/offset3_lyc99int_m0stat/m3stat_count`, `miscmstatirq/lycstatwirq_trigger_ly00_10_50_lcdoffset3_2` — are SameBoy-FAILING gambatte-only, fine to lose.) **Root cause: the line-153 LYC=153 source rise is read by ANY LYC=153 ROM at the line-0 frame boundary, not just the `lycint152_lyc153irq` tests — moving its rising edge 2 dots earlier shifts the IF those dma/window rows observe at their cc+0-framed line-0 reads (an A/B swap). So the line-153 LYC fire dot can only move with the global reclock that re-frames every LYC=153 line-0 read together = atomic Phase B.** This is the LYC-source analogue of the dot-4 mode-0 / dispatch-254→252 forbidden levers: a per-event cc+4-vs-SameBoy back-date that gains gambatte but forfeits SameBoy-passing rows. CONFIRMS the engine-vein-exhausted verdict by direct measurement (the most-principled-looking remaining lever is still an A/B swap). HARD-RULE compliant: measured, not banked.
- **A15 SHIPPED 2026-06-22 (the glitch-line PREFIX m0-IRQ suppression — flag-on +2 gambatte [4103→4105] / mooneye-neutral / byte-identical flag-OFF; the FIRST engine-event-chain piece beyond the A1-A14 boundary back-dates — a STAT rising-edge OVER-FIRE fix, not a boundary lever). The "contained boundary levers EXHAUSTED" verdict held; this is a DIFFERENT category (engine over/under-fire vs read-frame boundary).** Diagnosed by categorizing the full flag-on regression set (561 rows) + isolating the read-frame-INDEPENDENT engine fires (FF0F/IF reads, want E0/E2) from the read-frame-entangled mode reads, then SameBoy-1.0.2-tester ground-truthing each. **The bug:** on the LCD-enable glitch line the rising-edge `stat_update_tick` engine drove `mode_for_interrupt` off `vis_mode`, which shows mode 0 in TWO regions — the line-start PREFIX (`dot < GLITCH_MODE3_START`, the LCD-enable glitch) AND the post-render tail (`line_render_done`/`vis_early`, the real m0). With HBlank enabled the engine fired a SPURIOUS m0 STAT IRQ in the prefix (gambatte+SameBoy render outE0; the bare engine gave E2). `stat_line_level`/`stat_write_trigger_dmg` both already suppress the HBlank source in the prefix with `!(glitch_line && dot < GLITCH_MODE3_START)`; the rising-edge engine lacked that guard. **Fix (`update_mode_for_interrupt`, glitch branch split out of the shared `line>=144||glitch_line` vblank arm):** `vm==0 && !ds && !(line_render_done||vis_early)` → `MODE_FOR_INTERRUPT_NONE` (no mode source; LYC still ORs in), else `vis_mode`. `mode_for_interrupt` is unread flag-OFF → byte-identical. **SINGLE-SPEED gate (`!ds`) is LOAD-BEARING (measured):** SS-gated = **+2 / 0 regress / 0 lift-lost** (recovers `enable_display/ly0_m0irq_trigger` Dmg+Cgb, SameBoy-confirmed out0). The UNIVERSAL (non-`!ds`) variant = **+6 / 0 regress / −1 SameBoy-passing DROP** — the DS `ly0_m0irq_scxN_ds_{1,2}` reads BRACKET the glitch m0 IRQ dot (`_1` outE0 read-before, `_2` outE2 read-after), which the whole-dot model misframes (it fires at the prefix AND the post-render dot, never the DS mid-line dot SameBoy hits), so suppressing the DS prefix is a read-frame A/B swap that drops the SameBoy-passing `ly0_m0irq_scx0_ds_2` (outE2, SB=E2 verified). The DS slice is read-frame-entangled = atomic Phase-B. **MEASURED (isolated before-worktree at A14 [4103] vs main+A15, both default-flipped, reverted): gambatte 4103→4105 (+2); after_regr STRICT-SUBSET before_regr (559 ⊂ 561, ZERO new regression), stale 223→223 UNCHANGED (zero SameBoy-passing lift lost), mooneye flag-on 90/91 unchanged.** Test `glitch_line_prefix_suppresses_m0_irq_flag_on` (SS: prefix selects NONE not 0, exactly one glitch m0 fires). byte-identical flag-OFF (full gbtr 185/185 + mooneye 91/91 gate exit 0, golden byte-identical); 657 lib green; clippy `-D` clean; cavecrew-reviewer no issues. **VEIN NOTE for next session:** the FF0F-read engine over/under-fire set (46 rows) splits SameBoy-ground-truthed into ~32 recoverable-toward-SameBoy + ~8 SameBoy≠gambatte (would-rebaseline). Most glitch-m0-IF rows (`late_m0irq_retrigger`, SS `ly0_m0irq_scxN`) are read-frame-entangled (the post-render dot-248 `vis_early` fire ALSO over-produces E2 for IF reads, not just the prefix) — NOT cleanly bankable. The UNDER-fire group (`ly0/lycint152_lyc153irq` E2, `lycEnable/lyc0_ff41/ff45_disable` E2: engine MISSES a LYC fire SameBoy emits) is the untried distinct mechanism — probe next.**
- **A14 SHIPPED 2026-06-22 (the LCD-enable glitch-line LYC readable-compare back-date — flag-on +1 gambatte [4102→4103] / mooneye-neutral / byte-identical flag-OFF; the SECOND net-positive piece this session, the LAST contained glitch-line lever). lcdon STILL not contained (A14 banks only the readable-flag slice, not the forbidden dot-4 general boundary).** A13's sibling for the LYC leg of the lcdon residual. On the leading-edge (cc+0) path the glitch first line's *readable* LYC coincidence flag was un-back-dated for the −4 read offset: a glitch-line FF41 read in the last 4 dots (`dot >= GLITCH_LINE_DOTS−4` = 448..=451) is the cc+0 view of what its cc+4 trailing edge sees 4 dots later — the *next* line (line 1, since the glitch line is `line==0` and runs `GLITCH_LINE_DOTS`=452 dots) at dots 0-3. **Fix (`compare_ly`'s glitch branch, gated `leading_edge_reads && !ds && dot>=448`):** return that line-1 dots-0-3 value — DMG forces the readable flag invalid there (None/no-match, `compare_ly_irq` line-1 dot<4), CGB holds 0 (its readable flag `(line,0..=3)=>line−1` = 0, so a no-op vs the old `Some(0)`). So effectively only DMG's readable flag changes (Some(0)→None on the last 4 glitch dots). **IRQ side untouched on the glitch line:** `compare_ly` feeds only `self.cmp` (the readable FF41 bit; on DMG `refresh_cmp` mirrors `cmp_irq=cmp`), but the flag-on IRQ path is `stat_update_tick` off `ly_for_comparison`, which is −1 on the glitch line → the LYC latch never re-evaluates, so the readable-flag back-date reaches no IRQ/`stat_line` (and `compare_ly_irq`'s own glitch branch is unchanged, still `Some(0)`). **MEASURED (isolated before-worktree at A13 [4102] vs main+A14, both default-flipped, reverted): gambatte 4102→4103 (+1); the +1 is the RECOVERED DMG row `gambatte/enable_display/frame0_m2stat_count_1_dmg08_cgb04c_out91.gbc` the leading-edge path was dropping. after_regr STRICT-SUBSET before_regr (561 ⊂ 562: A14 removes exactly that one regression, adds ZERO new), 223 lift set UNCHANGED, mooneye flag-on 90/91 unchanged (IDENTICAL `lcdon_timing-GS`-only failure — lcdon still red).** Test `glitch_line_lyc_compare_backdates_four_dots_flag_on` (pins `Some(0)` flag-off + flag-on dot 447 before the window, `None` flag-on DMG dots 448-451). byte-identical flag-OFF (full gbtr 185/185 + mooneye 91/91 gate exit 0, golden byte-identical); 656 lib green; clippy `-D` clean; cavecrew-reviewer no issues. **This banks the glitch-end LYC readable slice the A13 handoff deferred — the remaining lcdon residual is now ONLY the dot-4 line-start mode-0 window (MEASURED-ATOMIC/FORBIDDEN: +63 gambatte but drops `intr_1_2_timing-GS` ×4, see the A13 bullet), so lcdon is fully characterized — both residual legs known, the readable one banked (A14), the IRQ-frame one (dot-4) atomic-only. Contained glitch-line levers are now EXHAUSTED.**
- **A13 SHIPPED 2026-06-22 (the LCD-enable glitch-line mode-3 boundary back-date — flag-on +10 gambatte / mooneye-neutral / byte-identical flag-OFF; the FIRST net-POSITIVE piece since A10). lcdon PROVEN not a contained win via the valid full-interconnect diag.** Re-instrumented lcdon through the FULL-GameBoy interconnect read path (the diag the handoff mandated: a scratch example `harness::boot` + `set_leading_edge_reads(true)`, FF41-read trace with the cc+0 leading-edge −4 offset applied; the prior session's bare-`Ppu::read` diag was INVALID — it bypassed the offset). **Finding supersedes the handoff's "6 divergences at line-1 dot80/dot252":** through the interconnect A7/A5a already make the post-glitch NORMAL lines (1+) neutral — every line-1/2 FF41 read matches flag-off. The residual divergences are on the GLITCH line itself, whose mode-3 window was un-back-dated for the cc+0 frame. **Fix (`vis_mode`'s glitch branch + `m0_flip_events`, both gated `leading_edge_reads`):** back-date the glitch mode-3 ENTRY `GLITCH_MODE3_START` 78→74 (the −4 read offset, exactly A7) and the EXIT by extending `vis_early` to glitch lines (removed the `!self.glitch_line` gate; `bare_flip` is false on glitch so it takes the `+4` `early_lead` = the FULL read offset → `vis_early` rises at dot 248, 4 before the `line_render_done` flip at 252). Confined to `vis_mode` — the OAM/VRAM/palette accessibility reads keep the raw `GLITCH_MODE3_START` (they were already byte-identical flag-on; the lcdon OAM/VRAM legs pass). **MEASURED (temp default-flip, full matrices, reverted): gambatte 4092→4102 (+10); the +10 are all RECOVERED regressions (rows the leading-edge path was DROPPING), every one in `gambatte/enable_display/` — `ly0_sprites_m0stat_2`, `frame0_m3stat_count_2`, `ly0_late_scx7_m3stat_scx0_3`, `ly0_late_scx7_m3stat_scx3_2`, `nextstat_2` (×2 models each). ZERO new regression (after_regr ⊆ before_regr proven by isolated A12-worktree diff), 223 lift set UNCHANGED, mooneye 90/91 unchanged.** Test `glitch_line_mode3_backdates_four_dots_flag_on` (pins (78,252) flag-off, (74,248) flag-on); 654 lib green, clippy clean, byte-identical flag-OFF (both changes gated → flag-off `start=78` ∧ `vis_early` never set). **lcdon_timing-GS STILL fails flag-on — PROVEN not contained:** after the mode fix the residual lcdon STAT divergences (valid diag) are (a) the line-START mode-0 window at dot 4 (cc0 reads mode 0 where cc4 reads mode 2 — a GENERAL every-line −4 boundary) and (b) the glitch-line-END LYC-coincidence flag (`cmp` at the line boundary — cc0 sees the glitch `compare_ly`=Some(0)=match while cc4 sees line-1 dot0 = no-match). BOTH are general −4 read-offset shifts that ripple the whole flag-on matrix (the dot-4 line-start boundary is read by the baselined m2int/ly0 floor rows) = the atomic Phase-B rebaseline, NOT a glitch-confined fix. So lcdon needs the dispatch/read-frame reclock; A13 banks only the contained glitch-MODE slice. **MEASURED-ATOMIC (do NOT re-chase): the dot-4 lever was implemented + measured (vis_mode `dot < 4` → `< 0` on leading-edge single-speed = vanish the line-start mode-0 window, temp default-flip, reverted) — it REGRESSES `intr_1_2_timing-GS` ×4 (Dmg/Mgb/Sgb/Sgb2), a SameBoy-PASSING must-hold mooneye row (the vblank→line-0 mode-1→2 transition), so it is FORBIDDEN as a contained piece — DESPITE a very tempting gambatte flag-on +63 (4102→4165, 502 reg / 226 lift). The +63 is exactly the read-frame-entangled cluster shifting under the rebaseline; banking it would drop a SameBoy-passing oracle (HARD RULE violation), and it lands properly only with the atomic global reclock that also re-frames `intr_1_2_timing`.** The dot-4 line-start mode boundary is read-frame-entangled exactly as the handoff's "−345 PROVEN" note says (the _outX FF41-mode-read cluster); it can only move with the global cc-exact reclock that re-frames every line-start read together. So the lcdon-not-contained verdict is MEASURED, not just reasoned. The glitch-end LYC fix (a `compare_ly` leading-edge back-date) is deferred to Phase B with the dot-4 general boundary (no standalone mooneye win without it). **Tooling to re-derive:** the scratch example above (boot lcdon_timing-GS.gb, set_leading_edge_reads on/off, run to LD B,B, peek HRAM FF98/99/9A fail-state) + a temp FF41-read eprintln in `Interconnect::read` logging cc0/cc4 line/dot/leading/trailing. **Also closed the A11/A12 systematic write-coherence sweep — the FF40 (LCDC) leg is CLEAN (no fix needed):** an LCD-enable that raises STAT (`legacy_level_edge`) does NOT double-fire on the flag-on dot engine, because the rise lands on the glitch line where the engine's LYC input is inert (`ly_for_comparison` = −1, so the LYC latch never re-sees the match), and the disable path early-returns (`!enabled`). Guard test `ff40_enable_lyc_match_fires_once_flag_on` pins it (1 fire both paths). So the STAT-affecting register writes are now all accounted for: FF41 (A11) + FF45 (A12) needed the gated re-sync; FF40 does not.
- **A12 SHIPPED 2026-06-22 (the FF45-write double-fire fix — the FF45 analogue of A11; net-ZERO, byte-identical flag-OFF). A "bank net-zero" correctness piece for Phase B.** Same systematic dual-engine issue as A11 but on the FF45 (LYC) write: `write_lyc_dmg`/`_cgb` fire the gambatte LYC-write trigger, and on flag-on the dot-clocked `stat_update_tick` then re-sees `lyc_interrupt_line` rise next tick → double-fire (confirmed flag-off=1, flag-on=2). Fix (`regs.rs` 0xFF45 arm, flag-on only): capture `before = pending_if`, run `write_lyc`, and when it set a NEW STAT bit (`(pending_if & !before) & IF_STAT`), re-derive `lyc_interrupt_line` for the new LYC + re-sync the `StatUpdate` line (edge discarded) so the next tick sees no fresh rise. The `& !before` keys on a newly-set bit so it never over-suppresses a legitimate dot-engine rise (the A11 lesson). **MEASURED net-ZERO (temp default-flip, reverted): gambatte 4092/5272 — identical 572-reg/223-lift set to A10/A11, zero swaps; mooneye 90/91 unchanged.** Read-frame-independent; matters at Phase B (the double-fire would bias the rebaseline). Test `ff45_match_fires_once_flag_on`; reviewer found no bugs (2 conservative under-application edge cases noted — safe, never over-suppresses); 653 lib green, clippy clean. **Note: this dual-engine write-coherence issue is SYSTEMATIC across register writes that touch the STAT line — A11 (FF41) + A12 (FF45) are fixed; a similar latent double-fire likely exists on other STAT-affecting writes (revisit at S5/Phase B when the gambatte write-trigger is retired for the pure rising-edge engine).**
- **A11 SHIPPED 2026-06-22 (the FF41-write double-fire fix — net-ZERO gambatte/mooneye, fixes a real flag-on coherence bug, byte-identical flag-OFF). A "bank net-zero" correctness piece for Phase B.** The FF41 write path (`regs.rs`) runs the gambatte `stat_write_trigger_dmg/cgb` on BOTH paths (it is the write path, not part of `stat_events_tick`). On the flag-on path the dot-clocked `stat_update_tick` rising-edge engine ALSO runs every dot — so an FF41 write that enables a source whose condition is already met fires IF **twice**: once from the write-trigger, again on the next tick when the `StatUpdate` engine re-sees the new enable as a 0→1 rise (confirmed: flag-off fires 1, flag-on fired 2 — `ff41_enable_lyc_fires_once_flag_on`). **Fix: when the write-trigger fired (`fire`), re-sync the `StatUpdate` line to the post-write level (`let _ = stat_update.update(...)`, edge discarded) so the next tick sees no fresh rise.** **MEASURED the two variants (temp default-flip, reverted): (a) replacing the write-trigger wholesale with the rising edge = net −31 gambatte (the simple level-OR does not reproduce the gambatte write-trigger's position-exact fires in our cc+4 frame); (b) UN-gated sync (every flag-on write) = +24 regressions recovered but −15 lifts — and ALL 15 lost lifts are SameBoy-PASSING (CGB ds/lcdoffset class-A gap-list rows: lycEnable/late_ff41_enable, m1/late_m0enable, etc.), because the un-gated sync also suppresses LEGITIMATE dot-engine post-write rises → FORBIDDEN. (c) `fire`-GATED sync = EXACTLY net-zero vs A10 (identical 572-reg/223-lift sets, zero swaps), mooneye unchanged, fixes the double-fire unit test.** So the gated form is the only safe one: it kills the double-count without touching the fire=false writes the dot engine legitimately handles. Read-frame-INDEPENDENT (it is about write-event coherence, not the read frame), so it banks standalone; it matters at Phase B (the double-fire would bias the rebaseline + could break SameBoy-passing rows in the rebaselined frame). Test `ff41_enable_lyc_fires_once_flag_on`; 652 lib green, clippy clean, byte-identical flag-OFF.
- **A10 SHIPPED 2026-06-22 (the vblank-ENTRY OAM pulse — recovers `vblank_stat_intr-GS`/`-C` flag-on; +8 gambatte / +5 mooneye / ZERO SameBoy-passing rows lost; byte-identical flag-OFF). The dot-12 half of the vblank port is MEASURED net-negative and DEFERRED.** The flag-on `StatUpdate` engine mirrors `vis_mode` in vblank (`update_mode_for_interrupt` line≥144 = `vis_mode()` → mode 0 at 144:0-3, mode 1 from 144:4), so `mode_for_interrupt` never selects the OAM (mode-2) source there and the rising-edge line emits none of the vblank OAM pulses — the `vblank_stat_intr` regression. SameBoy raises them as **direct `IF |= 2` pokes** (`display.c:2160` the 144-entry int, `:2185` the per-line DMG vblank ints), NOT `stat_interrupt_line` rises. New `Ppu::stat_update_vblank_oam_pulses` (`stat_irq.rs`, flag-on only, called at the end of `stat_update_tick`) reproduces the **144:0 entry pulse** with the *same* `!glitch_line` + `m2_pulse_fires` guard and DMG halt+dispatch-late masks (CGB 144 exempt) the proven flag-off `stat_events_tick` uses. **MEASURED (temp default-flip, full matrices, reverted): gambatte 4084→4092 (+8, 572 reg / 223 lift — recovered 8 regressions: `enable_display/frame{0,1}_m2irq_count_2` ×5, `lyc153int_m2irq_ifw_1`, `m1/{ly143,lyc143}_late_m2enable*` ×3); mooneye 89→90 groups (recovers `vblank_stat_intr-GS` ×4 + `vblank_stat_intr-C` ×1 = the misc group; only `lcdon_timing-GS` ×4 remains). ZERO lift lost (223→223 identical set — no SameBoy-passing row regressed).** **The visible-line m2 pulses (1-143 dot 0) needed NO new code** — the rising-edge level-OR engine NATURALLY reproduces `m2_pulse_fires`' `¬HBlank` / `¬held-LYC` blocking (a held source keeps the line high → no fresh edge), and the 144:0 poke cannot double-fire with it (mfi=0 at 144:0, and `m2_pulse_fires` requires HBlank disabled, exactly the case a held-HBlank engine rise is suppressed). **The DMG 145-153 dot-12 pulses (`display.c:2185`) are DEFERRED — split-measured net-NEGATIVE flag-on: adding them regresses 6 SameBoy-PASSING rows (`ly0/lycint152_m2irq_1` [Dmg], `lycm2int/lyc0m2int_m2irq_1` [Dmg], `window/late_enable_afterVblank_{1,2,3,4}` [Dmg] — all confirmed in `/tmp/sb_gap_list.txt`, SameBoy renders the expected value). SameBoy fires these pulses too (so they are faithful), but flag-on's cc+4 read/halt frame mis-places the resulting read until the global reclock — the atomic-convergence trap. The 144:0 pulse does NOT have this (independent of those rows' reads), so it banks standalone; the dot-12 pulses land with Phase B.** Tests `vblank_oam_pulse_144_entry_dmg_flag_on` / `_cgb_flag_on_not_halt_late` + the `vblank_line_oam_pulses_dot12_deferred_flag_on` guard; 651 lib green, clippy clean, byte-identical flag-OFF (`stat_update_tick` only runs flag-on; full gbtr+mooneye gate exit 0). **CORRECTION to the −357 driver hypothesis:** the flag-on per-cluster diff shows the −357 is DOMINATED by atomic render/read-frame shift (window/scx_during_m3/scy/bgtilemap/bgtiledata/enable_display ≈ −341 net, all needing the rebaseline), NOT StatUpdate-engine incompleteness (the STAT clusters — halt/m1/lycEnable/m0enable/miscmstatirq — net only ~−100, and most are read-frame-entangled too). So "completing the engine" recovers a bounded slice; the bulk is the atomic Phase-B reclock.
- **PHASE-B GAP MEASURED 2026-06-21 (the definitive distance to the default flip with A1-A9; temp default-flip, measured, reverted — branch byte-identical to `eeca7e8`).** Flipped both `leading_edge_reads` defaults true and ran the full matrices: **gambatte = 4084/5272 flag-on vs ~4441 flag-OFF = net −357** (WORSE than the pre-A6 leading-edge-read-ALONE spike's 4228 — because the A4 `StatUpdate` engine swap, incomplete relative to `stat_events_tick`, regresses the gambatte STAT event chains it doesn't yet reproduce: m0irq / lycm2int / m2int / the FF41-write trigger tables / the per-event halt masks; PLUS the leading-edge read bypasses the flag-off `stat_mode_edge` so the DS m3stat task6 +84 lift regresses, PLUS the glitch line + scx_during_m3 reads shift un-back-dated). **mooneye = 89/91 groups (DROPS `lcdon_timing-GS` ×4 [the LCD-on glitch line, A1-A9 exclude `glitch_line`] + `vblank_stat_intr-GS` ×4 + the misc group).** So Phase B is FAR from green — the A6-A9 localized lifts (kernel +1, sprite m3stat +40, canonical/mode3/hblank/sprites) are dwarfed by the incomplete-engine regressions. **The flip needs, all together: the COMPLETE StatUpdate engine (port the gambatte m0irq/lycm2int/m2int event chains + write-trigger tables + every event's halt mask, not just the mode-2/mode-0 rises A6 did), DS back-dating (vis_early/mode3_entry are single-speed only), the glitch-line back-date (lcdon), the vblank-region pulse port (vblank_stat_intr, probed), S4 OAM/VRAM/palette accessibility back-dating, A3 write-conflict — then rebaseline ~7000 + recapture 146 golden.** This is the documented all-or-nothing multi-session Phase B; the A1-A9 flag-gated pieces are the validated, byte-identical-OFF foundation that shrinks it. Net −357 is the current gap number to beat.
- **A9 SHIPPED 2026-06-21 (the sprite/window `vis_early` extension — lifts the sprite kernel pairs + `intr_2_mode0_timing_sprites`; +40 flag-on / 0 regress; byte-identical flag-OFF).** A5a's `vis_early` mode→0 back-date was gated to BARE lines (`bare_flip`), leaving sprite/window lines on the un-back-dated dispatch flip → `intr_2_mode0_timing_sprites` FAIL flag-on + the gambatte sprite `m3stat_2` reads (the sprite analogs of the kernel `m0int`) reading mode 3 instead of 0. A9 extends `vis_early` to all non-glitch single-speed lines (`m0_flip_events`), with `lead + 4` for sprite/window vs `lead + 3` for bare — the +1 reflects the sprite-extended mode-3 geometry (A5a's "+2-DMG derived-but-unmeasured" guess was wrong; the MEASURED value is +4). **Measured (`SLOPGB_SPRITE`/`SLOPGB_SPVL` sweep then hardcoded): `intr_2_mode0_timing_sprites` FAIL→PASS (DMG+CGB); the gambatte single-speed sprite `m3stat` cluster +40 LIFT / 0 REGRESS (dmg+cgb, `m3stat_2`→0 lifted, `m3stat_1`→3 held); window `m3stat` neutral (0/0); the whole must-hold suite + the bare kernel pair UNCHANGED.** New gbtr acceptance spec `sprite_kernel_pair_matches_sameboy_target` (gambatte.rs, flag-on) pins `10spritesPrLine_m3stat_1`→3 ∧ `_2`→0 on both models. Bare-line behavior byte-identical to A5a (`early_lead == 3` there); glitch + DS excluded (DS offset 2, deferred). 648 lib green, clippy clean, byte-identical flag-OFF (full gbtr+mooneye gate exit 0). **VBLANK PROBED + DEFERRED (the last must-hold mooneye flag-on fail, `SLOPGB_VB`, reverted):** modeling the line-144 OAM pulse as `mfi=2` at 144:0 (the StatUpdate rising edge fires it) + a DMG-only halt-late mask (CGB 144 exempt, per the old engine) fixes BOTH `vblank_stat_intr-GS` (DMG) AND `vblank_stat_intr-C` (CGB) flag-on with the must-hold family held — but the CRUDE unguarded pulse REGRESSES the sprite kernel (`10spritesPrLine_m3stat_2` 0→3): it fires an extra OAM interrupt the sprite m3stat ROMs don't expect. The clean fix needs the full `m2_pulse_fires` guards (OAM-enabled ∧ ¬HBlank ∧ ¬LYC-blocked) replicated for the 144:0 pulse + the 145-153 DMG dot-12 pulses — the meatier vblank-region port, the documented next lever.
- **A8 SHIPPED 2026-06-21 (the mode-0 IRQ-dispatch lag removal — `hblank_ly_scx_timing` passes flag-on; a PARTIAL dispatch reclock that IS clean, refining the "atomic" verdict; byte-identical flag-OFF).** The dispatch reclock is NOT all-or-nothing: the prior refutation moved the mode-0 IRQ all the way to SameBoy's `mode0_irq_dot` (our 252) and broke the kernel — but that was the FULL-SameBoy-frame move. The SMALLER move — fire the mode-0 IRQ at `line_render_done` (our **254** = the gambatte-calibrated `m0_rise_dot` frame), removing only the +1-dot `mfi_m0_prev` lag A6 left (which put the `StatUpdate` mode-0 STAT IF at 255, one dot late vs `stat_events_tick`) — is CLEAN. New flag-on branch in `update_mode_for_interrupt` (`stat_irq.rs`): `mode_for_interrupt→0` reads the CURRENT `line_render_done` (254) not the lagged `mfi_m0_prev` (255). **Measured (`SLOPGB_NOLAG` probe, then hardcoded, full flag-on family): `hblank_ly_scx_timing-GS` FAIL→PASS (DMG+CGB)**, and the ENTIRE must-hold suite + kernel UNCHANGED (canonical, intr_2_0, intr_2_mode3, intr_2_oam_ok, stat_irq_blocking, stat_lyc_onoff, halt_*, intr_*, reti held PASS; m2int=3 ∧ m0int=0 held — the kernel m0int read at 252 and the canonical both tolerate 254, only the 252 full-frame move regresses them). **Why 254 is clean but 252 is atomic:** m0int reads FF41 at our dot 252 = its mode-0-ISR latency past the dispatch; with the dispatch at 254/255 that read lands at the post-vis_early-flip dot (≥251 → mode 0), but moving the dispatch to 252 drags the read below 251 → mode 3. So the dispatch has a 2-dot clean window (254-255) above the kernel floor; the lag (255) was simply 1 dot inside it on the wrong side for the halt grids. Test `mode0_irq_fires_at_render_done_flag_on_not_lagged`; byte-identical flag-OFF (`stat_events_tick` never reads `mode_for_interrupt`); 648 lib green, clippy clean. **The mode-0 IRQ-dispatch is thus PARTIALLY reclocked (the lag removal lands hblank); the residual full-SameBoy-frame move (254→252) stays atomic with the ~7000 rebaseline.** **COORDINATED-GRID PROOF (the definitive answer to "coordinate the dispatch with the visible back-date", `SLOPGB_DISPLEAD`×`SLOPGB_VL` probe, reverted):** swept the mode-0 IRQ dispatch dot (254/252/251, decoupled via a probe `m0_disp_early` field) × the `vis_early` boundary (251/250/249) as an INDEPENDENT 2-D grid. **ONLY disp=254 (A8) holds everything** (m2int=3 ∧ m0int=0 ∧ canonical ∧ intr_2_0 ∧ hblank all PASS). **At disp≤252, EVERY `vis_early` position (249/250/251) gives m0int=3 (broken) + intr_2_0 FAIL + hblank FAIL** — the dispatch move drags m0int's mode-0-ISR `ldh a,(FF41)` read off the boundary REGARDLESS of where the visible flip sits (and intr_2_0 is pinned to the mode-2→0 IRQ *distance*, independent of the visible back-date entirely). So the coordination the goal hypothesised (dispatch + visible back-date together) does NOT exist — 254 is the unique frame our model can hold. Reaching SameBoy's 252 requires the deferred-commit CPU read-timing (the inert `cycle_clock.rs` wired into the read path so the ISR read lands at dispatch−1 like SameBoy, not our dispatch−2) — the port's CPU-side S1/S2, NOT a PPU boundary lever.
- **A7 SHIPPED 2026-06-21 (the mode-2→3 entry back-date — `intr_2_mode3_timing` passes flag-on; a CLEAN, non-dispatch-entangled read-boundary lever; byte-identical flag-OFF).** After the dispatch reclock was refuted as atomic (below), classified the 4 residual flag-on failures: `hblank_ly_scx`/gbmicrotest `int_hblank_halt` ARE dispatch-entangled (the m0 IF dot, atomic), but `intr_2_mode3_timing` is NOT — it counts (from a mode-2 OAM halt, like the canonical) the FF41 reads to mode **3**, i.e. the mode-2→3 *entry* boundary (our dot 84), which is independent of the kernel (reads at 248/252) and the mode-0 boundary. New `Ppu::mode3_entry_dot` (`stat_irq.rs`, flag-on path only): back-dates the entry to dot **80** = 84 − 4, the leading-edge read offset (cc+0 latches the PPU 4 dots before the cc+4 trailing view, so moving the boundary the same 4 dots keeps the read **observationally neutral** for the mode-2→3 detection — the principled value, NOT curve-fit). Contrast with `vis_early` (mode-0 *exit*), back-dated 3 = one LESS than the read offset, the −1 net that lands m0int's read on mode 0 (the kernel separation). **Measured (`SLOPGB_M3` sweep then the hardcoded −4, full flag-on family):** `intr_2_mode3_timing` **FAIL→PASS (DMG+CGB)**; the entire must-hold suite UNCHANGED (intr_2_mode0/canonical, intr_2_0, intr_2_oam_ok, stat_irq_blocking, stat_lyc_onoff, halt_*, intr_1_2-GS held PASS) and the kernel m2int=3 ∧ m0int=0 HELD. Single speed only (`!ds`; DS offset is 2, deferred). Test `mode3_entry_backdates_four_dots_flag_on`; byte-identical flag-OFF (always 84). 647 lib green, clippy clean.
- **A6 SHIPPED 2026-06-21 (the halt-late commit masks for the StatUpdate engine — the canonical-holding lever; for the FIRST TIME flag-on holds BOTH the kernel separation AND the canonical `intr_2_mode0_timing`; byte-identical flag-OFF, gate exit 0).** Instrumented the dispatch first (`SLOPGB_KPROBE`, reverted): flag-on the `StatUpdate` engine raises the mode-2 (OAM) STAT IF at line dot 0 with NO halt-late mask, so the canonical mooneye `intr_2_mode0_timing` (which `halt`s on the mode-2 OAM IRQ then counts NOPs to the FF41-reads-mode-0 dot) wakes one M-cycle (4 dots) EARLY → FAIL flag-on. Root cause: the gambatte `stat_events_tick` engine sets `stat_late`+`stat_halt_late` on that pulse; the `GB_STAT_update` engine sets neither. **The fix (new `Ppu::stat_update_halt_masks`, `stat_irq.rs`, flag-on path only): the mode-2 line-start pulse takes ONLY `stat_halt_late` (the halt-exit mask), NOT `stat_late` (the interrupt-sample mask)** — measured calibration, the crux: in the leading-edge (cc+0) frame the regular interrupt dispatch is already in SameBoy's frame, so the canonical's `halt`-wake needs the +1 (SameBoy `GB_cpu_run` mid-cycle halt sample) while the kernel `m2int_m3stat_1`'s *non-halt* `ldh a,(FF41)` dispatch must NOT be re-delayed (a `stat_late` here moves its read 248→252 and reverts m2int 3→0). The discriminator is `halt` vs `ei`-dispatch, captured by the two masks routing to different samplers (`if_late` vs `if_stat_late`, `interconnect/tick.rs`). The mode-0 rise sets `m0_rise` (faithful, but neutral on the suite until the mode-0 dispatch is reclocked — its rise is still our cc+4 dot 255). **Measured (flag-on, MASK=halt, `run_mooneye`/`run_gambatte`, vs the A5a flag-on baseline):** canonical `intr_2_mode0_timing` **FAIL→PASS (DMG+CGB)**; kernel **m2int=3 ∧ m0int=0 HELD**; PASS-held: intr_2_0_timing, intr_2_oam_ok_timing, stat_irq_blocking, stat_lyc_onoff, intr_1_2_timing-GS, halt_ime1_timing(+2-GS), halt_ime0_nointr_timing, intr_timing, reti_intr_timing. **Zero flag-on regression vs A5a** — the 4 still-FAIL flag-on (intr_2_mode0_timing_sprites, intr_2_mode3_timing, vblank_stat_intr-GS, hblank_ly_scx_timing-GS) were ALREADY FAIL under A5a (measured both configs: identical), each needing OTHER incomplete machinery (sprite/window `vis_early` +2-DMG, the mode-2→3 entry boundary back-date, the line-144/DMG-vblank OAM pulses the mfi-driven engine doesn't emit, and the mode-0 IRQ-dispatch reclock — NOT a miscalibrated mask). So A6 is a pure flag-on advance: it FIXES the canonical, HOLDS the kernel, breaks nothing. Tests `stat_update_mode2_pulse_halt_mask_only_flag_on` (mode-2 pulse: `stat_halt_late` set, `stat_late` NOT) + `stat_update_mode0_rise_takes_m0_rise_flag_on`; 646 lib green, clippy clean, byte-identical flag-OFF (`stat_update_tick` only runs flag-on; full gbtr+mooneye gate exit 0). **The S0 acceptance spec `kernel_pair_matches_sameboy_target` (gambatte.rs) is now UN-IGNORED and GREEN** — it runs the kernel pair on the flag-on path via a new `GameBoy::set_leading_edge_reads` validation hook (the interconnect/ppu setters un-`cfg(test)`'d) and asserts m2int→3 ∧ m0int→0 on BOTH DMG+CGB.
- **DISPATCH-RECLOCK REFUTED 2026-06-21 (the prior "next: move the dispatch to ~252" hypothesis is MEASURED-FALSE; A6's halt-mask is the correct resolution at the UNMOVED frame; probe reverted, branch byte-identical to `a89fdf9`).** Tested the long-assumed next lever — move the mode-0 IRQ DISPATCH (`mode_for_interrupt→0` via `mfi_m0_prev`) from our cc+4 dot 255 to SameBoy's `mode0_irq_dot` (our 252) by re-anchoring `mfi_m0_prev` to `vis_early` (dot 251 → IRQ at 252) on the flag-on path (`SLOPGB_DISP` probe). **Measured (flag-on, `run_mooneye`/`run_gambatte`, all reverted): net-NEGATIVE A/B swap.** Canonical `intr_2_mode0_timing` HELD PASS (it never needed the dispatch move — A6's halt-only mask already fixes it at the 254 frame); but **kernel `m0int_m3stat_2` REGRESSED 0→3** (m0int is itself a *mode-0-dispatched* read — moving its IRQ moves its `ldh a,(FF41)` off the post-flip dot) and **`intr_2_0_timing` REGRESSED PASS→FAIL** (the mode-2→mode-0 IRQ distance shortens by 3 dots), while **`hblank_ly_scx_timing-GS` STAYED FAIL** (so the dispatch dot is NOT its blocker). **CONCLUSION (the proof the goal asked for): the mode-0 dispatch dot is ATOMIC/ENTANGLED — it cannot move on the flag-on path without regressing every mode-0-*dispatched* read (m0int, intr_2_0) that is pinned to our 254 frame; only the global cc-exact reclock (which re-frames ALL those reads together + the ~7000 rebaseline) can move it. The premise "the canonical needs the dispatch at 252" was WRONG — A6's `halt`-only mask resolves the canonical at the UNMOVED 254 frame, and the 254 frame is exactly where m0int reads its post-flip mode 0.** So the residual flag-on failures are NOT a dispatch-dot problem: `hblank_ly_scx`/gbmicrotest `int_hblank_halt` need the **`m0_rise` mask phase** (the new engine's m0 rise lands at dot 255 vs the gambatte engine's `m0_rise_dot` 254 — a sub-dot second-half-determination mismatch, a separate lever); `intr_2_mode3_timing` the mode-2→3 entry-boundary back-date; `intr_2_mode0_timing_sprites` the sprite/window `vis_early`; `vblank_stat_intr-GS` the line-144/DMG-vblank OAM pulses the mfi-engine doesn't emit. Each is its own flag-gated piece on top of A6; the global default flip + rebaseline (Phase B) is the terminal all-or-nothing step.
- **A5a SHIPPED 2026-06-21 (the visible/dispatch decouple banked as flag-gated production machinery; byte-identical flag-OFF, gate exit 0, kernel separates flag-ON).** Acting on the "bank a green foundation piece" mandate, the measured separator from the bullet above is now real (not a probe): a new `Ppu::vis_early` field (`ppu/mod.rs`) back-dates the **CPU-visible** STAT mode→0 boundary (`vis_mode`) to SameBoy's `ModeTimeline::visible_mode0_dot` frame — 3 dots ahead of the IRQ-dispatch flip (`line_render_done`/`m0_src`) — **on the flag-on path only, bare single-speed lines** (`m0_flip_events`: `leading_edge_reads && bare_flip && !ds && proj <= lead + 3`). `vis_mode` reads `line_render_done || vis_early`; since the set is gated on `leading_edge_reads` (off in prod), `vis_early` is **always false flag-OFF**, so `vis_mode == line_render_done` exactly — byte-identical (full gbtr+mooneye gate **exit 0**, 644 lib green, clippy clean). The dispatch (`m0_src`/`mode_for_interrupt`) is unmoved, so this decouples the visible read from the IRQ dot. **Flag-ON verified**: `run_gambatte cgb` on the kernel pair = **m2int_m3stat_1 → 3 ∧ m0int_m3stat_2 → 0** (both correct — the kernel SEPARATES with the real machinery, not just the probe). Tests: `visible_mode0_backdates_three_dots_flag_on_bare_line` (flag-on flip at SameBoy+4 / our 251, flag-off stays at +7 / 254) + the two `leading_edge_*` subdot tests rewritten to pin the separation at the two read dots (leading sample at our dot 248 → mode 3, 252 → mode 0). **Scope/residuals (deliberately excluded, derived-but-unmeasured): the +3 back-date is the bare single-speed value; sprite/window lines want +2 (DMG) and DS +4, left on `line_render_done`. OAM/VRAM accessibility (`blocking.rs`) keeps the dispatch dot — the visible-vs-accessibility 3-dot window is the S4 back-dating. This is the visible-frame HALF of the atomic flip; the canonical mooneye `intr_2_mode0_timing` still breaks flag-on because the DISPATCH is still cc+4-framed (our 254, not SameBoy's ~252) — the dispatch reclock + ~7000 rebaseline is the remaining atomic work. A5a is the A1/A2/A4-pattern foundation piece: individually faithful, byte-identical OFF, validated ON.**
