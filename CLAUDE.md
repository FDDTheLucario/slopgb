# slopgb

Cycle-accurate GB/GBC emulator. Workspace: `crates/slopgb-core` (emulator, zero deps, no unsafe) + `crates/slopgb` (frontend: winit/softbuffer/cpal only).

**Read `docs/ARCHITECTURE.md` before touching core** — timing contract (tick-then-access M-cycles), memory map, module ownership, mooneye + game-boy-test-roms harness protocols.

## Rules

- TDD: failing test first. Every obscure hardware behavior gets a unit test.
- Never special-case test ROMs (mooneye or the game-boy-test-roms battery) — emulate the documented hardware behavior and cite the source in a comment when obscure.
- Before touching any baselined behavior, read the floor-class index header in `crates/slopgb-core/tests/gbtr/baselines/gambatte.txt`: every baselined cluster is an A/B-swept trade — one-sided "fixes" regress the now-green siblings.
- No new deps in core (std only); no unsafe anywhere (`forbid(unsafe_code)`); clippy `-D warnings` clean.
- No god files: keep every `.rs` **under 1000 lines**. Do split a growing file into cohesive submodules (`foo.rs` + `foo/`, each a second `impl` block via `use super::*`; struct/fields/consts stay in the parent) and externalize inline tests to a `#[cfg(test)] #[path = "X_tests.rs"] mod tests;` sibling (split that further into nested `#[path]` category modules if it too passes 1000). Don't let a module accrete unrelated concerns or a 1000-line inline `mod tests`. See `docs/tdd-split-plan.md` for the seam map.
- Commit + push frequently (after each phase/fix round). Repo-local `commit.gpgsign=false` (user's ssh key locked in non-interactive sessions).
- Each iteration: run `/rust-diff-review` on that iteration's diff, fix every finding before the next iteration.
- Keep this file updated (and `/clean-docs`-clean) as the project evolves.

When a hardware question comes up, consult in order:

| Source | For |
|---|---|
| `docs/hardware-state/` | this emulator's per-subsystem implementation state, quirks, and parked/disproven approaches (one file per subsystem; see its README index) |
| gbctr (Gekkio, Complete Technical Reference) | CPU/MBC timing, micro-ops |
| Pan Docs | everything else |
| `test-roms-src/<failing test>.s` asm | what a failing mooneye test actually checks |
| `<suite>/game-boy-test-roms-howto.md` (in the collection) | each gbtr suite's pass protocol + verified devices |
| SameBoy / mooneye-gb / gambatte source | undocumented corners, tie-breaks |

## Commands

```sh
test-roms/download.sh                                  # fetch both pinned ROM bundles (once)
cargo test -p slopgb-core --lib <module>               # unit tests
cargo test -p slopgb-core --test mooneye               # full mooneye matrix
cargo test -p slopgb-core --test gbtr                  # game-boy-test-roms battery (~4 min)
cargo run -p slopgb-core --example run_mooneye -- <rom> [model]   # single ROM debug
cargo run --release -- game.gb                         # play
```

Parallel cargo runs: set `CARGO_TARGET_DIR=target/<name>` to dodge lock contention.

## Mooneye protocol

Test ends on `LD B,B` (`GameBoy::debug_breakpoint_hit`). Pass ⇔ B,C,D,E,H,L = 3,5,8,13,21,34. Model from filename suffix (see ARCHITECTURE.md §Mooneye). Timeout 120 emulated s.

## State (2026-06-21)

All mooneye green (439/439 rom×model); game-boy-test-roms v7.0 battery green against ratcheted baselines (7041 cases = 6028 pass + 1013 baselined floor); 614 core unit tests. Missing test ROMs skip silently unless `SLOPGB_REQUIRE_ROMS=1` (CI) — run `test-roms/download.sh` first. Six class-F defect cases are exempted (not run): bully ×2 + strikethrough ×2 (defective Hacktix suites) and the scxly/mbc3-tester [Cgb] defective-reference legs — never drop a test SameBoy passes (blargg/mooneye/wilbertpol stay fully asserted).

**SameBoy cycle-exact port — BEGUN (2026-06-21); the half-dot "irreducible" verdict is REVERSED.** The prior "class-A/B floor is irreducible / a CPU-call-stack discriminator no timing model can represent" conclusion was **wrong**. Built SameBoy 1.0.2's headless tester + gap-finder and verified: **SameBoy passes ~420 of our baselined-failing gambatte rows** via a cycle-exact (T-cycle) timing model — including the kernel `m2int_m3stat_1` (renders `3`, non-blank OCR — re-verified this session) AND mooneye `intr_2_mode0_timing` simultaneously, so it is NOT a cross-oracle contradiction, just our whole-dot / tick-then-access model being too coarse. The resolver (no call-stack inspection): SameBoy samples reads at the M-cycle **leading edge** (cc+0, deferred-commit `pending_cycles`) vs our cc+4; keeps a **decoupled `mode_for_interrupt`** field separate from the CPU-visible mode; and fires the mode-2 IRQ 1 dot *before* / the mode-0 IRQ 1 dot *after* their visible edges (a 2-dot swing). The lift is an **atomic multi-session rewrite** (deferred-commit reads + boundary re-derivation land together — intermediate states are RED; recalibrates the cc-phase cluster to SameBoy's frame). **In-emulator the kernel pair now provably SEPARATES** (2026-06-21 #2, instrumented + reverted, `ppu-subdot-ladder.md`): leading-edge reads land m2int's FF41 read at line-1 dot 248 and m0int's at dot 252 — already in the right order — so back-dating the *visible* mode→0 flip into (248,252] (decoupled from the IRQ dispatch, which stays put) makes m2int read 3 ∧ m0int read 0 together. The residual is that the cc+0 read needs the **dispatch** at SameBoy's frame (~248) too; left at our cc+4 dot 254 it mis-frames the canonical mooneye `intr_2_mode0_timing` — so the dispatch reclock (the global ~7000-row rebaseline) is the next coordinated lever, not a missing within-frame coordination. Every shortcut in our cc+4 model is a measured A/B swap (R3 cc+2-read +19/−23; S3-boundary +5/−12; **M0LAG single-speed mode-0 IRQ +1 split +14/−25**; and the **combined kernel fix (IRQ-split + leading-edge FF41 read) +33/−48 that cascade-breaks mooneye2022 + gbmicrotest + wilbertpol + age + 146 golden frames** — both this session). The combo is the conclusive proof: the lift moves the IRQ-dispatch dot (pinned by counter-based gbmicrotest/mooneye2022 tests that an FF41-read-phase change cannot compensate) AND drifts rendered frames, so it is inseparable from the global cc-exact reclock. **Shipped this session (branch-green, byte-identical):** the full source-grounded port spec [`docs/sameboy-port/`](docs/sameboy-port/) (cpu/ppu/slopgb-core maps + `PORT-PLAN.md` staged roadmap + TDD plan) and the validated, inert deferred-commit foundation: [`src/cycle_clock.rs`](crates/slopgb-core/src/cycle_clock.rs) (the CPU-side `pending_cycles` clock, 5 tests pinning SameBoy `sm83_cpu.c` §2.1/§3/§6) + [`src/mode_timeline.rs`](crates/slopgb-core/src/mode_timeline.rs) (the PPU-side decoupled visible/interrupt-mode model, 5 tests pinning `display.c` mode-3 length + the mode-2/mode-0 anchor swing that separates the kernel pair) — both wired at port Stage S2+S3. The finer-resolution event-phase model is the half-dot grid's documented continuation (the class-A lift condition: "re-clock observable event commits to a [finer] grid"). The `event_phase`/`lead_eighths` half-dot scaffold is a correct-but-insufficient approximation, retired at port Stage S7. Continuation roadmap: [`docs/sameboy-port/PORT-PLAN.md`](docs/sameboy-port/PORT-PLAN.md). History + per-lever measurements: [`docs/hardware-state/ppu-subdot-ladder.md`](docs/hardware-state/ppu-subdot-ladder.md).

**PHASE B (the cc-exact reclock) — 4/4 FULL CONFIRM (2026-06-23 #10). All four thesis legs co-hold flag-on; the lone L2 sprite residual is FIXED. Defaults NOT yet flipped — the C-stage rebaseline is the remaining work.** The deferred-commit machine advance (B1), dispatch retime (B2), bare `early_lead`−2 (B3a), the deferred halt-wake `if_late` re-derivation (B4 `m0_halt_hold`, L1) and the sprite read-grid snap (B5, L2) are all flag-gated behind `tier2_reclock` (separate from `leading_edge_reads`; production + the S0 leading-edge specs byte-identical). **The 4/4 triad (both models, flag-on): kernel m2int=3 ∧ m0int=0 ✓ · mooneye `intr_2_mode0_timing` PASS ✓ · gbmicrotest `int_hblank_halt_scx0-7` PASS ✓ · mooneye `intr_2_mode0_timing_sprites` PASS ✓.** Pinned by gbtr `tier2_kernel_pair_matches_sameboy_target` + `tier2_int_hblank_halt_passes_dmg` + `tier2_intr_2_sprites_passes`. **L2 fix (read-grid snap, not the planned "per-config geometry port"):** SameBoy has NO closed-form sprite penalty for this case (`mode3_batching_length`=0 with objects+IRQs → full FSM); the test resolves mode-3 length to whole M-cycles; slopgb's `proj` is a smooth per-X staircase the cc+4 read quantizes correctly (production passes) but cc+0 exposes the sub-M-cycle dispatch phase. Fix: snap the sprite-line dispatch + `vis_early` to the CPU read grid (dot ≡ 0 mod 4) with `early_lead=0` on `n_sprites>0` lines (incl. off-screen X≥168 OAM) — offline-proven 0 fails / 103 rendered configs; the A9 +40 gambatte sprite `m3stat` lift holds flag-on (80/80). Prior-plan corrections (all measured): B2 retime INERT; "dispatch 254→252" wrong-direction; "S5 mode-0-raise −1" REFUTED for int_hblank (#8); the #9 "no uniform offset fits, per-config" L2 diagnosis was right but the resolution is a QUANTIZATION, not an offset table (#10). **C-STAGE STARTED (2026-06-23 #11, THESIS RESULT #11). C0 + C1.1 SHIPPED flag-gated, byte-identical OFF, pushed (`356f45d`, `1e8a9e7`); mooneye flag-on 87/91 → 89/91.** **C0** = deferred-frame DIV/serial phase recalibration (`interconnect/boot.rs`: `div += 4` under `tier2_reclock` at boot hand-off — the post-boot `div_counter` is calibrated for cc+4, the deferred read samples cc+0 one M-cycle early; fixes `boot_div` 9 legs + `boot_sclk_align` 2; `GameBoy::new_with_reclock` enables the flag before boot since the phase is set at construction; pinned `tier2_boot_div_passes`). **C1.1** = `intr_2_mode3_timing` (`mode3_entry_dot` takes NO −4 back-date under tier2 → 84, the flag-off value; LE-only keeps 80; pinned `tier2_intr_2_mode3_passes`). **C1.2** = `lcdon_timing-GS` (4 legs) FIXED + `int_hblank` re-derived for the +4 frame → mooneye flag-on 89/91 → **90/91**. lcdon: 3 legs, the C1.1 pattern (deferred read takes no LE −4 back-date) — glitch mode-3 entry (`stat_irq.rs`), glitch LYC compare (`lyc.rs`), and the bare-line mode-0 EXIT (`mode0.rs` `early_lead` 1→0, the correct unified value: kernel needs only (252,256], lcdon's post-glitch read needs 254). Found+fixed the C0/int_hblank coupling: the +4 DIV (applied at construction by the real flip) shifts the TIMA-counted halt-wake +1, so `m0_halt_hold` base 1→0; the int_hblank pin now `boot_with_reclock` (the set-after-boot path skipped the +4 and mis-validated). 6 gbtr tier2 pins green (incl new `tier2_lcdon_timing_passes`); byte-identical OFF. **C1.3** = `hblank_ly_scx_timing-GS` (4 legs) FIXED → **mooneye CONVERGENCE GATE MET: 91/91 flag-on, zero-SameBoy-drop** (`b60598c`). The S7 sub-M-cycle halt-wake residual: SameBoy's DMG mode-0-halt-wake resumes the CPU at the IRQ's sub-M-cycle clock (CPU M-cycle phase offset from the PPU dot grid by the rise's within-M-cycle position); slopgb's M-cycle-quantized deferred clock collapsed it, so a post-wake LY read straddling the LY-increment read the wrapped line. Fix (`Interconnect::halt_ly_phase`): the mode-0 halt-wake records the carry indexed by the rise's M-cycle phase cc (`HALT_LY_PHASE_BY_CC=[1,2,0,1]`; cc2→2/cc3→0 the geometrically-pinned straddlers); the FIRST post-wake FF44 read back-dates by it (one-shot, so the pre-halt `wait_ly` poll is untouched — why a uniform `vis_ly` back-date fails). int_hblank (TIMA) + intr_2 (mode-2 wake) untouched. 7 gbtr tier2 pins green (+`tier2_hblank_ly_scx_passes`); byte-identical OFF (gbtr 192 + mooneye 91 exit 0). **C1 COMPLETE.** **C2 MEASURED → the flip is BLOCKED on S5 (StatUpdate engine completion), not a clean rebaseline.** Flipped (C1.3): gambatte 422 regr / 239 stale; DMG ground-truth shows ~106 of 114 DMG regr are slopgb-LE BUGS (SameBoy==gambatte, slopgb-leading-edge wrong — window 47/halt 20/enable_display/m1/lycEnable/...), confirmed LE-ONLY (the engine; `frame0_m0irq_count` OFF=90 LE=0). The flip switches `stat_events_tick`→`stat_update_tick` (the incomplete S5 engine). **S5 STARTED (2026-06-23 #11e, `a084116`): first clean slice SHIPPED + the regr triaged + the engine-dispatch core localized.** Built a fast flag-on iteration loop (`tests/gbtr/gambatte_flagon_probe.rs`, `#[ignore]`, env-gated) + a re-measurable convergence gate (`SLOPGB_MOONEYE_RECLOCK` → mooneye 91/91 flag-on). **Triage (DMG 106 regr): 37 are RENDER-frame (LE-pass-tier2-fail = `vis_early`/`early_lead`/C0, isolable) + 69 are ENGINE-DISPATCH (LE-fail = the `stat_update_tick` rising edge dispatches the IRQ at a dot the cc+0 read mis-samples — the hard atomic core).** CGB adds sprites(87, render/L2) + speedchange/dma/serial/tima/lcd_offset(~30, S6/S7 DS) — OUT of S5 scope. **WINDOW slice SHIPPED:** active-window `early_lead` 2→0 (the C1.2 deferred-read law: no `vis_early` anticipation; glitch split out, keeps +2) → gambatte/window 287→301 (+14 both models, 0 pass→fail), 7 pins + 91/91 green. **`frame_m0irq_count=0` mechanism: NOT "engine fires nothing"** — it fires ~144 mode-0 edges/frame, but the cc+0 read-frame doesn't DELIVER them to the CPU (the dispatch↔read-phase miss + the halt `if_late`/`second_half` masking). **Remaining: S5 engine-dispatch core (the 69 LE-fail + CGB; needs dot-level SameBoy `GB_STAT_update` IF-write tracer + a slopgb CPU-delivery trace — principled, no curve-fit; the halt `*_m0stat_*` family = the C1.3 `halt_ly_phase` analogue for FF41-mode), THEN C2 (re-measure + finish the CGB-OCR frame-alignment + baseline genuine floor), C3 (flip), C4 (golden + all-oracle-zero-drop). Defaults NOT flipped.** **S5 #11g (2026-06-24) — the WINDOW family (107, START-HERE) EXHAUSTIVELY measured → #11f's "low-WX over-extends" diagnosis REFUTED → NO clean tier2 window slice (4 mechanisms; offsets bidirectional+non-uniform even within a sub-family). Fresh SBMODE measurement (measurement-frame isolated, not the setup frame #11f read): SameBoy extends ALL 53 DMG window measurement lines to ≈263+SCX&7 (no low-WX no-extend); slopgb UNDER-extends. Mechanisms: (1) wxA6 read-frame/sub-dot wall ~13 (visflip==SBex EXACTLY, S7), (2) normal-window boundary −2 ~7 (needs a vis-HOLD `vis_early` can't provide), (3) late_wy `wy_ok=false` ~16 (render-level WY-latch), (4) late_disable `en=false` aborted-extend ~10 (render-level). Window = the multi-config port the goal scopes (needs a tier2 PARALLEL window-length model + a vis-HOLD primitive — neither exists). Shipped: `render/mode0.rs` visflip + `render/window.rs` winmatch tracers, byte-identical OFF (gated `SLOPGB_S5DBG`); full table `docs/sameboy-port/tools/measurements/window-groundtruth-2026-06-24.md`. ZERO rows shipped, ZERO SameBoy-passing rows touched.** **S5 #11i (2026-06-25) — the "one atomic reclock" framing is REFINED: the C-stage is ≥3 mechanically distinct sub-M-cycle/engine families (direct SameBoy `dc`/sub-dot read-position evidence). (1) READ-OBSERVER (`m2int_m3stat`+window-length+m0enable/vram_m3/oam_access): want-pair reads land at DIFFERENT (cfl,dc) → the read-frame↔boundary reclock, eighth-grid orders ties. (2) WAKE-CLOCK (halt `*_m0stat_*` 32): want-0 AND want-2 reads land at the IDENTICAL `ly2 cfl0 dc0` on SameBoy (mode0 vs mode2) — the read-observer/eighth-grid lever CANNOT separate them (proven, read side identical); lever = sub-M-cycle WAKE clock (IRQ-rise/CPU-resume T-phase). slopgb split = wake before/after `vis_mode`'s dot0-3 line-start mode-0 hold (the SAME hold as #11h m1/lyc root 1); `scx3_3b` want2 is SameBoy-passing → back-date (#11e +11/−3) stays REFUTED. (3) ENGINE-DRIVER (m1/lyc, #11h). Each moves SameBoy-passing rows alone → all land together in the flip; a perfect read-frame reclock STILL leaves halt+m1/lyc red. Measurement only, NO code, byte-identical OFF. `docs/sameboy-port/tools/measurements/halt-vs-m3stat-dc-split-2026-06-25.md` + `atomic-reclock-recipe.md` #11i box.** **S5 #11j (2026-06-25) — MECH 3 ROOT 1 SHIPPED (first C-stage engine CODE since C1.3; flag-gated, byte-identical OFF, gate green; defaults NOT flipped).** The m1 "MISSING re-arm" is a missing LYC **dip**, not a missing mfi edge: SameBoy `SBLEVEL` at ly144 entry does `1->0 lyc_line=0` (release the held ly143 LYC match) THEN `0->1 mfi=1` (mode-1 re-rise → `IF|=2`), restoring the bit the CPU cleared servicing the ly143 LYC-STAT. slopgb held the match latched across line 144's `ly_for_comparison==-1` gap → no dip → the dot-4 mode-1 rise fused into the LYC fall → no edge → `if=01`. Fix (`stat_irq.rs::stat_update_tick`, LE/Tier-2 only): at `line==144 dot==0` drop a held-true LYC match no longer applicable (`lyc!=144`) **iff VBlank armed** (`stat_en & STAT_SRC_VBLANK`) — the VBlank gate is the measured discriminator (VBlank-off rows dip-but-no-rise on SameBoy → leave alone). Family probe (1092 rows flag-on) **774→783 (+9): 16 fixed / 7 moved**; the 16 include the two `lycint_m1intirq_{1,2}` BLANK-OCR rows (#11h loose end — blank *because* the re-arm was missing). The 7 moved are all `want1 got3`: SameBoy fires the SAME ly144 edge (verified 3/7) → dispatch now correct, residual is the deferred-read placement (**mech 1 read-frame**, the convergence trap). New pin `tier2_m1_vblank_rearm_passes` (both models); 8 tier2 pins; mooneye flag-on 91/91; gbtr+mooneye OFF byte-identical. **S5 #11k (2026-06-25) — MECH 3 ROOT 2 SUB-CASE SHIPPED: the line-0 VBlank carry (flag-gated, byte-identical OFF, gate green; defaults NOT flipped).** SameBoy's `stat_interrupt_line` is the SINGLE `mode_for_interrupt`-selected mode source OR LYC (NOT a wired-OR; `display.c:546-556`), and mfi is NEVER re-set between the line-144 entry (`:2215` `=1`) and line-0's OAM step (`:1828` `=2`) — so with VBlank enabled the STAT line stays continuously HIGH across the ly153→ly0 wrap and the line-0 OAM rise raises NO fresh edge (SameBoy `m2m1irq_ifw_2` fires ly1-143, NOT ly0). slopgb's `update_mode_for_interrupt` read `vis_mode` (mode 0 on DMG) across line-0 dots 0-3, dropping the line then re-raising it → a spurious line-0 STAT edge. Fix (`stat_irq.rs::update_mode_for_interrupt`, line-0 `dot<4` arm, LE/Tier-2 only): carry **mode 1 (VBlank)**, not `vis_mode`; CGB already read 1 there → DMG-only change, CGB byte-identical, decoupled from the visible FF41 mode-0. **+4/−0 flag-on, zero SameBoy-passing dropped** (6-family 783→786: `ly0/lycint152_m0irq_1`, `lycEnable/lycwirq_trigger_ly00_stat50_1`, `m2enable/late_m1disable_ly0_3`; non-family DMG +1 `lcdirq_precedence/m2irq_ly00_lcdstat30`). New pin `tier2_line0_vblank_carry_passes` (DMG `lycwirq_trigger_ly00_stat50_1` outE0); 9 tier2 pins; mooneye flag-on 91/91; gbtr+mooneye OFF byte-identical; lib 658. The named target `m2m1irq_ifw_2` DISPATCH now matches SameBoy (spurious ly0 gone) but OCR still `got=3` = the **mech-1 read-frame** residual (convergence trap), NOT root 2. **Remaining S5: mech 3 root 2 LYC-source/late-write sub-case (`lycwirq_*_stat50_2`, `lyc0_late_ff45_enable_3` still E2 — `ly_for_comparison`-wrap + FF45-write-trigger lever) + late-disable + CGB lycwirq; mech 1 read-frame (the 7 moved + m2int_m3stat + window); mech 2 wake-clock (sub-cc). `measurements/m1lyc-ifdelivery-groundtruth-2026-06-25.md` "#11k".** **S5 #11af (2026-06-29, `6e2f101`) — the WINDOW render-level shadow WY-trigger BUILT (the late-WY half of the #11g window model prior sessions parked as "neither exists"); clean +5/−0 SHIPPED flag-gated, defaults NOT flipped, 25 tier2 pins.** SameBoy latches `wy_triggered` from a CONTINUOUS `WY == LY` compare (`display.c` `wy_check`); slopgb's production `wy_latch` samples only the 3 gambatte weMaster dots, so a MID-LINE late-WY write SameBoy catches is MISSED → slopgb renders the line BARE where SameBoy extends mode 3 to `263 + SCX&7` (the POLLED read exit, +0). Fix = a tier2+CGB SHADOW (`Ppu::wy_trig_sb` + `Render::wx_match_dot`, byte-identical OFF) feeding the FF41-read law (`vis_mode_read`) ONLY, NOT `line_render_done`/the render: a sticky `WY == LY` latch + a WX-activation deadline (`wx_match_dot + 2`, the wy2-copy phase slack), firing ONLY for the on-line trigger (`trig_line == ly`). The cross-line `!win_active` latch stays bare (window aborted / WX or LCDC.5 toggled late = SameBoy bare) — the guard that turned a `−13` over-aggressive build into `+5/−0`. Full-CGB two-bin 492→487: `late_wy_10to1_ly1_1` + `FFto2_ly2_1`/`_scx2_1`/`_scx3_1`/`_wx0f_1`. Pin `tier2_window_late_wy_extend_passes`. The other 19 of the 24 non-DS window flip-BUG rows = 4 build-measured atomic mechanisms (boundary-WY deferred-frame phase + shorten-direction over-extend + wxA6 read-frame wall + `scx5_1` deadline near-miss) — full map `measurements/window-render-model-2026-06-29.md`. **S5 #11ag (2026-06-29, `85554ba`) — the WINDOW family ported to DOUBLE-SPEED (both the #11y/#11z length law AND the #11af shadow WY-trigger); clean +8/−0 SHIPPED flag-gated, 26 tier2 pins.** Both `vis_mode_read` laws were `!ds`-gated; under DS the deferred cc+0 FF41 read lands +1 dot vs SS (the ISR read offset is +3 not +4), so the length-law exit is `260 + SCX&7` (`259 + ds`) and the shadow exit `264 + SCX&7` (`263 + ds`), with the shadow deadline slack +4. DS additionally excludes sprite-laden lines from both laws (`!ds || n_sprites == 0` — with sprites the real mode-3 end extends past the bare exit and the DS read frame straddles it; the `10spritesPrLine_wx*_m3stat_ds_1` SameBoy-passes). Full-CGB two-bin 487→479 (7 length-law `m2int_wx*_m3stat_ds_2` + 1 shadow `late_wy_FFto2_ly2_ds_1`). Pin `tier2_window_ds_passes`; mooneye flag-on 91/91 + OFF 91/91; gbtr OFF 211/0 byte-identical; SS byte-identical (`ds` terms 0). The DS remainder (length `_1` scx5 EXTEND / shadow scx5 SCX-non-linear / boundary-WY / shorten-abort) + all 11 cgbpal_m3 = atomic; `cgbpal_m3end` read-collapse DIRECTLY confirmed by decoding SameBoy's framebuffer (`m3end_1`→7 blocked, `m3end_2`→0 accessible, BOTH SameBoy-passes → the #11w `+3` palette release drops a SameBoy-pass). Map `measurements/window-ds-cgbpal-2026-06-29.md`. **S5 #11ah (2026-06-29) — the DEFINITIVE CGB flip-BUG atomic-core inventory + DS S6 batch survey; NO code, byte-identical OFF, 26 pins held, defaults NOT flipped.** Full-CGB two-bin census (ON 479 fail / OFF 486 / `comm` = **185 flip-BUGs**; 112 SS / 73 DS; LE-split **141 ENGINE-DISPATCH + 44 RENDER-FRAME**). **5-class C2-lever partition: RENDER-LENGTH 65 · ENGINE-IF 64 · S6-DS 24 · READ-FRAME 20 · WAKE-CLOCK 12.** **DS S6 batch (32 rows: speedchange 8 / dma 7 / serial 4 / tima 4 / lcd_offset 9) = 0 clean slices, build-measured** (slopgb 3-mode + SameBoy `--cgb --length 4`): all read-frame / S6-conflict / dispatch, none an accessibility window. speedchange = m2int_m3stat read↔boundary + STOP `ly_for_comparison` phase; dma = HDMA-readback / GDMA-cycle S6 conflict table; lcd_offset = #11q/#11r lcd-offset dispatch-COUNT (re-surveyed). **NEW: serial/tima are NOT a can't-fix-both pair — SBREAD shows SameBoy reads the SAME IF bit for both `_1`/`_2` legs (4 dots apart) → `_1`=SameBoy-pass the C0-DIV read-frame regresses, `_2`=gambatte-ref the C2 flip rebaselines.** The slice-by-slice phase is EXHAUSTED; every residual needs a global (deferred read frame / C0 DIV / render mode-3 length) or sub-M-cycle (wake / IF-lifecycle) lever — atomic by the counter-pin entanglement. Next = C2. Inventory + lever spec: `measurements/cgb-flip-bug-atomic-core-inventory-2026-06-29.md`. **S5 #11ai (2026-06-29) — the C2 read-frame co-move BUILT + measured (the goal's C2 START); ESCAPE result, two prior diagnoses CORRECTED; NO code shipped, byte-identical OFF, 26 pins held, defaults NOT flipped.** Baselines: READ-FRAME 20/20 fail, LE-pass 44/44 fail, window 59 fail, mooneye flag-on 91/91. **Option A (PPU +4 machine advance at dispatch, `SLOPGB_C2ADV`) REFUTED — distinct from the #11z' bus.tick di_timing break: mooneye flag-on 89/91, the `intr_2_0/mode0/mode0_sprites/mode3/oam_ok_timing` + `hblank_ly_scx` tests HANG (`B=42`) because the +4 shifts the counter-pinned IRQ DISPATCH dot (hardware-pinned). So the +4 is read-position-ONLY; the dispatch must NOT move; slopgb rigidly COUPLES the ISR read + the dispatch (both whole-dot-PPU-driven), SameBoy DECOUPLES them via the T-granular `read_high_memory` sub-M-cycle sample (S7). Option A also drifts (window 59→90) and its "8 fixed" are OCR-frame DRIFT ARTIFACTS (`serial _2` E8→E0 anti-physical).** **#11ah's "C0 +4 DIV is the serial/tima lever" REFUTED — the `SLOPGB_C0DIV` sweep {−4,0,4,8,12} has ZERO effect; the FF0F-cc+4 read (`SLOPGB_C2IF`) also refuted. Corrected: serial/tima `_1` is the deferred-machine serial/timer COMPLETION frame (the leading-edge FF0F read samples IF as of the PREVIOUS M-cycle; the completion lands 1 M-cycle late), lever = the S6 deferred-completion advance, NOT C0-DIV.** SS read-frame (display_startstate/irq_precedence/m2int_m2irq): 5/6 RENDER-FRAME (global deferred read POSITION; display_startstate reads ly0 dot252 on a NORMAL line where SameBoy reads the GLITCH-line mode 0). **Refined spec: the READ-FRAME class (20) = 3 distinct atomic levers (serial/tima deferred-completion S6 · DS m2int read-grid S7 · SS deferred read-position), none a clean byte-identical slice; the unifying barrier is the read↔dispatch coupling only the S7 sub-M-cycle read clock + S6 completion reconciliation break (landing with the C-stage dispatch rebaseline, RED intermediate).** Map: `measurements/c2-read-frame-comove-built-2026-06-29.md`. **S5 #11al (2026-06-30, `69abb81`+`115b01a`) — the ENGINE-IF class build-measured: 1 clean engine mechanism DRAINED, the rest are READ-FRAME; clean `+3/−0` SHIPPED flag-gated, 26→27 tier2 pins, defaults NOT flipped.** Build-measured all 7 named ENGINE-IF root families (lycEnable/m1/m0enable/m2enable/miscmstatirq/ly0/lyc153int_m2irq) with the 3-mode probe + SameBoy SBLEVEL/SBWRITE/STAT_IRQ. **The clean engine bug = the CGB last-M-cycle LYC-write spurious re-latch** (`reclock.rs::stat_update_tick` `line_start_carryover` → `(line==1 && dot<=2) || (dot>=452 && !ds)`): a late FF45 write in slopgb's leading-edge frame commits 1 M-cycle EARLIER than SameBoy, on the current line's last M-cycle, where the freshly-matching just-written LYC re-latches `lyc_interrupt_line` → a spurious last-dot STAT edge SameBoy never fires (its write lands the NEXT line's `cfl0`, held carryover/`lyfc=-1`). The line-END complement of #11l/#11r's line-START hold (clean because `ly_for_comparison` is FIXED in the last M-cycle — only a fresh write moves the latch, not the lcd-offset edge that keeps the line-START hold REFUTED on CGB lines 2-143). +3/−0: `lyc0_late_ff45_enable_2` + `lyc153_late_ff45_enable_2` + `m2enable/lyc1_m2irq_late_lyc255_2` (all Cgb). SS-only (DS last M-cycle = 2 dots / write offset +1; `dot>=454 && ds` BUILT + measured = breaks `_ds_1`, fixes zero `_ds_2` → the S6 grid, parked). Pin `tier2_lyc_carryover_late_ff45_cgb_wrap_passes`. **The SHARPENED boundary (the #11ae/#11ah verdict refined): the remaining 61 ENGINE-IF are READ-FRAME, NOT a separable IF-lifecycle/blocking-level bug — slopgb and SameBoy fire the SAME edges a few dots apart (the LYC-153 match `dot6`↔`cfl0` pinned by wilbertpol ly_lyc_153-C; bare-line mode-0 `dot254`↔`cfl257`; mode-2 `dot0`==`cfl0`) and the ISR read straddles the gap; the `_2`/`_ifw_2` pairs have IDENTICAL dispatch + OPPOSITE wants = textbook read-frame A/B. The "dispatch-dot-correct/IF-lifecycle-wrong" framing collapses to the dispatch reclock, atomic with the C-stage.** Map: `measurements/engine-if-class-drained-2026-06-30.md`. **S5 #11am (2026-06-30) — the DEFINITIVE C2/C3 flip-blocker classification (ESCAPE; NO code, byte-identical OFF, 27 pins held, defaults NOT flipped).** Fresh full-CGB two-bin (ON 476 / OFF 486 / **183 flip-BUGs**) + `classify_cgb_regr.py` (SameBoy `--cgb --length 4` framebuffer OCR vs `_out<hex>`) splits them **132 SameBoy-PASS (the TRUE flip blockers — forbidden to drop, MUST be fixed) / 51 gambatte-ref (rebaseline-OK)** — sharpening #11ah's "~106 of 114 DMG" into a measured full-CGB classification. The 132 by atomic class: **RENDER-LENGTH 56 · ENGINE-IF 30 · S6-DS 21 · READ-FRAME 13 · WAKE-CLOCK 12** (LE-split 98 ENG / 34 REN; 68 DS / 63 SS). Families in BOTH sets (m1/m0enable/lycEnable/window/serial/tima) = the A/B `_1`-fix/`_2`-rebaseline pairs collapsing to one slopgb deferred read dot. **DECISIVE PROOF the "RENDER-LENGTH" label is partly the −4 ISR read frame, not length:** `window/late_disable_early_scx03_wx0f_1` — slopgb reads FF41 `ly1 dot256`→3 (got=3), SameBoy `ly1 cfl260`→0 (want=0); BOTH render bare with the visible 3→0 exit at the SAME dot (≈257), the render LENGTH is correct, the read lands a uniform −4 (`slopgb_dot = SameBoy_cfl − 4`). **A uniform +4 of read∧visible-boundary is a NO-OP for the FF41 read even WITH the `vis_early`/`vis_hold` dispatch-decoupling** (read-vs-boundary is frame-invariant; the recipe's "cancel" re-confirmed); +4-read-only=cc+4=production (fails kernel); +4-PPU-advance moves dispatch (#11ai); sub-M-cycle read clock is co-temporal (#11ab) — all refuted. **The flip needs BOTH (a) per-config render-length precision (the 56 — the proven `vis_mode_read` window-law template extended to late_disable-abort/sprite/cgbpal/vram/oam/enable_display, buildable row-by-row with SBMODE) AND (b) the architectural ISR-read-POSITION model (the ≈76 read-frame/wake/A-B-`_2` rows: SameBoy's read lands a config-dependent T-count after dispatch, slopgb collapses to −4) — even a perfect length port leaves ≈76 needing the S6/S7 interrupt-service reconciliation. Sharpest lever = the production render mode-3 length port (largest single dent, the only incrementally-buildable class).** Map: `measurements/c2-flip-blocker-classification-2026-06-30.md`. **S5 #11an (2026-06-30, `phase-b-s7` `c911af6`) — the UNIFIED bare-line read-frame law BUILT + REFUTED as a whole-dot lever; the read-straddle is config-dependent sub-M-cycle (ESCAPE; `pixel-pipe-reclock` core byte-identical OFF, defaults NOT flipped).** The #11am "sharpest lever = render-length port" is REFINED: levers (a) render-length + (b) read-frame UNIFY into ONE `vis_mode_read` law. The dual-emulator trace (kernel `m2int_m3stat_1`/`_2` + DS `_2` + `late_disable`, enriched with `win_active`/`vis_early`/`line_render_done` AT the deferred FF41 read) established the exact invariant: **slopgb's deferred FF41 read at dot D reproduces SameBoy `read_high_memory` at cfl D + read_offset (4 SS / 3 DS)**, so the FF41 read returns SameBoy's verdict by comparing D against SameBoy's CONFIG render exit − offset (anchored to SameBoy geometry, NOT slopgb's render — corrects even `late_disable`, where slopgb OVER-extends but SameBoy renders bare 257). Generalizes the shipped window law (`263+SCX&7−offset`) to the BARE exit (`253+SCX&7` SS / `254` DS). **REFUTED full-CGB two-bin (`SLOPGB_BARELAW`): +23 SameBoy-pass fixed / −27 SameBoy-pass DROPPED (`classify_cgb_regr.py` BUG=27 FLOOR=0) — pure curve-fit A/B swap.** The want-pairs collapse to ONE slopgb dot: `m0int_m3stat_ds_1` (want 3) and `m2int_m3stat_ds_2` (want 0) BOTH read slopgb **dot254**, but SameBoy spreads them 9 dots (cfl 250 vs 259) — **the read offset is config-dependent AND opposite-signed (m0int +4 / m2int −5), even diverging by whole LINES (m2int slopgb ly135 / SameBoy ly137)**. mooneye flag-on held 91/91 WITH the law (FF41-register-read-only, never the dispatch). The built read-frame BOUNDARY half is correct; the missing half is the **per-ISR deferred-read T-POSITION** (`interconnect/tick.rs` `advance_machine_t` + `cycle_clock.rs`) landing the read at SameBoy's per-ISR absolute dot — the genuinely-open S6/S7 architectural rewrite, the **single sharpest lever**. Map: `measurements/c2-unified-readframe-law-refuted-2026-06-30.md`. **S5 #11ao (2026-06-30, `phase-b-s7`) — the per-ISR read-POSITION model BUILT (the DS mode-2 dispatch delay) + REFUTED as whole-M-cycle; the separation is SUB-DOT, pinned to scx-PARITY (ESCAPE; `pixel-pipe-reclock` core byte-identical OFF, defaults NOT flipped).** Traced the FULL ISR T-sequence both emulators (new `SLOPGB vec`/`SL2` ↔ SameBoy `SBDISP`/`SBVEC`/`SB2` per-bus-op tracers): the handler is IDENTICAL linear code (STAT vector `jp`, NOP slide, FF41 read), the CPU T-accounting MATCHES (`+18 T` dispatch, 2-dot/M-cycle slide). **#11an's "opposite-signed +4/−5, whole-LINE" was a cfl-vs-dot artifact** — the EXIT-RELATIVE framing (read dot − the line's own `visexit`) cancels the mode-3-length and frame-phase confounds and gives the clean model: slopgb's read−exit is *uniformly* a little less than SameBoy's; the deficit `= read_off − exit_off`, and **the DS mode-2 (OAM-IRQ) handler reads at read_off 4-5 vs the mode-0 (HBlank-IRQ) 2-3 — a 2-dot mode-2-only split** SameBoy doesn't have; `m2int_m3stat_ds_2` is the sole failing m0/m2int-family row (the only one the 2-dot deficit flips across the exit). Built the lever (`SLOPGB_DSM2DELAY`, `stat_irq/reclock.rs::stat_update_halt_masks`): DS-only mode-2 `stat_late` dispatch delay (+1 M-cycle = the +2 dots), SS-EXEMPT (the prior all-speed `stat_late` collapsed the SS kernel; SS needs +2 dots = half an M-cycle). Fixes the want-pair (6/6), holds mooneye flag-on 91/91 (dispatch NOT moved — the IRQ is held from the *sampler* a cycle; DIV/timer untouched). **REFUTED full-CGB two-bin: +29 SameBoy-pass fixed / −26 SameBoy-pass DROPPED (`classify_cgb_regr.py`) — the dispatch-side twin of #11an's read-side BARELAW (+23/−27).** **DECISIVE scx-PARITY signature: FIXED = scx2/4/6/8 (even) `_ds_2`, REGRESSED = scx1/3/5/7 (odd) `_ds_1`** — `scx&1` shifts the mode-3 exit by a HALF-DOT; the +2 whole-dot delay aligns to even-scx exits and over/under-shoots the odd-scx half-dot exits. `DSM2DELAY+BARELAW` does NOT rescue. **CONCLUSION: the per-ISR read-position separation is SUB-DOT (half-dot, scx-parity-pinned); NO whole-dot lever — read-side (BARELAW) or dispatch-side (DSM2DELAY) — can straddle a half-dot exit with a whole-dot read/boundary for both scx parities. The single sharpest lever = the half-dot (8 MHz) PPU clock (the `dot_phase` pixel-pipe reclock `interconnect.rs` already names), co-landing with the per-config render-length port.** Map: `measurements/c2-readpos-dsm2delay-parity-refuted-2026-06-30.md`. **S5 #11ap (2026-06-30, `phase-b-s7` `09a8b6c`) — the HALF-DOT exit BUILT as a clean `scx&1` PARITY term; it RESOLVES the #11ao scx-parity, but the half-dot exit is NOT the blocker — the m0int/m2int read-POSITION COLLISION is, and it is co-temporal (ESCAPE; `pixel-pipe-reclock` core byte-identical OFF, defaults NOT flipped).** Fresh SameBoy half-dot ground truth (rebuilt the tester+tracers after a `/tmp` wipe; `cfl`=DOTS, `dc`=8MHz half-dots, true pos = `cfl*2+dc` — prior sessions read cfl only): the m2int_m3stat DS exit moves with scx (bracketed by the read verdicts), slopgb's native FF41 exit `255+SCX&7` is LINEAR while its deferred DS reads land on EVEN dots, so for EVEN SCX the linear exit is 1 above the read grid → even-scx `_ds_2` reads mode 3 (want 0). **Built the half-dot exit (`SLOPGB_HDEXIT`, `ppu/stat_irq.rs::vis_mode_read`): `exit = 255 + SCX&7 + (SCX&1)` (the `+(SCX&1)` is the half-dot resolution on the even read grid — raise-odd).** Co-landed with `SLOPGB_DSM2DELAY` (the read-position separator) it passes ALL m2int_m3stat DS 16/16 + m0int DS 2/2. **But the two-bin REFUTES it as a clean slice: HDEXIT lower-even alone +12/−8 SameBoy-pass; DSM2DELAY+raise-odd HDEXIT +28/−23 SameBoy-pass** (`classify_cgb_regr.py`). **DECISIVE: the wall is NOT the exit — it is the m0int/m2int read COLLISION:** `m2int_m3stat_ds_2` (want0) and `m0int_m3stat_ds_1` (want3) both land slopgb **dot254 AND the identical sub-M-cycle phase (clk both ≡0 mod 4)** at scx&7=0; SameBoy spreads them 9 dots via the per-ISR DISPATCH phase. **No exit threshold and no read SAMPLE clock can give one dot+phase opposite verdicts** — #11ab (co-temporal) generalized across DIFFERENT ISRs. The only separator (DSM2DELAY dispatch delay) drags the **IF delivery** (m2irq/m2enable) + the deferred read frame (palette/oam/dma) → 23 forbidden drops — the atomic entanglement, not a slice. **The half-dot exit is SOLVED + isolated OUT; single sharpest lever REFINED: the per-ISR deferred FF41-read POSITION DECOUPLED from the IF dispatch** (the deferred clock carries the IRQ-source sub-M-cycle phase into the read while the counter-pinned dispatch dot + IF delivery stay put — the §7.1 `read_high_memory` per-ISR T-position, the S6/S7 atomic reclock; the half-dot PPU tick does NOT crack it — the collision is at the CPU-driven read). Tooling: `docs/sameboy-port/tools/build_sameboy_tracers.sh` (idempotent SameBoy rebuild, survives `/tmp` wipes) + `scratchpad/{hd,sl}_measure.sh`. Map: `measurements/c2-halfdot-exit-parity-built-2026-06-30.md`. **S5 #11aq (2026-06-30, `phase-b-s7`) — the per-ISR read-POSITION CARRY BUILT (decoupled from the IF dispatch, the goal's single sharpest lever); #11ap's "co-temporal/unseparable" verdict REVERSED, the (a)+(b) co-land PROVEN at family level; ESCAPE on the GLOBAL read↔exit consistency. `pixel-pipe-reclock` core byte-identical OFF, defaults NOT flipped.** The CARRY = `Interconnect::dispatch_retime` adds `carry` T of read debt (`CycleClock::carry_read`) AFTER `dispatch_vector_retime` flushed to the IF-ack latch, so the vector fetch + handler reads shift `carry` T later WITHOUT moving the dispatch dot or IF delivery — the decoupling DSM2DELAY's *dispatch* delay could not give (keyed on `Ppu::stat_rise_oam`/`stat_rise_m0`, DS-only, `SLOPGB_M2CARRY`). **DECISIVE MEASUREMENT: the reads were NEVER collapsed** — slopgb base reads `m2int_m3stat_ds_1` dot252 / `_2` dot254 (2 apart); SameBoy cfl256/258; slopgb's whole frame is a uniform **+4 dots EARLIER** (DS mode-2 ISR; `carry_t=8` lands them EXACTLY at SameBoy 256/258, verified) / **+2 dots** (DS mode-0 ISR `m0int` dot254↔cfl256). The only error is the EXIT relative to the reads: slopgb `255+SCX&7` ABOVE both, SameBoy `257+SCX&7(+ds)=258` BETWEEN. **The (a)+(b) co-land PROVEN: per-source carry (mode-2 +4 / mode-0 +2) + ONE SBex exit-hold (`SLOPGB_M2HOLD`, the read_offset-0 BARELAW `257+SCX&7+ds`) converges BOTH the m0int AND m2int m3stat DS families 21/22** (only the separate `late_scx4` mechanism remains). **Why ESCAPE — the GLOBAL consistency barrier:** full-CGB two-bin vs base (mooneye 91/91 EVERY variant — dispatch dot NOT moved, counter-pins intact): carry mode-2 +2dot = +28/−22 · +1dot = +22/−7 · dual-carry+M2HOLD = +22/−50. The M2HOLD blanket exit law drops 50 SameBoy-passes because only the STAT-ISR reads are CARRIED; the non-carried bare-DS reads stay at slopgb's lower frame and the SBex hold mis-frames them. **The lever is no longer "impossible/co-temporal" — it is a validated mechanism (separates the reads without dragging IF, mooneye-neutral) with MEASURED per-source offsets and a family-verified recipe; the single barrier is that all reads + one exit law must land ATOMICALLY (a flag-gated subset is an A/B swap). The sharpest lever = the FULL per-ISR read-position reclock: carry EVERY deferred read by its IRQ-source offset to SameBoy's cfl, then ONE SBex exit law.** Scaffold env-gated, byte-identical OFF, on `phase-b-s7`. Map: `measurements/c2-readpos-carry-built-2026-06-30.md`. **S5 #11ar (2026-06-30, `phase-b-s7` `48498a1`) — the per-ISR read-position PEEK BUILT: the FIRST CLEAN read-position-decoupled C-stage slice (`+6/−0`, PINNED) + the DEFINITIVE per-class read-offset table; ESCAPE on the 132-convergence. `pixel-pipe-reclock` core byte-identical OFF, defaults NOT flipped.** #11aq's `carry_read` machine advance mis-positions every non-m3stat STAT-ISR read (build-measured machine-carry + scoped SBex = `+29/−58`; `M2CARRY` alone breaks all 5 spot-checked m0stat/m2stat/enable/oam reads). The fix (`stat_irq.rs::vis_mode_read`, armed by `dispatch_retime` via `Ppu::read_carried`) is a **transient PEEK** — shift ONLY the FF41 read's mode verdict, no machine advance: `if read_carried && tier2 && cgb && ds && m==3 && !win_active && !win_aborted && !wy_trig_sb && !glitch && ns==0 { off = stat_rise_oam?4:2; return dot+off < 257+scx&7+ds+(scx&1) ? 3 : 0 }`. Three load-bearing guards: `read_carried` (one-shot, scope to the carried ISR read — the #11aq −50 fix), `m==3` (fire only near the m3→0 exit; excludes m0stat/m2stat probing a different boundary — without it `+13/−?`), `!wy_trig_sb` (excludes the co-temporal `late_disable` family, MEASURED `_ds_1`/`_ds_2` read the IDENTICAL `ly1 dot254` opposite-wants — without it `+13/−7` all SameBoy-pass). **THE FULL PER-READ CARRY + ONE SBex EXIT BUILT (the goal's literal "carry EVERY deferred read to SameBoy's cfl, globally consistent") — `+9/−0`, tier2-unconditional** (flag-on 2544→2553). The law (`stat_irq.rs::vis_mode_read`): apply the SBex verdict to EVERY bare mode-3 FF41 read — carried STAT-ISR reads AND polled reads alike — via a transient read-frame offset (a peek, no machine advance → dispatch dot + IF delivery untouched, mooneye 91/91): `off = (read_carried && stat_rise_m0) ? 2 : 4` (the cc+0 leading-edge is 4 dots before SameBoy's cc+4 frame; only the mode-0 HBlank ISR read is +2 — #11aq MEASURED); `verdict = (dot + off) < SBex ? 3 : 0` with `SBex = 257 + SCX&7 + ds + (SCX&1)` parity. **The `POLLED_OFF` sweep is the DEFINITIVE full-carry measurement: +7/−0 at off 0-3, `+9/−0` at the clean plateau off 4-5 (the polled post-DMA reads `gdma/hdma_cycles_ds_2` land at SameBoy's frame), A/B-swapping only at off ≥ 6 (the co-temporal dma `_ds_1` siblings).** This CONVERGES the COMPLETE bare-mode-3 FF41-read set (m2int_m3stat DS `+6` + m2int_m0stat DS line-start mode0→2 `+1` + dma polled `+2`) and NO more — the residual 123 are NOT bare-mode-3 FF41 reads (FF0F IF-delivery / VRAM/OAM/palette accessibility / co-temporal wake), **structurally unreachable by ANY FF41 verdict law**, so the FLIP's ~76-cascade is MEASURED-IMPOSSIBLE via read-position (those need the S4/S6/IF-lifecycle ports). A 6-agent exhaustive per-class two-bin sweep (both emulators, every remaining blocker) proved it: ENGINE-IF(13)/accessibility(8) read FF0F/VRAM/OAM/palette not FF41; WAKE-CLOCK(5) co-temporal (every field identical, `SLOPGB_WAKEPEEK` built +3/−13 A/B); S6-DS speedchange + RENDER-window FF41 rows co-temporal with SameBoy-passing siblings (S6-DS would DROP the shipped pin) = the refuted M2HOLD/BARELAW shape. mooneye flag-on 91/91 + OFF 91/91; gbtr OFF 213/0 (pin `tier2_m2int_m3stat_ds_readpos_passes` covers all 9); byte-identical OFF; clippy clean. **The DEFINITIVE per-class offset table** (`offset = slopgb dot − SameBoy cfl`, 132 blockers): RENDER-LENGTH ff41 `+4`(24 read-pos)/`+0`(8 exit-pos late_wy)/`+18`(7 vram/cgbpal accessibility)/`+8`(glitch); ENGINE-IF ff0f `−8/−4/−16/−12` (IF-delivery, reads LATE); WAKE-CLOCK ff41 `−4`(7 DMG halt m0stat); S6-DS `+2..+7`; READ-FRAME `+4/−2`. **The offset is per-read-CLASS, NOT global** — spans `{−20..+18}`, **opposite-signed by register** (FF41 mode + / FF0F IF-delivery −), varies WITHIN RENDER-LENGTH by line-position; #11aq's "carry EVERY read + ONE SBex" REFUTED at the code level (a uniform carry breaks the other-offset classes). The full-carry law converges all 9 bare-mode-3 FF41-reachable rows (m2int_m3stat DS exit + m2int_m0stat DS line-start flip + dma polled); the residual 123 need distinct render-length/accessibility/IF-delivery/wake-clock/S6 levers (atomic — the exhaustive sweep confirmed every one is either a non-FF41 register or co-temporal with a SameBoy-pass, so no FF41 verdict law reaches them). Next candidate slice = the WAKE-CLOCK DMG halt-wake mode-0 peek (`−4`, the C1.3 `halt_ly_phase` analogue). Map: `measurements/c2-readpos-peek-built-2026-06-30.md` + `c2-readpos-offset-table-2026-06-30.tsv`. **S5 #11as (2026-07-01, `phase-b-s7` `de41a65`) — the RENDER MODE-3 LENGTH port BUILT + build-measured: 1 clean DS accessibility slice (`+1/−0`, pinned) + the DEFINITIVE co-temporal barrier; ESCAPE on the 125-convergence. Core byte-identical OFF, defaults NOT flipped.** Fresh base (HEAD `9811e6d`): flag-on 467 / off 486 → **176 flip-BUGs = 125 SameBoy-pass (must FIX) + 51 rebaseline** (`classify_cgb_regr.py`). **SHIPPED — the DS line-END OAM-read release** (`ppu/blocking.rs::ds_lineend_read_open`, wired into `oam_read_blocked` only): SameBoy releases the CGB double-speed mode-3 OAM read-lock one cycle LATER than SS (skips the `if (!cgb_double_speed)` early unblock `display.c:2104-2111`, drops to `:2118`), landing the deferred cc+0 read's unblock at slopgb dot `254 + SCX&7`; slopgb held to `line_render_done` (+2), so `oam_access/postread_ds_2` (`ly135 dot254`, SameBoy accessible) read blocked. Pin `tier2_oam_postread_ds_passes` (fix + the `_1` blocked regression guard); flag-on 467→466 (+1/−0). **OAM-only — the VRAM twin is CO-TEMPORAL:** `vram_m3/postread_ds_2` (want accessible @dot254) collides with `vramw_m3end/vramw_m3end_ds_2` (want the readback BLOCKED @dot254 — its VRAM write `WR8000 @dot250` costs a CPU M-cycle SameBoy spreads across the readback while slopgb's deferred frame collapses both to dot254), so a VRAM release is an A/B swap (+1/−1). **The DEFINITIVE co-temporal barrier (build-measured both emulators):** the render-length blocker set is dominated by CO-TEMPORAL pairs — `_1`/`_2` read the IDENTICAL slopgb dot + native mode/blocked with OPPOSITE wants, because the discriminating late WRITE (LCDC.5/WX/SCX clear, VRAM write) or a write-instruction's M-cycle cost COLLAPSES in slopgb's deferred clock: **window (26) ALL co-temporal** (`late_disable_scx03_wx0f_1` want0 & `_2` want3 both `dot256 m3`; late_reenable/late_wx/late_scx likewise), **late_scx4 (2) co-temporal**, **enable_display (6)** dispatch-COUNT/palette-VALUE not mode reads, **cgbpal_m3 (7)** DS accessibility co-temporal + m3end (`_1`→7/`_2`→0 both SameBoy-pass, #11ag). **Only `scx_during_m3/scx_m3_extend_ds_1` is SEPARABLE** (mode-3 EXTEND, `_1` dot330 m0 want3 / `_2` dot332 m0 want0) but needs a tier2 SCX-during-m3 vis-HOLD render model (DS, unbuilt, marginal +1). **Per-config render mode-3 length model (SameBoy SBMODE):** bare `257+SCX&7`, triggering-window `263+SCX&7`, **win-abort `257` (DROPS the SCX penalty — a genuine per-config law, but co-temporal/unusable)**, DS OAM read-lock `254+SCX&7`. **VERDICT: the render-length port is fully built + characterized; it drains only the SEPARABLE reads (the +1 OAM this session, on top of the +9 read-position + the window +13). Every other render-length blocker is co-temporal with a SameBoy-pass (A/B swap — the deferred WRITE collapse) or reads a different register (FF0F/palette). The 125-flip needs the atomic sub-M-cycle WRITE/DISPATCH reconciliation (ENGINE-IF 30 + S6-DS 21 + READ-FRAME 13 + WAKE 12 + the co-temporal render-length pairs) — the same S6/S7 rewrite the read-position lever named; NOT a render-length law (built) or a read-position peek (exhausted +9).** mooneye flag-on 91/91 + OFF 91/91; gbtr OFF 213/0 (golden + `tier2_oam_postread_ds_passes` clean, gambatte_matrix byte-identical); clippy `-D` clean. Map: `measurements/c2-render-length-cotemporal-2026-07-01.md`. **S5 #11at (2026-07-01, `phase-b-s7` `dd89941`) — the write-side render-LENGTH lever's FIRST clean slice: the CGB pre-draw window-abort bare-exit (`+4/−0`, pinned) + the #11as "co-temporal write collapse" diagnosis CORRECTED. Core byte-identical OFF, 120 SameBoy-pass blockers (was 124), defaults NOT flipped.** Build-measured the `late_disable` window family on BOTH emulators: **the `_1`/`_2` legs are NOT co-temporal — their LCDC.5-clear WRITES land a whole M-cycle apart (slopgb dot104 vs dot108), which slopgb RESOLVES via `advance_machine_t`.** The collapse #11as attributed to "the deferred write lands both at the same dot" is WRONG — it is the RENDER being insensitive to the disable dot (both legs render mode3 at the shared read dot256; SameBoy reads mode0 for `_1` / mode3 for `_2` at the identical cfl260). The one clean, incrementally-buildable sub-case is the **PRE-DRAW abort** (`win_mode` not yet set at the clear → `window_abort` early-returns): the window disabled before its first fetch renders BARE on SameBoy but with the SCX fine-scroll penalty DROPPED (mattcurrie §WIN_EN) → mode-3 exit cfl257, not 257+SCX&7; slopgb over-extends. Fix: `Render::win_predraw_abort{,_dot}` (flagged in `window_abort`'s `!win_mode` path, tier2+CGB), consumed by `stat_irq.rs::vis_mode_read` (SS, bare non-sprite, `LCDC_WIN_ENABLE==0` [excludes late_reenable], `win_abort_dot <= 105`, `dot+4 >= 257` → mode0). Fixes `late_disable_early_scx03_wx{0f,10,11,12}_1`; pin `tier2_window_predraw_abort_passes`. **Boundary is a FIXED per-config constant (~106 scx03), NOT a function of (abort_dot, scx, read_dot) NOR `wx_match_dot`-relative — REFUTED (`wx_match+1` +6/−4: the abort dot is wx-INDEPENDENT 104/108 while wx_match moves with WX; cross-family non-monotonic — early_scx03 abort104→bare, non-early late_scx0 abort100→extend at the same read).** The post-draw extend / late_reenable / late_wx / late_scx (boundary ~130) / cgbpal / DS-accessibility render-length rows need the per-config window-tile-completion LENGTH model (moves `line_render_done` → the counter-pinned dispatch — the atomic render reclock). **WAKE-CLOCK re-confirmed co-temporal:** the clean `halt m0stat` scx2 pairs read DIFFERENT slopgb dots (`_1` dot4/`_2` dot8) but the family-wide want-0/want-2 split does NOT correlate with the read dot (WAKEPEEK +3/−13) and the mode-0 rise cc is IDENTICAL across legs (same scx→same rise dot), so `halt_mode_phase` (a `halt_ly_phase`-style cc-keyed lever) CANNOT split them; the discriminator is the sub-M-cycle halt-ENTRY/CPU-resume T-phase (SameBoy reads both at `ly2 cfl0 dc0` — same half-dot — returning mode0 vs mode2 from a sub-dc STAT-update T). **DS pre-draw twin is genuinely co-temporal** (both read slopgb dot254 mode3; SameBoy splits sub-half-dot at cfl 516) — the S6-DS grid. gbtr OFF 215/0 (golden + gambatte_matrix byte-identical); mooneye 91/91 ON+OFF; clippy `-D` clean. Map: `measurements/c2-window-predraw-abort-2026-07-01.md`. **S5 #11au (2026-07-01, `phase-b-s7` `9c8420b`) — a SECOND clean write-side render-length slice by the SAME method: the CGB window-REENABLE bare-tail (`+4/−0`, pinned). 124→117 SameBoy-pass blockers (7 fixed across 2 slices, 0 dropped).** `late_reenable` is another mislabeled non-co-temporal pair — the LCDC.5 re-enable WRITE lands a whole M-cycle apart (slopgb `_1` reen92 / `_2` reen96), slopgb resolves it, only the render collapses. A window disabled then re-enabled mid-mode-3 redraws from the re-enable point; its mode-3 extends past the read iff the re-enable beat the WX-match redraw start (`re-enable_dot <= wx_match_dot − 3`, MEASURED uniform: base wxmatch97/boundary94, wx0f wxmatch105/boundary102). Fix: `Render::win_reenable_dot` (latched in `regs.rs::commit_eff` on LCDC 0→1 while `render.active`) → `stat_irq.rs::vis_mode_read` (win_active, native m3, `reen_dot > wx_match−3`, `SCX&7 <= 3`, bare non-sprite → mode0). Fixes `late_reenable_{2,scx2_2,scx3_2,wx0f_2}`; pin `tier2_window_reenable_passes`. SCX&7 ≤ 3 — the fine-scroll shifts the boundary at high SCX (scx5 boundary 98 not 94, MEASURED; scx5+ pass natively) = the atomic reclock's. gbtr OFF 216/0; mooneye 91/91 ON+OFF; byte-identical OFF. **The remaining ~110 residual are atomic: 74 hard-blocked (ENGINE-IF 30 counter-pinned dispatch · S6-DS 20 sub-half-dot · READ-FRAME 12 S6-completion · WAKE-CLOCK 12 sub-M-cycle wake) + ~36 harder render-length (late_wy WY-latch · cgbpal palette-reg · late_scx boundary-constant · DS sub-half-dot · post-draw extend). The two LCDC-toggle slices are the write-side lever's clean extent; the flip needs the atomic dispatch/wake/completion reclock (moves the counter-pinned dispatch — the multi-session rewrite).** Detail: [`docs/hardware-state/ppu-subdot-ladder.md`](docs/hardware-state/ppu-subdot-ladder.md) "THESIS RESULT #11" / "#11e" / "#11g" / "#11h–#11k" / "#11af" / "#11ag" / "#11ah" / "#11ai" / "#11al" / "#11an" / "#11ao" / "#11ap" / "#11aq" / "#11ar" / "#11as" / "#11at" / "#11au" / "#11av (halt_mode_phase built+refuted, WAKE atomic)" / "#11aw (late-WY un-trigger +3, immediate-WY shadow; 124→115)" / "#11ax (2026-07-01, ESCAPE — SS render-length slice method DRAINED: 7 families build-measured atomic; barrier SHARPENED to sub-T operation-ORDER [`late_scx4` both legs half-dot 520, `halt *_m0stat` both `ly2 cfl0 dc0` — half-dot clock necessary-but-insufficient, the discriminator is read↔STAT_update↔wake ORDER in SameBoy's per-8MHz-tick loop]; cgbpal atomic PROVEN via new SameBoy palette ground truth [offset0 blocked@176 / offset1 accessible@174, slopgb read frame INVERTED; `SLOPGB_PALLOCK84` A/B +4/−2 both SameBoy-pass dropped]; only new code = two `SLOPGB_S5DBG` palette tracers, byte-identical OFF; 115 blockers UNCHANGED; defaults NOT flipped)" / "#11ay (2026-07-01, ESCAPE — the #11ax 'sub-T co-temporal / identical-half-dot' barrier REFUTED by a genuine absolute monotonic clock `fp = absolute_debugger_ticks − display_cycles`; the 3 flagship 'atomic' pairs SEPARATE 2–386 half-dots: `late_scx4` = RENDER-LENGTH [reads 2 apart, mode-3 FLIP 8 apart — slopgb renders both legs long, no read-law reaches it], `halt *_m0stat` = sub-M-cycle WAKE [reads 8 apart, slopgb quantizes the wake onto OPPOSITE wrong dots ly2 dot0/dot4], `cgbpal_m3end` = READ-FRAME [reads 386 half-dots=193 dots apart]; `cfl*2+dc` LIED because it resets per line + is conserved across the flip [cfl+=k, GB_SLEEP dc−=2k]; the goal's half-dot advance + exact-T reads is NECESSARY *and* plausibly SUFFICIENT, correcting #11ax's 'insufficient'; still a multi-session atomic rewrite [render-length moves the counter-pinned dispatch]; NO slopgb-tree code, `fp` banked into `build_sameboy_tracers.sh` SBMODE/SBREAD/SBPALR — USE `fp`, never `cfl*2+dc`; `measurements/c2-absclock-cotemporal-refuted-2026-07-01.md`)" / "#11az (2026-07-01, ESCAPE — the half-dot pixel-pipe reclock SPECIFIED from the current code state as a constructive executable build plan [`docs/sameboy-port/HALFDOT-BUILD-PLAN.md`], NOT built; base `6f375fe` byte-identical, mooneye flag-on 91/91, 27 pins green, defaults NOT flipped. Re-confirmed base [455/486 → 115 SameBoy-pass + 50 rebaseline] + extracted the definitive SameBoy per-tick spec [PPU = divisor-2 8 MHz coroutine, dot-rate fixed across speed; FF41 read = `GB_display_sync` 0-cycle force-run to the exact half-dot → `STAT&3`; `STAT&3`/`mode_for_interrupt` two latches 1 dot apart, mode 3 never sources STAT IRQ; mode-2 IRQ −1 / mode-0 IRQ +1 dot swing; mode-3 length emergent `pos==160`, closed-form `167+SCX&7` trivial] + re-proved atomicity from code [`late_scx4`: render-length ALONE and read-position ALONE each an A/B swap, BOTH jointly required; generalises 5 classes × 3422 rows, offsets `{−20..+18}` opposite-signed by register; dispatch counter-pinned]. The plan maps the spec onto the exact transformation: `render_step`→half-dot pipe, the `early_lead` case-tower + the SEVEN accreted `vis_mode_read` shadow laws collapse into ONE half-dot visible-mode boundary, `read_deferred`→exact-half-dot sync, boundary-constant re-derivation table, per-class landing, multi-session staging. `measurements/c2-halfdot-build-plan-2026-07-01.md`)" / "#11ba (2026-07-02, BUILT — Part A-infra the half-dot GRAIN LANDED byte-identical, `phase-b-s7` `5622329`, docs `pixel-pipe-reclock` `8e8fcbb`; FIRST structural reclock code since C1.3). The PPU advances in 8 MHz half-dots on the tier2 deferred path: `Ppu::tick_half()`+`dhalf` (2 half-dots/dot SS, 1 DS; first half no-ops, second runs the whole-dot `tick()` body → aligned run byte-identical); `advance_machine_t` drives it, `tick_machine_dot`'s fold split to a shared `fold_ppu_events(ppu_if,cc)` called only on the dot-completing half — reproducing the `dot_ticks_on_cc` grid exactly. Production (`tick_machine`→whole-dot `tick`) untouched; `dhalf`/`sub_dot()` (the Part B seam) 0 flag-off. Gate ALL-GREEN: lib 660; 32 tier2 pins; mooneye flag-on 91/91; gbtr OFF 217/0 (prod byte-identical + 146 golden); flag-on two-bin ON 455 / OFF 486 (= base, UNCHANGED); clippy `-D` clean. **DS m3stat convergence PINNED empirically (dual-trace `fp`):** `late_scx4_ds` = mode-3 LENGTH differs (SCX write cfl89/91 straddles the fine-scroll sample; slopgb collapses both legs → `dot256`/`visexit254`; SameBoy flip at `cfl257 dc=2` half-dot, reads 2 fp apart) = the half-dot write-strobe+fine-scroll lever; `m2int_scx4_ds` = read-position, same exit, slopgb ALREADY separates (`dot256`/`258`). Direct confirmation of §5 atomicity: the LENGTH lever lives below the whole-dot grid. NEXT = the coupled half-dot render pixel-pipe + write-strobe + read on top of the grain (§6 step 3, `late_scx4` first). Defaults NOT flipped.)".

**Per-subsystem hardware-behavior notes — timing laws, quirks, the test ROMs that pin each, and the parked/disproven approaches not to re-chase — live in [`docs/hardware-state/`](docs/hardware-state/README.md) (one file per subsystem). Read the relevant file before touching that subsystem.** The floor-class index (classes A–H with lift conditions) is the header of `tests/gbtr/baselines/gambatte.txt`.
